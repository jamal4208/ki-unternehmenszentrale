# KI-Unternehmenszentrale – Einstieg für Jamal

## Status

- **V7.9.9 – Auftragsbezogene Dateiauswahl auf die Nutzerperspektive erweitert (lokal umgesetzt, getestet sowie im Browser und im echten Praxislauf durch Jamal abgenommen):** die Drei-Agenten-Kette konnte bisher nur aus vier überwiegend technischen Dateien wählen und ein Auftrag wie „Bewerte die KI-Unternehmenszentrale aus Sicht eines zukünftigen täglichen Nutzers" konnte deshalb im Wesentlichen nur die Kettenmechanik beurteilen; die serverseitige Allowlist `CHAIN_SELECTABLE_FILES` wurde daher **ausschließlich additiv** um `pilot-work-order-ui.js` (sichtbare Bedienung), `V1_BETRIEBSHANDBUCH.md` (versprochener Betriebsablauf) und `pilot-work-order-routes.js` (bedienbare Schnittstelle) auf sieben Einträge erweitert – `pilot-work-order-service.js` war bereits enthalten –, und das Cockpit wählt für eine neue Kette jetzt deterministisch genau die vier für die Nutzerperspektive empfohlenen Dateien vor (`pilot-work-order-ui.js`, `V1_BETRIEBSHANDBUCH.md`, `pilot-work-order-service.js`, `pilot-work-order-routes.js`) statt wie bisher alle auswählbaren Dateien; kein bestehender Eintrag wurde entfernt, weil die *gespeicherte* Auswahl jeder Kette bei jeder Ansicht und jedem Stufenstart erneut gegen genau diese Liste geprüft wird und ein Entfernen bestehende Ketten unlesbar gemacht hätte; die Auswahl bleibt eine enge, geschlossene, serverseitige Allowlist mit relativen Pfaden – **keine freie Pfadeingabe, keine Wildcards, kein Verzeichnis, kein rekursiver Pfad, keine Freigabe des Repositories, kein clientseitig vertrauenswürdiger Dateipfad, kein Zugriff auf `.env`/`.git`/`node_modules`/`data`, keine Datenbank, kein Health-Repository und keine Auswahl durch ein Sprachmodell**; jede Auswahl wird unverändert serverseitig geprüft (Pfad außerhalb der Liste → HTTP 400), eine Kette kann weiterhin nicht ohne mindestens eine ausgewählte Datei vorbereitet werden, die fixierte Auswahl bleibt nach der Vorbereitung sichtbar, alle drei Stufen erhalten exakt dieselbe Auswahl, bestehende Ketten bleiben unverändert lesbar, und weder die Vorauswahl noch ein Klick auf eine Datei löst eine Vorbereitung, Freigabe oder Ausführung aus; keine Migration, keine neue Route, keine Änderung der Kettenstatusmaschine, V7.9.8 (Ergebnisbudget 4500 gespeichert, Rohannahme 12000, technische Grenze 6000, keine Kürzung im Satz) fachlich unverändert. `npm run check` und der vollständige `npm test` sind grün; **V7.9.9 ist zusätzlich praktisch abgenommen** – im Cockpit waren genau sieben Dateien auswählbar, genau die vier empfohlenen waren vorausgewählt und gekennzeichnet, die drei technischen abgewählt, ein Ab- und erneutes Anwählen startete keine Aktion, ohne ausgewählte Datei war die Kettenvorbereitung gesperrt beziehungsweise wurde klar abgewiesen, eine neue Kette wurde mit genau den vier empfohlenen Dateien vorbereitet, die Auswahl blieb für alle drei Stufen fixiert und sichtbar, jede Stufe startete erst nach einer eigenen ausdrücklichen Jamal-Freigabe, alle drei Stufen waren erfolgreich und keine startete automatisch, es trat kein `RESULT_TOO_LARGE` auf, das Ergebnis war fachlich deutlich stärker auf Bedienbarkeit, Verständlichkeit, Sicherheit und Alltagstauglichkeit ausgerichtet, die Kette wurde vollständig mit einem strukturierten PM-Gesamturteil samt priorisierten Entscheidungen und Empfehlung an Jamal abgeschlossen, die alte gescheiterte V7.9.7-Kette blieb unverändert als historisches Beweisstück sichtbar und es wurde kein Auftrag automatisch abgeschlossen, keine externe Aktion ausgelöst und kein Repository verändert (Beleg: Auftrag `pilot-order-a78d6a76-51ad-4770-ac2c-3df5d40f6bae`, Kette `pilot-agent-chain-e5b928e1-1a61-477b-a90b-dc9e01622228`). Kein Deployment.
- **V7.9.8 – Ergebnisbudget für die Recherche- und die Projektmanager-Stufe technisch erzwungen (lokal umgesetzt, getestet, vor Commit/Push gestoppt):** fünf von zwölf bestehenden Ketten waren an `RESULT_TOO_LARGE` gescheitert – drei früher in Schritt 2 (6731, 6360, 7684 Zeichen, Anlass für V7.8.1), eine in Schritt 3 mit 6002 Zeichen und der erste echte Drei-Agenten-Praxislauf in Schritt 1 mit 7584 Zeichen, weil der V7.8.1-Ausgabehaushalt ausschließlich für Schritt 2 galt; Schritt 1 und Schritt 3 besitzen jetzt jeweils einen eigenen, maschinenlesbaren Fünf-Abschnittsvertrag im Prompt (Recherche: Kurzfazit, belegte Kernbefunde, Reibungsverluste, priorisierte Verbesserungen, Grenzen und Unsicherheiten; Projektmanager: Gesamturteil, wichtigste belegte Stärken, wichtigste belegte Schwächen, priorisierte Entscheidungen, Empfehlung an Jamal) und die Zentrale verdichtet das Ergebnis nach dem Lauf deterministisch und idempotent auf maximal 4500 gespeicherte Zeichen – ausschließlich durch Weglassen ganzer Punkte oder ganzer Sätze in fester Reihenfolge, niemals innerhalb eines Satzes, ohne Auslassungszeichen, jede Weglassung gezählt, auditiert und im Cockpit je Stufe benannt sichtbar; die technische Grenze bleibt unverändert 6000 Zeichen (durch Tests geschützt), ein strukturloses oder trotz vollständiger Reduktion zu großes Ergebnis wird kontrolliert abgelehnt statt abgeschnitten (nichts gespeichert, keine Folgestufe gestartet), die Sicherheitsprüfung auf behauptete verbotene Aktionen läuft weiterhin vor der Verdichtung auf dem Rohtext, jede Stufe braucht unverändert eine eigene Jamal-Freigabe und `COMPLETED` entsteht erst nach einem regulären Schritt 3; keine Migration, keine neue Route, Schritt 2 bleibt mit der V7.8.1-Logik unverändert, budgetkonforme Antworten bleiben byteidentisch. `npm run check` und der vollständige `npm test` sind grün; die Browser-/Praxisabnahme wurde in einem echten Drei-Agenten-Praxislauf erfolgreich durchgeführt (alle drei Schritte erfolgreich, Kette `COMPLETED`, kein `RESULT_TOO_LARGE`, vollständige Ergebnisse und vollständige Vorgängerübergabe, korrekte Verdichtung ohne abgeschnittenen Satz, keine automatische Folgeaktion); V7.9.9 war zu diesem Zeitpunkt noch nicht umgesetzt und ist inzwischen lokal umgesetzt (siehe Eintrag oben).
- **V7.8.1 – Ergebnisbudget von Schritt 2 technisch erzwungen (lokal umgesetzt, vor Commit/Push gestoppt):** der Dokumentations-Agent überschritt in drei echten Browserläufen die technische Ergebnisgrenze (6731, 6360, 7684 Zeichen, jeweils `RESULT_TOO_LARGE`), weil im Codex-Aufrufpfad kein Output-Budgetparameter existiert und Promptvorgaben die Modellausgabe nicht begrenzen; Schritt 2 liefert jetzt fünf maschinenlesbare Abschnitte und die Zentrale verdichtet das Ergebnis nach dem Lauf deterministisch auf maximal 4500 gespeicherte Zeichen – ausschließlich durch Weglassen ganzer Punkte oder ganzer Sätze, niemals innerhalb eines Satzes, jede Weglassung gezählt, auditiert und im Cockpit sichtbar; die technische Grenze bleibt unverändert 6000 Zeichen, ein trotz Reduktion zu großes oder strukturloses Ergebnis wird kontrolliert abgelehnt statt abgeschnitten (nichts gespeichert, Schritt 3 nicht gestartet); keine Migration, keine neue Route, Schritt 1 und Schritt 3 unverändert. `npm run check` und `npm test` sind grün; der echte Referenzlauf 4 wurde inzwischen vollständig bis Schritt 3 ausgeführt, Schritt 2 blieb durch technische Normalisierung sicher im Übergabebudget und blieb ohne `RESULT_TOO_LARGE`.
- **V7.8.0 – Drei-Agenten-Kette auftragsfähig (lokal umgesetzt, vor Commit/Push gestoppt):** der unveränderte Kernauftrag (Titel, Ergebniswunsch, Qualitätskriterien) wird jetzt in allen drei Kettenstufen mitgeführt; Jamal legt die Dateiauswahl im Cockpit vor dem Vorbereiten der Kette einmal für alle drei Stufen fest; Altketten ohne fixierte Auswahl werden explizit als solche markiert; Vorgängertexte werden nicht mehr still gekürzt (kontrollierter Abbruch bei Überschreitung der Read-only-Grenze) und eine Überlänge ist bereits vor dem Start sichtbar und sperrt die betroffene Stufe; das Cockpit zeigt Auftrag je Stufe, tatsächlich verwendete Dateien und Vorgänger-Vollständigkeit; Migration 24 ergänzt die nötigen Metadaten. Die auftragsfähige Drei-Agenten-Kette ist im echten Referenzlauf vollständig bis Schritt 3 gelaufen; persönliche Freigabe je Stufe und der Read-only-Rahmen bleiben unverändert. `npm run check` und `npm test` sind grün.
- **V1 lokal fertig und betriebsbereit**
- Version: **V7.0 Phase A – Guided Work Foundation** (umgesetzt, abgenommen, gesichert mit Commit `4a74ebe`)
- **V7.0 Phase B – Betriebsstabilität** ist umgesetzt, getestet, browserseitig abgenommen und mit Commit `3487a84` gesichert
- **V7.0 Phase C – Execution Bridge Isolation mit Mock-Executor** ist umgesetzt, getestet, abgenommen und mit Commit `0858b4e` gesichert; Mock-Executor ist keine KI; Apply nur Fixture, Health-Apply blockiert
- **V7.0 Phase D – Codex als kontrollierten Executor anbinden** ist umgesetzt, getestet, echter Codex-Fixture-Pilot bestanden und mit Commit `6553452` gesichert; Codex läuft ausschließlich isoliert gegen das Fixture-Repository, Codex-Start und Apply für Health bleiben hart blockiert
- **V7.0 Phase E – Health-Ende-zu-Ende-Abnahme und Freeze-Kandidat** ist umgesetzt, getestet und mit Commit `52ce012` gesichert; read-only V7.0-Freeze-Status (`IN_REVIEW|FREEZE_CANDIDATE|FROZEN`)
- **V7.0 ist mit Jamals ausdrücklicher Entscheidung vom 25.07.2026 offiziell `FROZEN`** (Basis `52ce012`); Phase A bis E abgeschlossen; keine neuen Funktionen mehr in V7.0; neue Funktionen erst ab V7.1 nach separater Freigabe
- **V7.1 Phase A – Dokumenten-/Wissenseingang, Werkzeug-/Lizenzregister, Plugin-Gateway** ist umgesetzt und mit 661 automatisierten Prüfpunkten grün getestet (inkl. Nachbesserung aus der ersten manuellen Safari-Abnahme: Checkbox-Layout und strukturierte Blockierungsantwort), **committed und gepusht mit Commit `59f985f`**
- **V7.1 Phase B – HeyGen als erster kontrollierter Medien-Connector** ist umgesetzt (`CONTROLLED_CONNECTOR_HANDOFF`: die Zentrale bereitet nur ein geprüftes Auftragspaket vor, der echte HeyGen-Renderlauf würde – falls überhaupt – über einen externen Connector nach separater Freigabe laufen); kein API-Key gespeichert, keine Veröffentlichung, kein automatischer Start; **committed und gepusht mit Commit `ff43089`**
- **V7.1 Phase B.1 – HeyGen-Pilot abgeschlossen, Agenturbetrieb mandantenfähig vorbereitet:** der bereits extern erfolgreich ausgeführte erste reale HeyGen-Pilot (fiktives Café, keine Veröffentlichung) ist strukturiert als Pilot-Review dokumentiert; additiv eine kleine Mandantenbasis (zwei neutrale Testkunden, keine echten Kundendaten), verpflichtende Kunden-/Marken-/Kampagnenbindung je Videoauftrag, eine fünfte getrennte Freigabestufe (Kundenentwurf) und eine Ergebnisrückführungs-Statuskette bis zur getrennten Kundenfreigabe (Veröffentlichung bleibt strukturell unerreichbar); Kunden erhalten weiterhin keinen HeyGen-Zugang; mit 929 automatisierten Prüfpunkten grün getestet; **committed und gepusht mit Commit `37e8a28`** (HEAD und `origin/main` stehen exakt darauf)
- **V7.1 Phase C – Canva als zweiter kontrollierter Design-Connector:** Canva ist – nach demselben Muster wie HeyGen – als `CONTROLLED_CONNECTOR_HANDOFF` vorbereitet; die Zentrale erstellt ein geprüftes Designauftragspaket (Briefing, Marken-/Assetrechte, Kunde-/Marke-/Kampagnenbindung, sieben getrennte Freigabestufen), die echte Canva-Designerstellung würde – falls überhaupt – über einen externen, authentifizierten Canva-Connector nach separater Freigabe laufen; kein API-Key gespeichert, keine Veröffentlichung, kein automatischer Start; Kunden erhalten weiterhin keinen Canva-Zugang; HeyGen und Shopify sind unverändert; mit 1165 automatisierten Prüfpunkten grün getestet; **committed und gepusht mit Commit `52b2d02`** (HEAD und `origin/main` stehen exakt darauf)
- **V7.1 Phase C.1 – realen Canva-Pilot kanonisch abgeschlossen, Kundenfeedback-Schleife vorbereitet:** ein realer, außerhalb des lokalen Servers durchgeführter Canva-Pilot (fiktiver Testkunde, Marke „Café Amore“, Instagram-Post „Sonntagsfrühstück“, Design-ID `DAHQeIjc2ls`, als bearbeitbares Canva-Design gespeichert, `NOT_BILLABLE_TEST`) ist jetzt als kanonische Pilot-Ergebnisakte mit Jamals internem Review dokumentiert; eine kontrollierte Kundenfeedback-Schleife (Feedback erfassen, Änderungen anfordern, erneut prüfen, getrennte Kundenfreigabe) ist vorbereitet, Veröffentlichung bleibt strukturell nicht freigegeben; kein Kundenzugang zu Canva, kein Social Posting, keine Kosten; **committed und gepusht mit Commit `6621d93`** (gemeinsam mit Phase C.1.1)
- **V7.1 Phase C.1.1 – Reviewmodell skalierbar gemacht:** Jamal ist kein verpflichtender Prüf-/Freigabeengpass mehr. Eigenprojekt → Owner-Review; Standardkunde → Agenten-QS + Kunden-Selbstprüfung; Premiumkunde → Agenten-QS + optionales menschliches Review + Kundenfreigabe; Risikofall → Eskalation. Café-Amore bleibt `OWNER_REVIEW`. Mehrsprachigkeit nur als Leitplanke (DE zuerst; später PT/EN/ES/FR lokalisiert) – nicht implementiert. **committed und gepusht mit Commit `6621d93`**
- **V7.2 Phase A Schritt 1 – Auth-Kern und persistente Identitätsschicht:** neue, vollständig gekapselte `auth.sqlite` (Nutzer, Sessions, Mandanten, Reset-Tokens, Audit-Log), Passwort-Hashing über `crypto.scrypt`, 256-Bit-Sessiontokens; noch keine Route-Gates, kein Login. **committed und gepusht mit Commit `e2cd018`**
- **V7.2 Phase A Schritt 2 – Route-Gates, Auth-Routen und Owner-/Customer-Trennung:** jede Route hat jetzt eine explizite Zugriffspolitik; im Produktivmodus sind alle bestehenden Chef-Routen und -Assets nur mit Owner-Login erreichbar, Execution ist zusätzlich vollständig deaktiviert; neue Anmelde-/Abmelde-/Sessionstatus-/Passwort-Reset-/Einladungsrouten (`/api/auth/*`); Jamals lokaler Chef-Modus bleibt im Entwicklungsmodus auf Loopback weiterhin ohne Login nutzbar (sichtbare Konsolenwarnung); noch **kein** Kundenportal, keine kundenbedienbare Fachroute; mit 1450 automatisierten Prüfpunkten grün getestet; **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**
- **V7.2 Phase A Schritt 3 – Deutsches Kundenportal und Owner-Verwaltung:** Jamal kann als Owner über eine neue Verwaltungsseite (`/owner/kunden`) Mandanten aktivieren/suspendieren und Kunden einladen/sperren/reaktivieren/Sitzungen widerrufen; eingeladene Kunden aktivieren ihr Konto über einen Einladungslink (`/portal/einladung`), melden sich an (`/portal/login`) und sehen ausschließlich ihre eigene, schlichte Portalstartseite (`/portal`) mit Konto-/Sitzungsinformation und einem ehrlichen Bereitschaftsstatus – ausdrücklich noch **ohne** Fachaufträge, Werkzeuge oder Veröffentlichung; Passwort-Reset (`/portal/passwort-vergessen`, `/portal/passwort-neu`) nutzt ausschließlich die bereits in Schritt 2 gesicherten Auth-Routen; neuer lokaler Owner-Bootstrap-Befehl `npm run auth:bootstrap-owner`; jede neue Route und jedes neue statische Asset ist explizit klassifiziert; mit 1535 automatisierten Prüfpunkten grün getestet (Baseline 1450 + 85 neue); **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**; bereit zur manuellen Abnahme durch Jamal (siehe `CURRENT_STATUS.md`)
- **V7.2 Phase A Schritt 4 – Betriebs-, Sicherheits- und Produktabnahme der Portalbasis:** Gesamtabnahme von Schritt 1–3 (kein neuer Funktions-Sprint); ein echter Sicherheitsbefund gefunden und behoben (Owner-Benutzeraktionen konnten ohne Rollenprüfung fremde OWNER-/SUPPORT-IDs adressieren, jetzt generisch `404` mit Regressionstest); drei neue Akzeptanztestdateien (Sicherheit/Betrieb/Bedienbarkeit, +62 Prüfpunkte, 0 neue Routen); vollständige manuelle End-to-End-Abnahme mit zwei echten, isolierten Testservern (Dev/Prod) und zwei Mandanten real durchlaufen (33/33 Schritte grün); mit 1597 automatisierten Prüfpunkten grün getestet; dokumentierte, bewusste Restlücken ohne akute Sicherheitsrelevanz (In-Memory-Ratenlimiter, offener Backupweg/Löschkonzept für die Server-Auth-Datenbank); **Phase-A-Urteil: mit klar benannten Restgrenzen abgenommen und commitbereit**; **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**; vollständiger Bericht: `V7_2_PHASE_A_ACCEPTANCE.md`
- **V7.2 Phase B Schritt 1 – Erste echte Kundenfachfunktion (Arbeitsauftrag anlegen, automatisch prüfen, Status verfolgen), PRODUKTKORRIGIERT:** Die KI-Unternehmenszentrale ist als Self-Service-Agentenzentrale gebaut – der OWNER ist **kein** fachlicher Prüfer normaler Kundenprojekte und **kein** Pflichtschritt. Kunden (`CUSTOMER_ADMIN`/`CUSTOMER_USER`) können erstmals über `/portal/auftrag-neu` einen einfachen Arbeitsauftrag (Titel, gewünschtes Ergebnis, optional Hintergrund/Zeitpunkt) anlegen; die Zentrale prüft automatisch die Vollständigkeit und setzt selbst `READY_FOR_PROCESSING` oder `NEEDS_CLARIFICATION`, der Kunde verfolgt den Status auf der Portalstartseite bzw. unter `/portal`, ergänzt bei Bedarf und kann seinen eigenen Auftrag stornieren; der Owner sieht mandantenübergreifend alle Aufträge unter `/owner/auftraege` nur als Betriebsübersicht und greift ausschließlich in Ausnahmefällen ein (Sicherheit, Missbrauch, rechtliche Sensibilität, außergewöhnliche Kosten, technische Blockade, explizite Eskalation) über genau zwei Aktionen: Eskalieren oder Stoppen – **keine reguläre Owner-Freigabe/-Ablehnung/-Rückfrage mehr**. Statusmodell in Schritt 1: `DRAFT → SUBMITTED → READY_FOR_PROCESSING/NEEDS_CLARIFICATION` (automatisch), `ESCALATED`/`CANCELLED` (terminal) als Ausnahmen; vier weitere Statuswerte für spätere Schritte (automatische Agentenübergabe, echte Kundenfreigabe eines Ergebnisses) bereits vorbereitet, aber unerreichbar; strikte Mandantenisolation (Tenant ausschließlich aus Session, generisches `404` bei fremder/unbekannter Auftrag-ID); neue Migration 8 (`work_orders`-Tabelle) sowie acht neue Audit-Ereignistypen (automatische Entscheidungen mit `actorUserId: null`); **ausdrücklich noch keine automatische Ausführung, kein Agentenstart, keine Veröffentlichung, kein Billing**; mit 1693 automatisierten Prüfpunkten grün getestet (Baseline 1597 + 96 neue, inkl. Produktkorrektur); End-to-End-Verifikation des Selbstbedienungs-Flusses vollständig gegen den echten `server.js#requestHandler` mit isolierter Testdatenbank durchgeführt; **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**
- **V7.2 Phase B – Schutz- und Einwilligungsgrundlage:** vor dem Commit der obigen Self-Service-Korrektur ergänzt: eine verbindliche Business-Use-Policy (`BUSINESS_USE_POLICY.md`, ausschließlich legitime geschäftliche Nutzung, verbotene Nutzung wird blockiert), ein kleines lokales Safety-Gate (`business-use-policy.js` – **keine** vollständige KI-Moderation, sondern eine konservative, testbare Vorprüfung mit `ALLOW`/`BLOCK`/`ESCALATE`), eine additive Verstoß-/Sanktionsgrundlage (Migration 9, `policy_violations`, bei besonders schwerem Verstoß automatischer Sessionwiderruf und Markierung zur sofortigen Betreiberprüfung – **kein** automatischer Lizenzentzug) sowie ein Avatar- und Persönlichkeitsrechte-Grundsatz (`AVATAR_CONSENT_POLICY.md`: kein realistischer Avatar einer echten Person ohne deren direkte, dokumentierte, widerrufbare Zustimmung; das bestehende `avatarConsentConfirmed`-Feld reicht dafür ausdrücklich **nicht**; Connys Testfall bis Ende August 2026 als Referenz geplant, noch nicht begonnen). Jamal bleibt durchgehend Plattformbetreiber, kein fachlicher Pflichtprüfer. Keine fertigen AGB, keine abgeschlossene Rechtsprüfung, keine neue Avatar-Funktion, keine Agentenausführung; **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**
- **V7.2 Phase C Schritt 1 – Kontrollierte Übergabe an die Agentenzentrale und erstes prüfbares internes Ergebnis:** ein Auftrag mit Status `READY_FOR_PROCESSING` kann jetzt erstmals über eine kontrollierte, ausschließlich dem Owner vorbehaltene technische Startaktion (`POST /api/owner/work-orders/:id/run` – „Technischen Agentenlauf starten“, **keine** fachliche Freigabe) an die interne Agentenzentrale übergeben werden. Ein Projektmanager-Agent strukturiert den Auftrag deterministisch aus dem bestehenden kanonischen 25-Agenten-Register, wählt bis zu drei passende Fachagenten und genau einen Qualitätsagenten, arbeitet rein intern/textbasiert (kein externer Provideraufruf, kein Canva, kein HeyGen) und erzeugt ein prüfbares, unveränderliches internes Ergebnis (Titel, Zusammenfassung, vollständiger Text, Qualitätsstatus, offene Punkte). Vor der Ausführung greift das Safety-Gate ein zweites Mal (`BLOCK`/`ESCALATE` verhindern jeden Lauf vollständig); erkennt der Projektmanager eine echte fachliche Lücke, wird der Auftrag stattdessen `NEEDS_CLARIFICATION` mit konkreter Rückfrage; bei einem technischen Fehler bleibt der Auftrag kontrolliert `READY_FOR_PROCESSING` (später erneut startbar) statt fälschlich `RESULT_READY`. Der Kunde sieht ein `RESULT_READY`-Ergebnis im Portal ausschließlich zur Ansicht – **noch keine Freigabe, keine Änderungsanforderung, keine Veröffentlichung, kein Billing**; der Owner sieht nur eine technische Betriebsübersicht (Laufstatus, Agenten, Zeiten, Fehlercode) ohne fachliche Freigabefunktion. Neue Migration 10 (`work_order_runs`, `work_order_run_agents`, `work_order_results`, alle append-only); neun neue Audit-Ereignistypen; keine neue Route auf oberster Ebene (alle neuen Endpunkte hängen in den bestehenden Prefix-Dispatchern, Routenzahlen unverändert: 70 GET/52 POST/8 GET-Prefix/4 POST-Prefix/27 statische Assets); mit **1829** automatisierten Prüfpunkten grün getestet (Baseline 1749 + 80 neue, real und zweifach isoliert nachgemessen – eine zwischenzeitliche Fehlbehauptung von 1849 wurde widerlegt und von Jamal korrigiert); **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**
- **V7.2 Phase C Schritt 2 – Kundenänderungsrunde, Versionierung und echte fachliche Kundenfreigabe:** der Kunde kann ein `RESULT_READY`-Ergebnis jetzt entweder mit einer konkreten Änderung zurückgeben (`POST /api/portal/work-orders/:id/change-request` – kontrollierter, synchroner Revisionslauf über den unveränderten Orchestrator aus Schritt 1, erzeugt eine neue, unveränderliche Ergebnisversion, alte Versionen bleiben lesbar unter `GET .../result-versions`) oder fachlich freigeben (`POST .../approve` → `CUSTOMER_APPROVED`). **Ausschließlich der Kunde** entscheidet – der Owner hat weder eine Freigabe- noch eine Änderungsanforderungsroute und sieht unter `/owner/auftraege` ausschließlich zwei neue, rein lesende Bereiche (Änderungswünsche, Ergebnisversionen/Freigabestatus); eine Kundenfreigabe löst **keine** Veröffentlichung, kein Billing, keine externe Provideraktion aus. Drei Safety-Gates sichern den Änderungswunsch/Revisionslauf/die Ergebnisübernahme ab; neue Migration 11 (`work_order_change_requests`, `work_order_customer_approvals`) erzwingt Idempotenz (höchstens ein aktiver Änderungswunsch je Auftrag, keine doppelte Freigabe derselben Version) und Unveränderlichkeit per Datenbank-Constraint/-Trigger, real gegen die Datenbank verifiziert. Neun neue Audit-Ereignistypen; keine neue Route auf oberster Ebene (Routenzahlen unverändert: 70 GET/52 POST/8 GET-Prefix/4 POST-Prefix/27 statische Assets); mit **1935** automatisierten Prüfpunkten grün getestet (Baseline **1829**/65 Testdateien real und zweifach isoliert nachgemessen + 48 Prüfpunkte/2 Testdateien aus der Umsetzung + weitere 58 Prüfpunkte/2 dedizierte Testdateien `work-order-approval.test.js`/`work-order-version-security.test.js` aus dem letzten Qualitätsabgleich vor Commit); **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**
- **V7.3 – Jamal-Arbeitsmodus („Heute arbeiten“):** interne Produktivitäts- und Bedienbarkeitsphase, **keine** Kundenportal-Erweiterung, **kein** neuer externer Provider, **kein** Canva, **kein** HeyGen, **keine** Veröffentlichung, **kein** Billing. Ganz oben im Cockpit steht jetzt genau eine zentrale Arbeitskarte „Heute arbeiten“ (Leitprinzip „Oben arbeiten. Unten nachschauen.“): Jamal gibt ausschließlich das gewünschte Ergebnis ein (Projekt/Hinweise/Zeitpunkt optional, keine Agenten-/Tool-/Statuswahl), ein priorisiertes Projekt ist bereits vorausgewählt (Kontinuität → Standardfall Zentrale selbst → kompakte Auswahl), der bestehende Projektmanager-Orchestrator strukturiert Ziel, Arbeitsschritte, Fachagenten (max. 3) und genau einen Qualitätsagenten (dieselbe Phase-C-Architektur, dasselbe Safety-Gate, keine zweite Laufarchitektur) und erzeugt eine unveränderliche Ergebnisversion. Genau eine primäre Hauptaktion je verständlichem deutschem Status (`Noch nicht gestartet…Eskalation nötig`); Rückfragen nur bei echtem fachlichen Bedarf (max. eine Hauptfrage); Jamal entscheidet nur noch `Passt` (intern erledigt, keine externe Aktion), `Änderung anfordern` (neue Version, alte bleibt unverändert lesbar), `Später` oder `Stoppen`. Der bisherige Tageslauf bleibt vollständig erhalten, ist jetzt aber einklappbar unterhalb der neuen Karte; technische Details (Agenten-IDs, Laufplan, Versionszähler) liegen ausschließlich in einem einklappbaren Diagnosebereich. Erste Selbstverbesserungsaufgabe real durchlaufen: der Auftrag „Vereinfache den täglichen Arbeitsmodus … innerhalb von fünf Sekunden“ wurde selbst durch Projektmanager-Agent, Content-Agent und QA-Agent bearbeitet (Ergebnis `PASSED`/`PASSED_WITH_NOTES`) – das Ergebnis dieser Selbstverbesserung ist exakt die hier beschriebene neue Arbeitskarte. Drei neue Dateien (`jamal-work-mode.js`, `jamal-work-mode-ui.js` + drei Testdateien), eine neue GET-Route und ein neuer POST-Aktions-Prefix (Routenzahlen: 71 GET/52 POST/8 GET-Prefix/5 POST-Prefix/28 statische Assets); mit **2008** automatisierten Prüfpunkten grün getestet (Baseline **1935**/69 Testdateien + 73 neue Prüfpunkte/3 neue Testdateien); **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**. Nächster sicherer Schritt: kontrollierte externe Werkzeugnutzung (weiterhin nach separater Freigabe).
- **V7.3 Persistenznachtrag (vor Commit-Freigabe geschlossen):** der Jamal-Arbeitsstand lag zunächst bewusst nur im Prozessspeicher – ein Serverneustart hätte Ergebniswunsch, Status, Rückfrage, Agentenauswahl, Arbeitsplan und alle Ergebnisversionen ersatzlos gelöscht (echter Betriebsblocker). Behoben durch eine additive **Migration 12** (`jamal_work_items`, `jamal_work_results`, Ergebnisse per Datenbank-Trigger **append-only**) auf der bestehenden `auth-db.js`-SQLite-Schicht sowie ein neues, reines Übersetzungsmodul `jamal-work-mode-store.js`; `auth-db.js` bleibt einziges Modul mit direktem `better-sqlite3`-Import, **kein** LocalStorage, **keine** neue npm-Abhängigkeit, Migrationen 1–11 byteidentisch unverändert. Ergebniswunsch, Status, offene Rückfrage, Agentenauswahl, Arbeitsplan, alle Ergebnisversionen und der `Erledigt`/`Passt`-Status überstehen einen Serverneustart nachweislich (automatisierte Tests + manuelle Neustart-Abnahme auf einem isolierten Testserver). Ehrliche Grenze: **keine** Cluster-/Mehrbenutzer-Schreibgarantie – bei zwei gleichzeitig schreibenden Prozessen gilt das einfache SQLite-Last-Writer-Wins-Prinzip, für Jamal als einzigen Nutzer dieses Arbeitsmodus eine bewusst akzeptierte Grenze. Neue Testdatei `jamal-work-mode-persistence.test.js` (+18 Prüfpunkte); mit **2026** automatisierten Prüfpunkten grün getestet (**73** Testdateien); Routenzahlen unverändert; `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert; **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**.
- **V7.4 – Canva-Produktionskorridor abgeschlossen (Offline-/Stub-Grundlage committed `f2b0909` + authentifizierter externer Canva-Abnahmelauf dokumentiert):** der Offline-/Stub-Korridor ist committed und gepusht. Da aus Cursor kein authentifizierter Canva-Zugriff verfügbar war, wurde der reale Abnahmelauf außerhalb von Cursor über Jamals eigene ChatGPT-Canva-Integration durchgeführt: mehrere Vorschau-/Generierungsrunden, genau ein dauerhaft übernommenes finales Design (Canva-Design-ID `DAHQkWMxdPo`, „AI Enterprise Headquarters in Twilight"), keine Veröffentlichung, kein Social-Media-Posting, kein Billing – als Arbeitsgrundlage, kein finales Corporate Design; vollständige Dokumentation: `CANVA_AUTHENTICATED_RUN_ACCEPTANCE.md`. Die direkte automatisierte Rückführung eines echten Providerergebnisses in die lokale Datenbank bleibt offen.
- **V7.4 Schritt 1 – Kontrollierte externe Werkzeugnutzung: Canva als erster Produktionskorridor – Offline-/Stub-Grundlage (kein echter Canva-Handoff in diesem Schritt):** ein interner Jamal-Arbeitswunsch (`RESULT_READY`/`DONE`) kann jetzt kontrolliert in einen Canva-Produktionsauftrag übersetzt werden – **ausschließlich für Jamals internen Arbeitsmodus/Owner**, **keine** Kunden-Self-Service-Nutzung, **kein** Kundenzugriff auf Jamals Canva-Konto. Ablauf: die Zentrale prüft die Canva-Eignung (`CANVA_RECOMMENDED`/`CANVA_OPTIONAL`/`CANVA_NOT_SUITABLE`/`CANVA_BLOCKED_BY_POLICY`, erneute eigenständige Business-Use-Policy-Prüfung als zweite Sicherheitsebene), erzeugt ein strukturiertes Briefing (Ziel, Zielgruppe, Format, Text, visuelle Richtung „Apple statt Dubai“, Marke, Bildhinweise, Qualitätskriterien) und prüft Bild-/Markenrechte sowie Consent bei realen Personen (`CLEAR`/`UNCLEAR`/`BLOCKED`, `AVATAR_CONSENT_POLICY.md` bleibt verbindlich) – bei ungeklärten Rechten **kein** Handoff. Erst nach Jamals expliziter, serverseitig auditierter Freigabe (`Canva-Produktion starten`, mit vollständiger Vorschau von Auftrag/Format/Text/Bildern/Rechtestatus) startet ein kontrollierter (Stub-/Fixture-fähiger) Connectoraufruf; das Ergebnis (Design-Titel, geprüfter Edit-/View-Link, Providerstatus) wird zurückgeführt und von einer deterministischen internen Qualitätsprüfung bewertet (`PASSED`/`PASSED_WITH_NOTES`/`REVISION_REQUIRED`/`BLOCKED`). Jamal entscheidet danach nur noch `Passt` (`ACCEPTED_INTERNAL`) oder `Änderung anfordern` (neue Briefingversion/Revisionsnummer, alte Revision bleibt nachvollziehbar, jeder weitere Lauf braucht erneut eine explizite Freigabe). **Keine** automatische Veröffentlichung, **kein** Auto-Post, **kein** Social-Media-Endpunkt, **kein** Billing – `publicationLocked` bleibt strukturell immer `true`, es existiert keine Publish-/Social-Media-Route. Neues additives Datenmodell (**Migration 13**, `jamal_canva_productions`, Migrationen 1–12 unverändert; keine Zugangstoken/Provider-Secrets in der Datenbank) und zehn neue, eng allowlisted Audit-Ereignistypen (`CANVA_BRIEFING_PREPARED` … `CANVA_HANDOFF_BLOCKED_BY_RIGHTS`); vier neue Module (`jamal-canva-briefing.js`, `jamal-canva-production-service.js`, `jamal-canva-routes.js`, `jamal-canva-ui.js`) – bewusst **niemals** von `jamal-work-mode.js`/`jamal-work-mode-ui.js` referenziert, damit der bestehende Jamal-Arbeitsmodus-Hauptfluss ohne jeden Canva-Bezug bleibt; eine neue GET-Route (`/api/jamal-work-mode/canva-state`) und sechs neue Aktionen im bestehenden POST-Aktions-Prefix (Routenzahlen: **72 GET/52 POST/8 GET-Prefix/5 POST-Prefix/29 statische Assets**); mit real gemessenen **2136** automatisierten Prüfpunkten in **76** Testdateien grün getestet (3 neue Testdateien `jamal-canva-production.test.js`/30, `jamal-canva-security.test.js`/31, `jamal-canva-ui.test.js`/29 = **90** neue Prüfpunkte; die zuvor dokumentierte V7.3-Baseline nannte 2026/73 – die reale Nachmessung der 73 unveränderten Bestandstestdateien ergibt **2046**, eine historische Dokumentationsabweichung von 20 Prüfpunkten vor diesem Schritt, unabhängig von den hier vorgenommenen Änderungen; keine Assertion wurde entfernt oder abgeschwächt, siehe Abschlussbericht); `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert; **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**. Echter Canva-Lauf in diesem Schritt **nicht** durchgeführt (kein authentifizierter Canva-Connector-Zugriff aus dieser Umgebung verfügbar) – der Offline-Stub-/Fixture-Korridor ist vollständig fertiggestellt und abnahmebereit; der reale Canva-Connectoraufruf bleibt ein separater, klar benannter externer Schritt über Jamals bereits bestehende authentifizierte Canva-Verbindung. Nächster sicherer Schritt: Abnahmeentscheidung durch Jamal, danach ggf. weitere kontrollierte Werkzeuge.
- **V7.5 – Agentenorganisation, tägliches HR-Coaching und Technologie-/Plugin-Marktradar:** eine dauerhafte Führungs- und Entwicklungsstruktur für alle 25 bestehenden Agenten, abgeleitet ausschließlich aus dem bestehenden, unveränderten `agent-registry.js` (keine zweite Agentenliste). Neue Organisationssicht (zehn Zielbereiche, Führungsebene, Berichtslinie, Autonomierahmen, Werkzeugfähigkeit je Agent); persistenter täglicher HR-Entwicklungslauf mit exakt 25 rollenbezogenen 1%-Vorschlägen (Trainingsziel, Übung, Qualitätskriterium, möglicher Spielraum, Risiko/Grenze, HR-Empfehlung `KEEP_CURRENT`/`TRAIN_FIRST`/`RECOMMEND_SMALL_EXPANSION`/`REDUCE_SCOPE`/`ESCALATE`) – vollständig lokal-deterministisch, idempotent, revisionsfähig, **niemals** automatisch autonomieverändernd (auch eine Genehmigung markiert ausschließlich den Prüfstatus, nie eine Berechtigung). Neues lokales Technologie-/Plugin-Marktradar (aus `tool-registry.js` befüllt, u. a. GitHub/Airtable/Canva/HeyGen/Vercel/Google Workspace/Notion/n8n/Slack/HubSpot/Webflow/Lexoffice/Make als bekannte interne Kandidaten) mit Empfehlungsstufen `WATCH`…`BLOCKED` – **keine automatische Webrecherche, keine Verbindung, keine Installation**; dazu eine kontrollierte Agent-Technology-Fit-Zuordnung (Nutzen, Einsatzfall, Testvoraussetzung, Priorität). Neue Führungsansicht „Agenten führen" (Kopfübersicht + fünf Bereiche `Organisation`/`Heute entwickeln`/`Autonomie prüfen`/`Technologie-Radar`/`Agenten & Werkzeuge`, progressive Offenlegung statt 25 gleichzeitiger Karten, feste Sicherheits-/Autonomiehinweise). Additive **Migration 14** (vier neue Tabellen, Migrationen 1–13 unverändert), fünf neue OWNER_ONLY-GET-Routen und ein neuer POST-Aktions-Prefix (Routenzahlen: **77 GET/52 POST/8 GET-Prefix/6 POST-Prefix/30 statische Assets**), neun neue datensparsame Audit-Ereignistypen. Drei neue Testdateien (`agent-leadership.test.js`/26, `agent-leadership-security.test.js`/33, `agent-leadership-ui.test.js`/28 = **87** neue Prüfpunkte); mit real gemessenen **2223** automatisierten Prüfpunkten in **79** Testdateien grün getestet (Baseline **2136**/76); `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert; **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**. Keine automatische Autonomieänderung, keine Plugininstallation, keine externe Recherche – Jamal bleibt alleinige Entscheidungsinstanz.
- **V7.5 – Unternehmensleitlinien V1.0 als verbindliche Betriebslogik verankert:** die gemeinsam beschlossenen Unternehmensleitlinien sind jetzt offizielle **Version 1.0** (`COMPANY_PRINCIPLES.md`, Status „verbindliche Arbeitsgrundlage", nicht unveränderlich, künftige Änderungen nur versioniert) plus maschinenlesbares Modell `company-principles.js` (identische Version, keine zweite Dokumentationswahrheit) – und wirken direkt in der Betriebslogik statt nur als Textseite: sechs Führungsrahmen (Covey/Rosenberg/1%-Methode/PDCA/Hochzuverlässigkeit/strategische Vorausschau), zehn Grundwerte, elf Sicherheitsplanken in eigenen Worten. HR-Vorschläge um 14 Felder erweitert (Beobachtung→Bedeutung→Empfehlung→Entscheidung nach Rosenberg, 1%-Schritt, Messkriterium, Sicherheitsgrenze, PDCA-Stufe, Owner, Nutzenbereich) – kein Floskel-Coaching, keine unbelegte Fehlerbehauptung, stattdessen „Entwicklungspotenzial"/„präventiver Trainingsfokus" ohne echte Daten. Minimaler echter PDCA-Zyklus (`PLAN`→`DO`→`CHECK`→`ACT`, kein automatischer Fortschritt, kein Stufensprung). Neue Hochzuverlässigkeits-Signale (`agent-reliability-signal-service.js`, Tabelle `agent_reliability_signals` – keine automatische Sanktion, keine Autonomiereduktion). Technologie-Radar um Zeithorizont/Unsicherheitsgrad/drei Szenarien/heutigen Vorbereitungsschritt erweitert (keine Marktbehauptungen erfunden, keine Installation ausgelöst). Nutzenbereiche (`benefitArea`) und Prioritäten (`NOW`/`NEXT`/`LATER`/`WATCH`, höchstens drei UI-Hinweise – „weniger beginnen, Wichtiges zuverlässig abschließen"). Ergebnisverantwortung (genau ein Owner je Aufgabe). Finance-/Controlling-Gruppe bleibt ehrlich leer und als `CAPABILITY_GAP` markiert (kein 26. Agent). UI „Agenten führen" zurückhaltend erweitert (Leitlinienversion, Führungsfokus, HR-/Radar-Detailfelder, progressive Offenlegung, keine Designüberarbeitung). Ausschließlich additive Erweiterung der bestehenden **Migration 14** (11+11 neue Spalten, eine neue Tabelle) – **keine Migration 15**, Migrationen 1–13 unverändert. Zwei neue read-only OWNER_ONLY-GET-Routen (`.../company-principles`, `.../reliability-signals`) und vier neue Aktionen im bestehenden POST-Aktions-Prefix (Routenzahlen: **79 GET/52 POST/8 GET-Prefix/6 POST-Prefix/30 statische Assets**), fünf neue datensparsame Audit-Ereignistypen. Eine neue Testdatei `company-principles.test.js` (**51** neue Prüfpunkte) plus 15 neue HTTP-Prüfpunkte in `agent-leadership-security.test.js`; mit real gemessenen **2269** automatisierten Prüfpunkten in **80** Testdateien grün getestet (Baseline **2223**/79); `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert; **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**. Keine automatische Autonomieänderung, keine externe Recherche, keine Plugininstallation.
- **V7.6.1 – Apple-first/Google-controlled Office-, Google-Workspace- und Finance-Korridor vollständig offline vorbereitet:** die verbindliche Zielarchitektur ist jetzt vollständig lokal vorbereitet, **ohne** jede echte externe Verbindung. Apple bleibt Jamals persönlicher Arbeitsraum, Google Workspace (`office@jacogbr.de`, noch lokal nicht nachweisbar eingerichtet – `ACCOUNT_PREPARED_EXTERNALLY_OR_PENDING_CONFIRMATION`) wird der künftige kontrollierte Unternehmensraum (Systemlandkarte: `APPLE_GOOGLE_OPERATING_MODEL.md`). Neues lokales Identitätsmodell (drei Startidentitäten, kein Passwort/Token, kein Agentenlogin) und 33 statische Google-Workspace-Fähigkeiten (acht Berechtigungsstufen, aktueller Zustand überall `DISCONNECTED`, maximaler lokaler Zielstatus `PREPARE_DRAFT`; Freigabematrix: `GOOGLE_WORKSPACE_APPROVAL_MATRIX.md`). Neues Office-Agenten- und Office-Auftragsmodell mit vier vollständig lokalen Offline-Korridoren (E-Mail-Entwurf, Kalender-Entwurf, Dokument-Entwurf, Kontaktsuchauftrag) – technisch hart begrenzt auf `WAITING_FOR_AUTHENTICATION`, **kein** echter Provideraufruf. Neuer Finance-/Controlling-Handoff-Korridor **ohne** vollwertigen Buchhaltungsagenten – die Finance-Capability-Gap bleibt ehrlich sichtbar, **keine** Buchung, **keine** Zahlung, **kein** Rechnungsversand (Freigabematrix: `FINANCE_HANDOFF_APPROVAL_MATRIX.md`). Deterministischer Offline-Stub-Connector ohne Tokens/OAuth/Netzwerkaufruf. Additive **Migration 15** (drei neue Tabellen, Migrationen 1–14 unverändert). Neue Ansicht „Office & Finanzen" in der Oberfläche. Neun neue OWNER_ONLY-GET-Routen plus ein neuer POST-Aktions-Prefix (Routenzahlen: **88 GET/52 POST/8 GET-Prefix/7 POST-Prefix/31 statische Assets**), neun neue datensparsame Audit-Ereignistypen. Zwei Aktivierungs-Checklisten für spätere, separat mit Jamal freizugebende Schritte außerhalb von Cursor: `V7.6_GOOGLE_WORKSPACE_ACTIVATION_CHECKLIST.md`, `V7.6_FINANCE_ACTIVATION_CHECKLIST.md`. Drei neue Testdateien (`office-finance.test.js`/32, `office-finance-security.test.js`/57, `office-finance-ui.test.js`/26 = **115** neue Prüfpunkte); mit real gemessenen **2384** automatisierten Prüfpunkten in **83** Testdateien grün getestet (Baseline **2269**/80); `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert; **committed und gepusht mit Commit `5b59b17`** (HEAD und `origin/main` stehen exakt darauf). Keine Google-Anmeldung, kein OAuth, keine Mail, kein Termin, keine Drive-Datei, keine Kontakte, keine Apple-Migration, keine Buchung, keine Zahlung, keine Rechnung.
- **V7.6.2 – Projekt-Finish-Portfolio, interne Referenzläufe und verkaufsfähige Marketingagentur vorbereitet:** reiner Planungs- und Entscheidungsstand, **keine neue Implementierung**. Alle relevanten eigenen Projekte außerhalb der Zentrale (Health Upgrade Kompass, Marketing Agentur OS, Expansion-/Export-App, Sprachtrainer, Karriere-/Static-Sites, persönliche Werkzeuge) sind read-only erfasst und bewertet; verbindliche Abschlussreihenfolge: 1. Health Upgrade Kompass bis `REFERENCE_READY`, 2. Marketing Agentur OS bis `SELLABLE_PILOT_READY`, 3. Expansion-/Export-App-Trennungsentscheidung. 25 Agenten bleiben unverändert bestehen, kein Agent 26 beschlossen. Details: `PROJECT_FINISH_PORTFOLIO.md`, `PROJECT_FINISH_DECISION_BOARD.md`, `HEALTH_REFERENCE_FINISH_PLAN.md`, `MARKETING_AGENCY_SELLABLE_PILOT_PLAN.md`, `INTERNAL_PROJECT_REFERENCE_PLAN.md`, `HR_FUTURE_AGENT_DECISION_AGENDA.md`. **Committed und gepusht mit Commit `c9635a5`** (HEAD und `origin/main` stehen exakt darauf).
- **V7.6.3 – Health Upgrade Kompass als ersten echten Referenz-Arbeitslauf verankert:** die Unternehmenszentrale führt den Health Upgrade Kompass erstmals als echten, kontrollierten End-to-End-Projektlauf – **keine** Health-Funktion implementiert, **keine** Health-Datei geändert, Health-Repository ausschließlich read-only über das bestehende `health-repo-status.js` gelesen. Kanonischer Referenzlauf `health-reference-work-run-v1` (Titel „Health Upgrade Kompass bis zum abnehmbaren Referenz-Walkthrough führen"), Status startet `PREPARED_FOR_EXECUTION`. Agentenzuteilung ausschließlich aus dem bestehenden 25-Agenten-Register: Projektmanager-Agent (Hauptverantwortlicher), Produktmanager-Agent/Entwickler-Agent/Content-Agent (drei Fachagenten), QA-Agent (Qualität/Sicherheit) – **kein neuer Agent, kein Agent 26**. Sieben feste, nur vorbereitete Arbeitspakete (`HEALTH_BASELINE_CONFIRMATION` … `JAMAL_FINAL_ACCEPTANCE`), sieben Jamal-Freigabepunkte, elf Statuswerte, wobei `REFERENCE_READY` **ausschließlich** nach Jamals ausdrücklicher Bestätigung (`confirmed: true`) erreichbar ist – kein automatischer Sprung, grüne Tests allein reichen nicht. Der Projektmanager-Agent bereitet aus einem Arbeitspaket einen standardisierten Cursor-Übergabevertrag als reinen Textentwurf vor (Projektpfad, Health-Branch/-HEAD, erlaubter/verbotener Umfang, Nicht-Ziele, Testanforderungen, Sicherheitsgrenzen, `autoCommitAllowed: false`, `autoPushAllowed: false`) – **keine automatische Ausführung**, Jamal genehmigt manuell. Additive **Migration 16** (vier neue Tabellen, Migrationen 1–15 unverändert), eine neue OWNER_ONLY-GET-Route und ein neuer POST-Aktions-Prefix (Routenzahlen: **89 GET/52 POST/8 GET-Prefix/8 POST-Prefix/32 statische Assets**), neun neue datensparsame Audit-Ereignistypen. Neue kompakte Cockpit-Karte „Health-Referenzlauf" (Grundsatz „Oben arbeiten. Unten nachschauen.", keine Schaltfläche führt eine echte Health-Ausführung/Commit/Push aus). Drei neue Testdateien (`health-reference-work-run.test.js`/22, `health-reference-work-run-security.test.js`/25, `health-reference-work-run-ui.test.js`/21 = **68** neue Prüfpunkte); mit real gemessenen **2454** automatisierten Prüfpunkten in **86** Testdateien grün getestet (Baseline **2386**/83); `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert; **lokal umgesetzt, aber noch nicht committed, gepusht oder deployt**. Details: `HEALTH_REFERENCE_WORK_RUN.md`. Nächster Schritt (separate Freigabe erforderlich): Jamal prüft und genehmigt das erste ausführbare Health-Arbeitspaket.
- Gesicherter vorheriger Ausgang: **V6.46.0** / `e611c9c`
- Betrieb nur lokal auf diesem Mac – kein Cloud- oder Deploymentbetrieb

## Projektordner

`/Users/jamal/Documents/New project/ki-unternehmenszentrale`

## Normaler Start

**Empfohlen (zeigt vorher den Status, verhindert doppelte Server):**

1. Terminal öffnen
2. In den Projektordner wechseln:
   `cd "/Users/jamal/Documents/New project/ki-unternehmenszentrale"`
3. Status prüfen: `npm run central:status`
4. Server starten: `npm run central:start`
5. Safari öffnen
6. Adresse aufrufen: `http://127.0.0.1:4173/`

**Einfacher Fallback (weiterhin gültig):**

1. Terminal öffnen
2. In den Projektordner wechseln
3. Server starten: `npm start`
4. Safari öffnen, Adresse aufrufen: `http://127.0.0.1:4173/`

## Server sauber beenden

- Bei Start über den Controller: `npm run central:stop`
- Bei Start über `npm start`: im Terminal, in dem der Server läuft, **Ctrl + C**

## Hinweis bei EADDRINUSE

- Port **4173** wird bereits benutzt.
- **Keinen zweiten Server starten.**
- Zuerst `npm run central:status` ausführen: zeigt Port, PID, Version, Commit, Startzeit und genau einen sicheren nächsten Schritt.
- Der Controller beendet **niemals automatisch** einen fremden Prozess, nur weil der Port belegt ist.
- Nur wenn nötig: den bestehenden, tatsächlich eigenen Server-Prozess beenden (`npm run central:stop`), danach einmal neu starten (`npm run central:start`).
- Kompakter Serverstatus (Port, Version, Commit, Startzeit) ist zusätzlich direkt in der Oberfläche sichtbar; technische Details sind standardmäßig eingeklappt.

## Datensicherung

1. Im Cockpit „Lokale Datensicherung“ öffnen
2. „Daten exportieren“ wählen
3. Die JSON-Datei sicher aufbewahren
4. Vor größeren Änderungen immer zuerst sichern

## Wiederherstellung

1. „Sicherung auswählen“
2. Importvorschau prüfen
3. Import ausdrücklich bestätigen
4. Seite neu laden

## Normaler Arbeitsablauf

**Seit V7.3 (empfohlen für Jamals eigene Projekte):** ganz oben im Cockpit „Heute arbeiten“ öffnen, priorisiertes Projekt bestätigen oder wechseln, gewünschtes Ergebnis eingeben, Arbeitslauf starten, Rückfrage (falls vorhanden) beantworten, Ergebnis oben prüfen und `Passt`/`Änderung anfordern`/`Später` entscheiden.

**Bisheriger, weiterhin vollständig erhaltener Tageslauf** (jetzt einklappbar unterhalb von „Heute arbeiten“):

1. Fokusprojekt wählen
2. Gewünschtes Ergebnis eingeben
3. Arbeitsvorschlag prüfen
4. Agenten-Prüfphase nur bewusst freigeben
5. Runtime-Pilot nur bewusst vorbereiten, freigeben und starten
6. Ergebnis bewusst prüfen und übernehmen
7. Tageslauf abschließen
8. Regelmäßig Backup exportieren

## Sicherheitsgrenzen

- keine automatische externe KI-Ausführung
- keine automatische Plugin-Ausführung
- keine automatische Git-Aktion
- keine automatische Veröffentlichung
- keine Zahlungen, Verträge oder Deployments
- **Jamal entscheidet über jede echte Außenwirkung**

## Fehlerfall

1. Nichts löschen
2. Keinen `git reset` ausführen
3. Fehlermeldung vollständig sichern
4. Letzten funktionierenden Commit und das letzte Backup nennen

Ausführlich: `V1_BETRIEBSHANDBUCH.md`
