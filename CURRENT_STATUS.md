# CURRENT STATUS

## V7.6.5 – Health-Referenzlauf vollständig abgeschlossen, `REFERENCE_READY` dokumentiert (lokal umgesetzt, zur Commitprüfung bereit)

**Ausgangsstand (real gemessen):** Branch `main`, HEAD/`origin/main` unverändert `ddf4fe85f482f33be81d512f4eb55eff9c7b814d` (V7.6.4, committed/gepusht), Working Tree zu Beginn sauber, kein Stash, 25 kanonische Agenten. Kanonischer Referenzlauf `health-reference-work-run-v1` stand zu Beginn dieses Dokumentations-Updates bereits **live in der Datenbank** bei Fortschritt `7 von 7`, Laufstatus `REFERENCE_READY` (über mehrere separat freigegebene, dokumentierte Cursor-Arbeitsaufträge seit V7.6.4 erreicht) – dieser Eintrag zieht ausschließlich die begleitende Markdown-Dokumentation der Unternehmenszentrale auf diesen bereits erreichten, live verifizierten Datenbankstand nach.

- **Zweck dieses Eintrags:** Die Einträge V7.6.3/V7.6.4 unten dokumentierten den Zwischenstand `1 von 7` (Paket 1 `COMPLETED`, Paket 2 `WAITING_FOR_JAMAL_APPROVAL`). Zwischen V7.6.4 und diesem Eintrag wurden die Arbeitspakete 2–7 in separat freigegebenen, einzeln dokumentierten Cursor-Arbeitsaufträgen umgesetzt, getestet, per Browser-QA geprüft und jeweils einzeln im Health-Repository committed/gepusht. Dieser Eintrag hält ausschließlich den **Endstand** fest; er löscht keinen der historischen Zwischenstände unten.
- **Abgeschlossene Arbeitspakete (chronologisch, jeweils einzeln freigegeben, umgesetzt, QA-geprüft, committed und gepusht im Health-Repository):**
  1. `HEALTH_BASELINE_CONFIRMATION` → `COMPLETED` (bereits in V7.6.4 dokumentiert)
  2. `START_GATE_AND_ENTRY` → `COMPLETED` (Einstieg/Start-Gate verständlicher geführt)
  3. `SIX_ANSWERS_AND_RESULT` → `COMPLETED` (Fortschritt/Sechs-Antworten-Regel/„Orientierung, keine Diagnose" geschärft)
  4. `ADVISOR_HANDOFF_AND_CUSTOMER_AREA` → `COMPLETED` (Beraterinnenkontakt bestätigungspflichtig statt sofort auslösend)
  5. `PERSISTENCE_PRIVACY_AND_LEGAL_BOUNDARIES` → `COMPLETED` (sofortige lokale Löschfunktion, Rechtstexte als Entwurf gekennzeichnet)
  6. `REFERENCE_WALKTHROUGH_AND_QA` → `COMPLETED` (vollständiger Referenz-Walkthrough, Ergebnisschutz vor Mindestantwortzahl ergänzt)
  7. `JAMAL_FINAL_ACCEPTANCE` → `REFERENCE_READY` (Jamal hat den finalen Walkthrough selbst durchgeführt und die Abnahme ausdrücklich mit **FREIGEGEBEN** bestätigt; ausschließlich über `recordFinalAcceptance({ confirmed: true })` ausgeführt)
- **Laufstatus:** `REFERENCE_READY` (technisch ausschließlich über `recordFinalAcceptance({ confirmed: true })` erreicht, kein automatischer Sprung, kein grüner Test und kein `COMPLETED`-Arbeitspaket allein hätte dafür ausgereicht – siehe `HEALTH_REFERENCE_WORK_RUN.md` Abschnitt 7).
- **Fortschritt:** `7 von 7 Arbeitspaketen abgeschlossen`.
- **Finaler Health-Commit (Referenz, read-only):** Branch `work/check-start-gate-2026-07-19`, HEAD `a896dbe20426facd5d76201c877cb3664b376011`, Working Tree sauber.
- **Diese Aktualisierung enthält:** ausschließlich Dokumentationsänderungen an `CURRENT_STATUS.md`, `PROJECT_MASTER.md`, `HEALTH_REFERENCE_WORK_RUN.md`, `MIGRATION_PLAN.md`. Keine Codeänderung, keine API-Änderung, keine Änderung an Migrationen, keine Health-Datei verändert (Health-Repository ausschließlich read-only gelesen), kein Deployment.
- **Nicht enthalten:** neue Produktfunktionen, neue Agenten, kein Agent 26, keine zweite Arbeitslaufarchitektur, keine Folgephase (Testkundin/Deployment) formal begonnen.
- **Urteil:** Der Health-Referenzlauf `health-reference-work-run-v1` ist vollständig abgeschlossen. Alle sieben Arbeitspakete sind abgeschlossen, Jamal hat die finale Abnahme ausdrücklich bestätigt, der Laufstatus ist `REFERENCE_READY`. Die Dokumentation der Unternehmenszentrale spiegelt diesen Endstand jetzt korrekt wider. **Nächster Schritt (separate Freigabe erforderlich):** Entscheidung über eine mögliche Folgephase (kontrollierter interner Testbetrieb, echte Testkundin, Deployment-Vorbereitung) liegt bei Jamal und ist nicht Bestandteil dieses abgeschlossenen Referenzlaufs.

## V7.6.4 – Einzelne Health-Arbeitspakete korrekt abschließen, ohne den Referenzlauf vorzeitig als fertig zu markieren (lokal umgesetzt, zur Commitprüfung bereit)

**Ausgangsstand (real gemessen):** Branch `main`, HEAD/`origin/main` unverändert `73a6a055414ff4baffa7f8dbf903a6e4a711e37b` (V7.6.3), Working Tree zu Beginn sauber, kein Stash, 25 kanonische Agenten, **86** Testdateien, **2454** Prüfpunkte, Migration 16. Kanonischer Referenzlauf `health-reference-work-run-v1`: Arbeitspaket 1 (`HEALTH_BASELINE_CONFIRMATION`) mit eingereichtem Ergebnisbericht und `APPROVED`-QA-Befund, Paketstatus `WAITING_FOR_FINAL_ACCEPTANCE`, Laufstatus `QA_REVIEW`; Arbeitspaket 2 (`START_GATE_AND_ENTRY`) mit vorhandenem Prompt-Entwurf, Status `WAITING_FOR_JAMAL_APPROVAL`, nicht freigegeben, nicht ausgeführt. Health Upgrade Kompass zu Beginn und am Ende unverändert: Branch `work/check-start-gate-2026-07-19`, HEAD `81dca3a9967b1763d7b3e881fffe213fe64f9d62`, Working Tree sauber.

- **Festgestellte Architekturlücke (V7.6.3):** einzelne Arbeitspakete besaßen keinen eigenen Abschlussstatus, sodass ein QA-bestätigtes Paket weiterhin als nicht abgeschlossen sichtbar blieb, Fortschritt fälschlich `0 von 7` und `nextWorkPackage` fälschlich Paket 1 blieben, die UI weiterhin QA forderte, obwohl QA bereits bestanden war, Statusübergänge über `transitionWorkPackage()` keinen Auditnachweis erzeugten und der Laufstatus `APPROVED_FOR_EXECUTION` nicht als Zwischenzustand zeigte.
- **Ziel:** einzelne Arbeitspakete sauber abschließen können, ohne den gesamten Referenzlauf oder Health als `REFERENCE_READY` zu markieren.
- **Statusmodell fachlich getrennt:** minimal additiver, eigenständiger Arbeitspaket-Status `COMPLETED` (nur für Arbeitspakete, kein Laufstatus). Bedeutet ausschließlich: Arbeitspaket ausgeführt, Ergebnisbericht eingereicht, QA bestanden, Paket fachlich abgeschlossen, nächstes Paket vorbereitbar/freigebbar. Bedeutet ausdrücklich **nicht**: Gesamtlauf abgeschlossen, finale Abnahme erfolgt, Health `REFERENCE_READY`, Produktionsreife, Commit-/Push-Freigabe. Regel: Pakete 1–6 nach bestandenem QA → `COMPLETED`; Paket 7 (`JAMAL_FINAL_ACCEPTANCE`) nach bestandenem QA → weiterhin `WAITING_FOR_FINAL_ACCEPTANCE` (Gesamtabnahme steht dort noch aus); gesamter Lauf → `REFERENCE_READY` erst nach bestätigter finaler Abnahme.
- **Laufstatusführung korrigiert (`syncRunStatusToActivePackage`):** der Laufstatus wird nach jedem Arbeitspaket-Statuswechsel automatisch aus dem Status des ersten noch nicht abgeschlossenen/abgebrochenen Arbeitspakets abgeleitet. Wird ein Paket auf `APPROVED_FOR_EXECUTION` gesetzt, führt der Lauf denselben Zwischenzustand sichtbar mit; ist ein Paket abgeschlossen und das nächste `WAITING_FOR_JAMAL_APPROVAL`, zeigt der Lauf ebenfalls `WAITING_FOR_JAMAL_APPROVAL`. Kein automatischer Sprung zu `REFERENCE_READY` – weiterhin ausschließlich über `recordFinalAcceptance({ confirmed: true })` erreichbar, technisch unverändert abgesichert (siehe `HEALTH_REFERENCE_WORK_RUN.md`).
- **Audit ergänzt:** neuer, datensparsamer Ereignistyp `HEALTH_REFERENCE_WORK_PACKAGE_STATUS_CHANGED` protokolliert jeden Arbeitspaket-Statuswechsel (Freigabe, Ausführung gestartet, Ergebnis eingereicht, QA begonnen, Paket abgeschlossen, Änderungen angefordert, blockiert, abgebrochen). Metadaten ausschließlich `healthReferenceRunId`, `workPackageKey`, `previousStatus`, `nextStatus` – keine Berichte, Inhalte oder Gesundheitsdaten.
- **`nextWorkPackage`-/Fortschrittslogik korrigiert:** `nextWorkPackage` liefert das erste Arbeitspaket, dessen Status weder `COMPLETED`, `REFERENCE_READY` noch `CANCELLED` ist. Fortschritt zählt ausschließlich `COMPLETED`/`REFERENCE_READY`; `WAITING_FOR_FINAL_ACCEPTANCE`, `QA_REVIEW`, `RESULT_SUBMITTED`, `CHANGES_REQUESTED`, `BLOCKED` zählen ausdrücklich nicht als abgeschlossen.
- **Persistenz:** additive **Migration 17** (eigenständiger, um `COMPLETED` erweiterter Wertebereich für `health_reference_work_packages.status`; erneute Audit-Ereignistyp-Erweiterung um `HEALTH_REFERENCE_WORK_PACKAGE_STATUS_CHANGED`; Migrationen 1–16 unverändert, keine destruktive Änderung, Tabellen unter Versionsnamen neu angelegt und Daten verlustfrei kopiert – gleiches etabliertes Muster wie jede vorherige CHECK-Erweiterung).
- **UI korrigiert (`health-reference-work-run-ui.js`):** dynamische, arbeitspaketbezogene nächste Handlung (`computeNextAction`, z. B. „Prompt für Start-Gate und Einstieg prüfen und freigeben" statt generischem Text) und dazu passender größter Blocker (`biggestBlockerText` nennt jetzt konkret das betroffene Arbeitspaket). Live im Browser verifiziert (Cockpit-Karte „Health-Referenzlauf"): Status „Wartet auf Jamal-Freigabe", Fortschritt „1 von 7 Arbeitspaketen abgeschlossen", nächstes Arbeitspaket „Start-Gate und Einstieg", größter Blocker „Prompt für Start-Gate und Einstieg wartet auf Prüfung und Freigabe durch Jamal.", Paket 1 aufklappbar mit Status „Abgeschlossen" plus Ergebnisbericht/QA-Befund, Paket 2 mit Status „Wartet auf Jamal-Freigabe" plus Prompt-Entwurf. Keine Behauptung „Health sei fertig"/`REFERENCE_READY`/finale Abnahme, keine Commit-/Push-Schaltfläche, keine Schaltfläche fordert für Paket 1 erneut QA an.
- **Persistierte Daten kontrolliert korrigiert (keine manuelle SQL-Manipulation, ausschließlich über `transitionWorkPackage` der bestehenden Service-Architektur):** Vorbedingungen vor der Korrektur real geprüft (Ergebnisbericht vorhanden, QA-Befund vorhanden, `passed: true`, keine finale Ablehnung, keine Changes Request, Status `WAITING_FOR_FINAL_ACCEPTANCE`) – danach Paket `HEALTH_BASELINE_CONFIRMATION` → `COMPLETED`. Ergebnis: Paket 1 `COMPLETED`, Paket 2 unverändert `WAITING_FOR_JAMAL_APPROVAL`/nicht freigegeben/nicht ausgeführt, Laufstatus `WAITING_FOR_JAMAL_APPROVAL`, Fortschritt `1 von 7`, `nextWorkPackage = START_GATE_AND_ENTRY`, `REFERENCE_READY` weiterhin nicht gesetzt, keine finale Gesamtfreigabe.
- **Tests:** keine neue Testdatei, acht bestehende Dateien geändert. `health-reference-work-run.test.js` 22→**33** Prüfpunkte (u. a. `COMPLETED` existiert nur für Arbeitspakete, Pakete 1–6 werden nach QA `COMPLETED`, Paket 7 wartet weiterhin auf finale Abnahme, `REFERENCE_READY` nur durch bestätigte finale Abnahme, Laufstatus folgt `APPROVED_FOR_EXECUTION`/`WAITING_FOR_JAMAL_APPROVAL`, Audit-Nachweis mit korrekten Metadaten). `health-reference-work-run-security.test.js` 25→**34** Prüfpunkte (End-to-End-HTTP-Szenario: Paket 1 vollständig bis `COMPLETED` durchlaufen, Paket 2 vorbereitet, Laufstatus/Fortschritt/`nextWorkPackage`/`nextAction` über `GET /api/health-reference/status` verifiziert, Audit-Ereignis ohne sensible Daten). `health-reference-work-run-ui.test.js` 21→**24** Prüfpunkte (keine Schaltfläche löst erneute QA/Ergebniseinreichung für abgeschlossene Pakete aus, Fortschritt/`nextWorkPackage` generisch aus `run.progress`/`run.nextWorkPackage` abgeleitet, `pkg.statusLabel` für Paketstatus verwendet). `portal-operations-acceptance.test.js` real nachgezogen (Migrationsarray 1–16→1–17). Keine bestehende Assertion entfernt oder abgeschwächt. Realer, reproduzierbarer Gesamtstand: **2477** Prüfpunkte / weiterhin **86** Testdateien (**+23** Prüfpunkte gegenüber der V7.6.3-Baseline 2454/86). `npm run check`/`npm test` Exit 0, `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert.
- **Health read-only verifiziert:** Health-Repository (`work/check-start-gate-2026-07-19`) real per `git status`/`git rev-parse HEAD`/`git stash list` geprüft – Branch, HEAD `81dca3a9967b1763d7b3e881fffe213fe64f9d62` und sauberer Working Tree am Ende dieses Laufs identisch zum Ausgangsstand, kein Stash, keine Health-Datei verändert.
- **Nicht enthalten:** jede Health-Dateiänderung, Freigabe oder Ausführung von Paket 2, jede Health-Implementierung, jeder neue Agent, kein Agent 26, keine zweite Arbeitslaufarchitektur, keine automatische finale Abnahme, kein Setzen von `REFERENCE_READY`, keine externe Aktion, kein Deployment, kein Commit, kein Push.
- **Urteil:** die KI-Unternehmenszentrale kann einzelne Health-Arbeitspakete nun korrekt abschließen, ohne den gesamten Referenzlauf vorzeitig als fertig zu markieren. `HEALTH_BASELINE_CONFIRMATION` ist abgeschlossen und `START_GATE_AND_ENTRY` wartet als nächster kontrollierter Schritt auf Freigabe. **Nächster Schritt (separate Freigabe erforderlich):** die verantwortliche Leitung prüft und genehmigt den `START_GATE_AND_ENTRY`-Prompt. Details siehe `HEALTH_REFERENCE_WORK_RUN.md`.

## V7.6.3 – Health Upgrade Kompass als ersten echten Referenz-Arbeitslauf der Unternehmenszentrale verankert (lokal umgesetzt, zur Commitprüfung bereit)

**Ausgangsstand (real gemessen):** Branch `main`, HEAD/`origin/main` unverändert `c9635a56d5fa4fcc730fd7471db5b1cd0ea08aa8` (V7.6.2, reiner Planungsstand), Working Tree zu Beginn sauber, kein Stash, 25 kanonische Agenten, **83** Testdateien, **2386** Prüfpunkte (real nachgemessen; die V7.6.1-Dokumentation nannte 2384, siehe Testdelta unten), Migration 15, Finance/Controlling weiterhin `CAPABILITY_GAP`, keine Google-Anmeldung, kein OAuth, kein Deployment. Health Upgrade Kompass zu Beginn: Branch `work/check-start-gate-2026-07-19`, HEAD `81dca3a9967b1763d7b3e881fffe213fe64f9d62`, Working Tree sauber, Scale-V1-Vorarbeit als Phase 2 committed/gepusht, keine echte Waage, keine UI-Anbindung der Scale-Vorarbeit.

- **Ziel:** die Unternehmenszentrale führt den Health Upgrade Kompass erstmals als echten, kontrollierten End-to-End-Projektlauf – „Wir arbeiten operativ am Health Upgrade Kompass. Die KI-Unternehmenszentrale übernimmt Führung, Struktur, Agentenzuordnung, Freigaben, Qualität und Abschlussnachweis." Dieser Lauf implementiert **keine** Health-Funktion; er verankert ausschließlich den verbindlichen Health-Ergebniswunsch, die Agentenzuordnung, sieben Arbeitspakete, Freigabegrenzen, messbare Abschlusskriterien, den Übergabevertrag an Cursor sowie Fortschritts-/Ergebnisnachweise – bis zu Jamals abschließender Referenzfreigabe.
- **Bestehende Architektur verwendet, keine zweite Arbeitslaufarchitektur:** analysiert und wiederverwendet wurden `daily-work-run.js`/`daily-work-run-ui.js` (Tageslauf-/Ergebniswunschmuster), `agent-registry.js` (kanonisches Register), `office-work-service.js`/`office-finance-routes.js` (nächstliegendes bestehendes Muster für ein zusätzliches, additives Fach-/Auftragsmodul auf derselben `auth-db.js`-Schicht), `auth-audit.js` (Auditkern), `auth-db-migrations.js` (Migrationssystem), `route-access-policy.js`/`server.js` (Routing/Zugriffspolicy), `health-repo-status.js` (bereits bestehender, read-only abgesicherter Health-Live-Statuslesepfad – **kein neuer Git-Zugriff**), `INTERNAL_PROJECT_REFERENCE_PLAN.md`/`HEALTH_REFERENCE_FINISH_PLAN.md`/`PROJECT_FINISH_DECISION_BOARD.md` (V7.6.2-Planungsgrundlage). **Keine** doppelte Agentenzuordnung, **keine** separate Schattendatenbank, **keine** neue Persistenzarchitektur – der Referenzlauf ist ein spezialisiertes, additives Modul (`health-reference-work-run-service.js`/`health-reference-work-run-routes.js`) auf der bestehenden `auth-db.js`-SQLite-Schicht.
- **Kanonischer Referenzlauf (`health-reference-work-run-v1`):** Titel „Health Upgrade Kompass bis zum abnehmbaren Referenz-Walkthrough führen", Projekt Health Upgrade Kompass, Projektpfad als reine Referenz `/Users/jamal/Documents/New project/health-upgrade-kompass`, Ergebniswunsch „Der Health Upgrade Kompass durchläuft stabil und nachvollziehbar den vollständigen Referenzweg von Start und sechs Antworten über Ergebnis und Beraterinnenübergabe bis zum Kundenbereich und kann von Jamal anhand eines dokumentierten Walkthroughs abgenommen werden." Status startet verbindlich `PREPARED_FOR_EXECUTION` – **keine** Behauptung, Health sei bereits `REFERENCE_READY`. Genau ein Lauf, feste ID, idempotente Anlage (`getOrCreateCanonicalRun`).
- **Agentenzuteilung (ausschließlich bestehende kanonische Agenten, keine erfundene ID):** genau ein Hauptverantwortlicher **Projektmanager-Agent** (Ergebnisverantwortung, zerlegt den Finish in Arbeitspakete, verhindert Scope-Ausweitung, übergibt an die Ausführungsebene, eskaliert an Jamal); maximal drei Fachagenten **Produktmanager-Agent** (Nutzerfluss/Start-Gate/Verständlichkeit/mobile Nutzung), **Entwickler-Agent** (technische Umsetzungsvorbereitung/Persistenz/Flow-Stabilität/Tests, keine Phase-2-Waagenintegration), **Content-Agent** (Texte/Beraterinnenübergabe/Kundenbereich/Walkthrough-Dokumentation/Ehrlichkeitsgrenzen); genau ein QA-/Sicherheitsagent **QA-Agent** (Abschlusskriterien/Datenschutz/fehlende Rechtstexte/technische Grenzen, keine falsche Produktionsreife, keine echte Waagenbehauptung). **Kein neuer Agent, kein Agent 26**, weiterhin exakt 25 kanonische Agenten.
- **Sieben feste, sequenzielle Arbeitspakete (nur vorbereitet, keines ausgeführt):** `HEALTH_BASELINE_CONFIRMATION` (Branch/HEAD/Tests/Flow, keine Änderung), `START_GATE_AND_ENTRY` (Start, erste klare Handlung), `SIX_ANSWERS_AND_RESULT` (sechs Antworten, Validierung, Ergebnis, mobile Nutzbarkeit), `ADVISOR_HANDOFF_AND_CUSTOMER_AREA` (Beraterinnenübergabe, Kundenbereich), `PERSISTENCE_PRIVACY_AND_LEGAL_BOUNDARIES` (Consent, Datenschutzgrenzen, Rechtstexte), `REFERENCE_WALKTHROUGH_AND_QA` (vollständige Teststrecke, Referenzartefakt), `JAMAL_FINAL_ACCEPTANCE` (Jamals Walkthrough, Freigabe oder Rückgabe, erst danach `REFERENCE_READY`).
- **Verbindliche Nicht-Ziele:** echte Waagenhardware, BLE, Yolanda-SDK, neue Scale-UI, automatische Hardwareerkennung, Google Workspace, Finance, Marketingagentur, Expansion-App-Abtrennung, vollständiger Produktionsbetrieb, automatische Veröffentlichung/Kundenkommunikation/Freigabe, neue Agenten, Agent 26. Die bestehende Scale-V1-Vorarbeit bleibt ausdrücklich Phase 2.
- **Sieben Jamal-Freigabepunkte:** Scope-Freigabe, Freigabe eines ausführbaren Cursor-Arbeitsauftrags, Freigabe jeder relevanten Scope-Erweiterung, Freigabe rechtlicher/datenschutzrelevanter Formulierungen, Freigabe vor Commit, Freigabe vor Push, finale Referenzabnahme. Kein Agent darf selbst committen, selbst pushen, den Scope eigenständig erweitern, Rechtstexte als geprüft behaupten, eine echte Waagenfunktion behaupten oder Health als produktionsreif erklären.
- **Übergabevertrag an die Ausführungsebene (`buildHandoverContract`/`prepareWorkPackagePromptDraft`):** aus einem Arbeitspaket erzeugt der Projektmanager-Agent einen standardisierten, rein textuellen Cursor-Prompt-Entwurf mit Projektpfad, Health-Branch/-HEAD zum Vorbereitungszeitpunkt (live über das bestehende, read-only `health-repo-status.js` gelesen), Ergebnisziel, erlaubtem/verbotenem Dateiumfang, fachlichem Umfang, Nicht-Zielen, Testanforderungen, Sicherheitsgrenzen, erwartetem Git-Status, Berichtsanforderungen und ausdrücklich `autoCommitAllowed: false`/`autoPushAllowed: false`/`commitRequiresJamalApproval: true`/`pushRequiresJamalApproval: true`. **Keine automatische Ausführung implementiert** – der Entwurf setzt den Arbeitspaketstatus auf `WAITING_FOR_JAMAL_APPROVAL`, Jamal kopiert oder genehmigt den Auftrag manuell außerhalb dieses Laufs.
- **Statusmodell (elf Werte, additiv, keine Änderung bestehender Statuswerte anderer Module):** `PREPARED_FOR_EXECUTION` → `WAITING_FOR_JAMAL_APPROVAL` → `APPROVED_FOR_EXECUTION` → `IN_EXECUTION` → `RESULT_SUBMITTED` → `QA_REVIEW` → `CHANGES_REQUESTED`/`WAITING_FOR_FINAL_ACCEPTANCE` → `REFERENCE_READY`, daneben `BLOCKED`/`CANCELLED`. **`REFERENCE_READY` ist ausschließlich über `recordFinalAcceptance({ confirmed: true })` erreichbar** – kein automatischer Sprung, keine generische Statusübergangs-/Freigabefunktion kann diesen Status auslösen, grüne Tests/QA-Befunde allein reichen technisch nachgewiesen nicht aus (siehe Tests unten).
- **14 Abschlusskriterien für `REFERENCE_READY`:** funktionierende Startseite, verständliches/stabiles Start-Gate, vollständige sechs Antworten, korrektes/verständliches Ergebnis, nachvollziehbare Beraterinnenübergabe, erreichbarer/verständlicher Kundenbereich, keine tote Hauptnavigation, geprüfte mobile Hauptstrecke, grüne relevante Tests, sichtbare Datenschutz-/Ehrlichkeitsgrenzen, klar abgegrenzte Scale-Demo/Phase-2-Funktion, vorhandenes Walkthrough-Protokoll, dokumentierte Restpunkte, Jamals ausdrückliche Referenz-Walkthrough-Freigabe.
- **Persistenz:** additive **Migration 16** (`health_reference_runs`, `health_reference_work_packages`, `health_reference_approvals`, `health_reference_results`; Migrationen 1–15 unverändert). Gespeichert werden ausschließlich Lauf-ID/Projekt/Ergebniswunsch/Agentenzuordnung/Arbeitspaketstatus/Jamal-Freigaben/Ergebnisberichte/QA-Befunde/Abschlussnachweis/Zeitstempel/Auditverweise – **keine** Health-Nutzerdaten, **keine** medizinischen Daten, **keine** aus dem Health-Repository kopierten personenbezogenen Daten.
- **API (`health-reference-work-run-routes.js`):** eine neue OWNER_ONLY-GET-Route (`GET /api/health-reference/status`) und ein neuer POST-Aktions-Prefix `/api/health-reference/:action` (`ensure-run`, `prepare-work-package-prompt`, `record-approval`, `transition-work-package`, `submit-result-report`, `submit-qa-finding`, `request-changes`, `record-final-acceptance`, `block-or-cancel-run`) – identische CSRF-/Origin-Prüfung/Bodylimit/Known-fields-Allowlist/`Cache-Control: no-store` wie bestehende Owner-Routen; **keine** Login-/Send-/Deploy-/Commit-/Push-Route. Neue Routenzahlen: **89 GET, 52 POST, 8 GET-Präfixe, 8 POST-Präfixe, 32 statische Assets** (+1 GET/+1 POST-Präfix/+1 statisches Asset gegenüber der V7.6.1-Baseline 88/52/8/7/31).
- **Audit:** neun neue, datensparsame Ereignistypen (`HEALTH_REFERENCE_RUN_CREATED`, `HEALTH_REFERENCE_WORK_PACKAGE_PREPARED`, `HEALTH_REFERENCE_PROMPT_DRAFT_CREATED`, `HEALTH_REFERENCE_APPROVAL_RECORDED`, `HEALTH_REFERENCE_RESULT_REPORT_SUBMITTED`, `HEALTH_REFERENCE_QA_FINDING_RECORDED`, `HEALTH_REFERENCE_CHANGES_REQUESTED`, `HEALTH_REFERENCE_FINAL_ACCEPTANCE_PREPARED`, `HEALTH_REFERENCE_REFERENCE_READY_GRANTED`) – Metadaten-Allowlist um `healthReferenceRunId`/`workPackageKey`/`approvalKey`/`resultKind` erweitert, niemals ein Health-Nutzer-/Gesundheitsdatum.
- **UI (`index.html#health-reference-run-card`, gerendert von `health-reference-work-run-ui.js`):** kompakte Karte nach dem Grundsatz „Oben arbeiten. Unten nachschauen." – oben Projekt/Ergebniswunsch/Status/Hauptverantwortlicher/nächstes Arbeitspaket/größter Blocker/Fortschritt als „X von 7 Arbeitspaketen" (kein erfundener Prozentwert)/eine klare nächste Handlung; darunter aufklappbar Agentenzuordnung/sieben Arbeitspakete/Freigaben/Nicht-Ziele/Abschlusskriterien/Ergebnis-/QA-Nachweise. Zulässige Aktionen ausschließlich: Lauf/Arbeitspaket ansehen, Prompt-Entwurf vorbereiten, Jamal-Freigabe dokumentieren; die finale Abnahme erfordert bewusst eine Bestätigung außerhalb der Schaltfläche (kein `confirmed: true` im Client fest verdrahtet). **Keine Schaltfläche führt eine echte Health-Ausführung, einen Commit oder einen Push aus.**
- **Tests:** drei neue Testdateien – `health-reference-work-run.test.js` (**22** Prüfpunkte: 25 Agenten unverändert, keine neue Agentendefinition, Projektmanager als einziger Hauptverantwortlicher, maximal drei Fachagenten, genau ein QA-Agent, Lauf startet nicht als abgeschlossen, Prompt-Entwurf enthält Pfad/Branch/HEAD/Grenzen/Tests, keine automatische Commit-/Push-Freigabe, Scale bleibt Phase 2, feste Autonomiegrenzen, `REFERENCE_READY` ausschließlich nach Jamals expliziter Bestätigung – auch bei durchgehend grünem QA-Befund technisch nachgewiesen unerreichbar, Audit-Nachweis, keine externe Aktion exportiert), `health-reference-work-run-security.test.js` (**25** Prüfpunkte: OWNER_ONLY auf GET-Route/statischem Asset/POST-Prefix, CSRF/Origin wirksam, keine Login-/Send-/Deploy-/Commit-/Push-Aktion, unbekannte Felder abgewiesen, datensparsames Audit, keine Tokens/Secrets/Stacktraces in Antworten) und `health-reference-work-run-ui.test.js` (**21** Prüfpunkte: Karte nach der Heute-arbeiten-Karte, aufklappbare Details, genau eine primäre nächste Handlung je Zustand, kein Produktionsreife-Anspruch, keine echte Health-Ausführung/Commit/Push, alle Aktionen ausschließlich über `/api/health-reference/*`, kein Tracking/LocalStorage/externe Ressource). Drei bestehende hartkodierte Routenzahlen-Assertions (88→89 GET in `daily-work-run.test.js`/`agent-runtime.test.js`/`server-http-router.test.js`) sowie Routen-/Präfix-/Assetzahlen (52/8/7→8/31→32) und die Migrationsarray-Assertion (1–15→1–16 in `portal-operations-acceptance.test.js`) real nachgezogen – keine bestehende Assertion entfernt oder abgeschwächt. Realer, reproduzierbarer Gesamtstand: **2454** Prüfpunkte / **86** Testdateien (**+68** neue Prüfpunkte in den 3 neuen Testdateien). `npm run check`/`npm test` Exit 0, `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert.
- **Health read-only verifiziert:** Health (`work/check-start-gate-2026-07-19`) wurde in diesem gesamten Lauf ausschließlich über das bestehende `health-repo-status.js` gelesen (Branch/HEAD/Working-Tree-Status) – kein Git-Schreibzugriff, kein Branchwechsel, kein Commit, kein Push. HEAD/Working-Tree-Status unverändert gegenüber dem Ausgangsstand.
- **Nicht enthalten:** jede echte Health-Funktion, jede Health-Dateiänderung, jeder Branchwechsel/Commit/Push im Health-Repository, jede echte Waagenintegration, jeder neue Agent, jede externe Aktion (Google/Mail/Kalender/Canva/HeyGen), jedes Deployment, jeder Commit/Push in der Unternehmenszentrale selbst.
- **Urteil:** der Health Upgrade Kompass ist als erster echter, kontrollierter Referenz-Arbeitslauf in der KI-Unternehmenszentrale verankert. **Nächster Schritt (separate Freigabe erforderlich):** Jamal prüft und genehmigt das erste ausführbare Health-Arbeitspaket. Details siehe `HEALTH_REFERENCE_WORK_RUN.md`.

## V7.6.2 – Projekt-Finish-Portfolio, interne Referenzläufe und verkaufsfähige Marketingagentur vorbereitet (reiner Planungs- und Entscheidungsstand, keine Implementierung)

**Ausgangsstand (real gemessen):** Branch `main`, HEAD/`origin/main` unverändert `5b59b177d8a5e36982cd7fafb9d6f239a7fb7d79` (V7.6.1, committed und gepusht), Working Tree zu Beginn sauber, 25 kanonische Agenten, 83 Testdateien, 2384 Prüfpunkte, Migration 15, Finance/Controlling weiterhin `CAPABILITY_GAP`, keine Google-Anmeldung, kein OAuth, kein Deployment. **Ausdrücklich kein Code, kein Test, keine Migration, kein HTML/CSS/JS in diesem Lauf verändert – ausschließlich sechs neue Markdown-Dokumente erstellt und fünf kanonische Dokumente ergänzt.**

- **Ziel:** alle relevanten eigenen Projekte außerhalb der Unternehmenszentrale (Health Upgrade Kompass, Marketing Agentur OS, Expansion-/Export-App, Sprachtrainer-Familie, Karriere-/Static-Sites, persönliche Werkzeuge) read-only erfassen, ihren tatsächlichen Reifegrad bewerten, eine begründete Abschlussreihenfolge festlegen, echte interne End-to-End-Testläufe der Zentrale planen (nicht ausführen), Health Upgrade Kompass als möglichen End-to-End-Referenzfall prüfen und die Marketingagentur als erstes verkaufbares Produkt vorbereiten.
- **Strategischer Grundsatz für diese Phase:** erst eigene Projekte fertigstellen und die Unternehmenszentrale daran beweisen, danach zunächst die Marketingagentur verkaufen – die vollständige Unternehmenszentrale wird erst später breiter vermarktet.
- **Neue Dokumente (ausschließlich im Hauptprojekt):** `PROJECT_FINISH_PORTFOLIO.md` (alle erfassten Projekte, Reifegrade, Fertigstellungsbereiche, Blocker, Reststunden, Abschlussreihenfolge), `INTERNAL_PROJECT_REFERENCE_PLAN.md` (fünf geplante, noch nicht ausgeführte End-to-End-Testläufe der Zentrale mit Agentenzuordnung/Freigaben/Abbruchkriterien), `HEALTH_REFERENCE_FINISH_PLAN.md` (Pflichtumfang/Teststrecke/Abnahmekriterien bis `REFERENCE_READY`), `MARKETING_AGENCY_SELLABLE_PILOT_PLAN.md` (Produktversprechen, drei Pakete mit Preisbereichen als Planungsannahmen, Pilotprozess), `PROJECT_FINISH_DECISION_BOARD.md` (drei wichtigste Entscheidungen, Prioritäten 1–3), `HR_FUTURE_AGENT_DECISION_AGENDA.md` (Kompetenzlücken-Beobachtung, kein Agent 26 beschlossen).
- **Kernbefund Projektportfolio:** Health Upgrade Kompass (eigenes Git-Repo, Branch `work/check-start-gate-2026-07-19`, dirty seit vor diesem Lauf, unverändert) ist mit Abstand am weitesten fortgeschritten (`INTERNAL_PILOT_READY`, Richtung `REFERENCE_READY`, geschätzt 60–110 Reststunden). Marketing Agentur OS (kein Git-Repo) ist intern nutzbar, aber Richtung Verkauf noch `FUNCTIONAL_INCOMPLETE` (geschätzt 70–130 Reststunden, insbesondere fehlt ein echter Beispielauftrag als Nachweis statt Platzhaltern). Expansion-/Export-App teilt sich technisch weiterhin denselben Ordner/Code wie Health Upgrade Kompass und ruht seit rund sechs Wochen. Alle Prozentangaben sind Schätzbereiche mit Begründung, keine exakten Messwerte.
- **Verbindliche Abschlussreihenfolge (max. drei aktive Prioritäten):** 1. Health Upgrade Kompass bis `REFERENCE_READY`, 2. Marketing Agentur OS bis `SELLABLE_PILOT_READY`, 3. Expansion-/Export-App-Trennungsentscheidung vorbereiten. Alle weiteren Projekte bleiben nachrangig.
- **Agenten-/HR-Prüfung:** alle fünf geplanten Testläufe lassen sich mit den bestehenden 25 Agenten abdecken; **kein neuer Agent in diesem Lauf definiert oder beschlossen**. Content-Agent (7) und QA-Agent (6) sind bei paralleler Ausführung mehrerer Läufe die wahrscheinlichsten Engpassrollen. Ein spezialisierter Finance-/Controlling-Ausführungsagent bleibt ein möglicher, aber ausdrücklich nicht beschlossener späterer Kandidat.
- **Nicht enthalten:** jede Code-/Test-/Migrations-/Datenbankänderung, jede Änderung an Health Upgrade Kompass oder einem anderen fremden Repository, jede Google-/OAuth-Anmeldung, jede Canva-/HeyGen-Produktion, jede echte Kundenansprache, jede neue Agentendefinition, Commit, Push, Deployment.

## V7.6.1 – Apple-first/Google-controlled Office-, Google-Workspace- und Finance-Korridor vollständig offline vorbereitet (lokal umgesetzt, zur Commitprüfung bereit)

**Ausgangsstand (real gemessen):** Branch `main`, HEAD/`origin/main` `6ffa3f8c212a9417af523327ddb445354b95093c` („V7.5 Agentenführung und Unternehmensleitlinien V1.0 verankern"), Working Tree sauber, 25 kanonische Agenten, Unternehmensleitlinien Version 1.0, **2269** Prüfpunkte / **80** Testdateien, 79 GET/52 POST/8 GET-Präfixe/6 POST-Präfixe/30 statische Assets, Migration 14, `better-sqlite3@13.0.1`, `npm audit`: 0 Schwachstellen. Ausdrücklich: keine Google-Anmeldung, kein OAuth, kein Gmail-/Kalender-/Drive-/Kontaktzugriff, keine Apple-Datenmigration, keine Bankverbindung, keine Zahlung, kein Commit, kein Push in diesem Lauf.

- **Ziel:** die verbindliche Zielarchitektur `Apple-first / Google-controlled` so weit wie technisch und fachlich möglich **vollständig offline** vorbereiten, damit eine spätere echte Google-Workspace-/Finance-Anbindung nur noch wenige, klar abgegrenzte Jamal-Freigaben außerhalb von Cursor benötigt. Apple bleibt Jamals persönlicher Arbeitsraum, Google Workspace wird der kontrollierte Unternehmens- und Agentenraum – keine Vollmigration, keine automatische Übernahme privater Apple-Daten, keine doppelte manuelle Pflege ohne klare Zuständigkeit.
- **Systemlandkarte (`APPLE_GOOGLE_OPERATING_MODEL.md`):** kanonische, menschenlesbare Beschreibung der Datenhoheit je System (Apple: persönlicher Kalender/Kontakte/Notizen/Erinnerungen/Fotos/private Dokumente; Google Workspace: `office@jacogbr.de`, geschäftliche Kommunikation, Unternehmenskalender, zentrale Dokumente, kontrollierte Agentenentwürfe), zulässige Überschneidungen, verbotene Doppelpflege, Kalender-/Kontakt-/Dokument-/E-Mail-/Freigabe-/Migrations-/Rückfallstrategie sowie bewusst offengehaltene spätere Entscheidungsoptionen.
- **Identitäts-/Kontenmodell (`external-identity-service.js`, Tabelle `external_identities`):** drei Startidentitäten, alle als geschäftliche Adressen der Unternehmensdomäne `jacogbr.de` einheitlich mit Provider `GOOGLE_WORKSPACE` modelliert (Apple bleibt ausschließlich Systemzuständigkeit, siehe `APPLE_GOOGLE_OPERATING_MODEL.md`, und wird nicht mit dem technischen Mailanbieter verwechselt) – `jamal@jacogbr.de` (`OWNER_PERSONAL`, Status **`ACCOUNT_PREPARED_EXTERNALLY_OR_PENDING_CONFIRMATION`**), `office@jacogbr.de` (`COMPANY_OFFICE`, Status **`ACCOUNT_PREPARED_EXTERNALLY_OR_PENDING_CONFIRMATION`** – lokal nicht nachweisbar eingerichtet, keine Annahme als Tatsache dokumentiert), `info@jacogbr.de` (`PUBLIC_INBOX`, Status **`USER_ACCOUNT_OR_ALIAS_UNCONFIRMED`** – lokal nicht unterscheidbar, ob eigenständiges Konto oder nur Alias/Weiterleitung). **Kein** Passwort-, Token-, Recovery-Code- oder OAuth-Feld; `agentDirectLoginAllowed` ist für jede Identität hart auf `false` fixiert (CHECK-Constraint + Anwendungslogik) – kein Agent erhält je eigene Zugangsdaten.
- **Google-Workspace-Fähigkeitsmodell (`google-workspace-capability-service.js`):** 33 statische, unveränderliche Fähigkeiten (12 Gmail, 8 Calendar, 9 Drive/Docs, 4 Contacts) mit Risiko/Datensensitivität/Lesen-oder-Schreiben/externer Wirkung/empfohlener Einstiegsstufe/benötigter Jamal-Freigabe/zuständigem Agenten. Acht Berechtigungsstufen (`DISCONNECTED` … `BLOCKED`); **aktueller Zustand jeder Fähigkeit bleibt `DISCONNECTED`**, maximal empfohlener lokaler Zielstatus ist `PREPARE_DRAFT` – kein Codepfad aktiviert `JAMAL_APPROVED_WRITE`/`LIMITED_AUTOMATED_WRITE`, kein automatischer Stufenwechsel. Ausführliche Freigabematrix: `GOOGLE_WORKSPACE_APPROVAL_MATRIX.md`.
- **Office-Agentenmodell und Office-Auftragsmodell (`office-work-service.js`, Tabelle `office_work_items`):** fachliche Zuordnung ausschließlich vorhandener `agent-registry.js`-Agenten (Office Lead `orchestrator-agent`, E-Mail `communication-agent`, Kalender `workflow-agent`, Dokumente `documentation-agent`, Kontakte `customer-value-agent`, Projektmanager-Orchestrierung `project-status-agent`, Qualität/Sicherheit `security-agent`, Jamal-Freigabe ohne Agent) – **kein 26. Agent**, genau ein Owner je Auftrag, Contributors zulässig. Vier vollständig lokale, deterministische Offline-Korridore: Gmail (`prepareEmailDraft`: Empfänger/Betreff/Text/Anhangsbeschreibung/sensible-Inhalte-Markierung, sendet nichts), Kalender (`prepareCalendarDraft`: erfasst Terminwunsch, markiert jeden Entwurf hart als `AVAILABILITY_NOT_VERIFIED`, erstellt keinen echten Termin), Drive/Dokumente (`prepareDocumentDraft`: acht Dokumenttypen, erstellt keine echte Datei), Kontakte (`prepareContactSearchRequest`: reiner Suchauftrag, liest keine echten Kontakte). Statusmodell mit technisch hart begrenztem Höchststand `WAITING_FOR_AUTHENTICATION` (`assertExecutionStatusAllowed` wirft bei jedem Versuch, `READY_FOR_PROVIDER`/`EXECUTED` zu erreichen) – kein echter Provideraufruf, keine vorgetäuschte Providerreferenz.
- **Finance-/Controlling-Handoff-Korridor (`finance-handoff-service.js`, Tabelle `finance_handoffs`):** die bestehende Finance/Controlling-Capability-Gap (0 Agenten, `agent-organization-service.js#CAPABILITY_GAP`) bleibt ausdrücklich sichtbar und wird **nicht** durch einen 26. Agenten oder einen vollwertigen Buchhaltungsagenten kaschiert. Sieben Handoff-Typen (Belegsichtung, Rechnungsentwurf, Zahlungsvorschlag, Kostenklassifikation, Steuerberaterübergabe, Monatsübersicht, Liquiditätsnotiz) – `executionBlocked` ist per `CHECK (executionBlocked = 1)`-Constraint UND zusätzlich programmatisch fixiert, kein Codepfad kann buchen, zahlen, eine Rechnung versenden oder ein Bank-/Buchhaltungssystem ansprechen. Ausführliche Freigabematrix: `FINANCE_HANDOFF_APPROVAL_MATRIX.md`.
- **Provider-Abstraktion (`google-workspace-connector.js`):** expliziter Offline-Stub – kein echter HTTP-Client, keine Tokens, keine OAuth-Logik, kein Netzwerkaufruf. `callGoogleWorkspaceStub` liefert immer eine deterministische, eindeutig als Stub gekennzeichnete Antwort (`isStub: true`, `stubReferenceId` mit festem `stub-offline-`-Präfix, niemals eine echte Google-ID) und lehnt jede tatsächlich injizierte Providerfunktion mit einer Ausnahme ab.
- **Persistenz:** additive **Migration 15** (`external_identities`, `office_work_items`, `finance_handoffs`; Migrationen 1–14 unverändert; keine Token-, Passwort-, OAuth-Credential- oder vollständigen Providerpayload-Felder; `auth_audit_events`-CHECK-Aufzählung um neun Ereignistypen erweitert – gleiches etabliertes Muster wie Migration 7/8/9/10/11/13/14).
- **API (`office-finance-routes.js`):** neun neue OWNER_ONLY-GET-Routen (`/api/office-finance/summary`, `.../system-map`, `.../identities`, `.../capabilities`, `.../approval-matrix`, `.../work-items`, `.../finance-handoffs`, `.../authentication-status`, `.../activation-checklists`) und ein neuer POST-Aktions-Prefix `/api/office-finance/:action` (`review-external-identity`, `create-office-work-item`, `review-office-work-item`, `create-finance-handoff`, `review-finance-handoff`) – identische CSRF-/Origin-Prüfung/Bodylimit/Known-fields-Allowlist/`Cache-Control: no-store` wie bestehende Owner-Routen; keine Login-/OAuth-/Callback-/Send-/Create-/Delete-Providerroute. Neue Routenzahlen: **88 GET, 52 POST, 8 GET-Präfixe, 7 POST-Präfixe, 31 statische Assets** (+9 GET/+1 POST-Präfix/+1 statisches Asset gegenüber der V7.5-Baseline 79/52/8/6/30).
- **Audit:** neun neue, datensparsame Ereignistypen (`EXTERNAL_IDENTITY_REVIEWED`, `PROVIDER_CAPABILITY_REVIEWED`, `OFFICE_WORK_ITEM_CREATED`/`REVIEWED`, `OFFICE_EXTERNAL_ACTION_APPROVED`, `OFFICE_AUTHENTICATION_REQUIRED`, `FINANCE_HANDOFF_CREATED`/`REVIEWED`, `FINANCE_SPECIALIST_REQUIRED`) – ausschließlich ID/Kategorie/Status/benötigte Berechtigungsstufe/Entscheidung/Zeitpunkt, niemals E-Mail-Inhalt, Dokumentinhalt, Bankinformation, Token, Secret oder vollständiger Providerpayload.
- **UI „Office & Finanzen"** (`index.html#office-finance-view`, gerendert von `office-finance-ui.js`): Kopfkarte mit Apple-first/Google-controlled-Status, Google-Kontenstatus, ausstehender Authentifizierung, höchstens drei wichtigen Entscheidungen, Finance-Capability-Gap, heutigem nächsten Schritt; acht Bereiche als Tabs (`Systemaufteilung`, `Identitäten`, `E-Mail`, `Kalender`, `Dokumente`, `Kontakte`, `Finanzen`, `Freigaben`, `Verbindungen`); progressive Offenlegung über `<details>`; Read-only/Entwurf/Schreiben sichtbar unterschieden; **keine** vortäuschende „Verbinden"-Schaltfläche, stattdessen deaktivierter Button „Verbindung später gemeinsam einrichten". Wiederverwendet bestehende Designklassen (`v71-*`) – keine Designüberarbeitung.
- **Spätere Freigabeschritte vorbereitet:** `V7.6_GOOGLE_WORKSPACE_ACTIVATION_CHECKLIST.md` (16 Schritte, je mit Konto/Browserprofil/2FA/Recovery/Adminprüfung/minimaler OAuth-Berechtigung/betroffenen Daten/Read-only-Grenze/Rückgängig-Weg/Abbruchkriterium) und `V7.6_FINANCE_ACTIVATION_CHECKLIST.md` (11 Schritte) – reine Handlungsanleitungen für Jamal außerhalb von Cursor, keine davon in V7.6.1 ausgeführt.
- **Tests:** drei neue Testdateien – `office-finance.test.js` (**32** Prüfpunkte: Fachlogik/Persistenz von Identitäten/Fähigkeiten/Office-Aufträgen/Finance-Handoffs/Connector-Stub, kein 26. Agent, genau ein Owner je Auftrag, technische Ausführungsgrenze), `office-finance-security.test.js` (**57** Prüfpunkte: Kunden/Support blockiert (404) auf allen neun Routen + statischem Asset, OWNER erlaubt, CSRF/Origin wirksam, unbekannte Felder abgewiesen, keine Login-/OAuth-/Send-/Create-/Delete-Providerroute, datensparsames Audit, keine Tokens/Secrets/Stacktraces in Antworten) und `office-finance-ui.test.js` (**26** Prüfpunkte: acht Tabs, progressive Offenlegung, keine vortäuschende Schaltfläche, Apple-first/Google-controlled/Finance-Gap sichtbar, kein Tracking/LocalStorage/externe Ressource). Bestehende hartkodierte Routen-/Migrationszahlen-Assertions real nachgezogen (88 GET in `route-access-policy.test.js`/`server-http-router.test.js`/`agent-runtime.test.js`/`daily-work-run.test.js`, Migrationsarray 1–15 in `portal-operations-acceptance.test.js`) – keine bestehende Assertion entfernt oder abgeschwächt. Realer, reproduzierbarer Gesamtstand: **2384** Prüfpunkte / **83** Testdateien (**+115** neue Prüfpunkte in den 3 neuen Testdateien). `npm run check`/`npm test` Exit 0 (außerhalb sandboxbedingter Werkzeugeinschränkungen, siehe unten), `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert.
- **Bekannte Werkzeugbesonderheit (kein Produktfehler):** Innerhalb der sandboxierten Cursor-Werkzeugumgebung kann der reale, spawnende Integrationstest in `server-http-router.test.js` (Execution-Bridge-Fixture unter einem temporären `HOME`) mit `400` statt `200` fehlschlagen, wenn die Shell-Ausführung selbst mit eingeschränkten Dateisystemrechten läuft (Schreibzugriff nur auf das Arbeitsverzeichnis). Mit vollen Rechten (wie in einem normalen Terminal) läuft `npm test` reproduzierbar grün durch (2384/2384). Dieselbe Abweichung ist unabhängig von den V7.6.1-Änderungen auch auf dem unveränderten `origin/main`-Stand reproduzierbar – kein Regressionsrisiko durch diesen Lauf.
- **Health/Fixture read-only verifiziert:** Health (`work/check-start-gate-2026-07-19`) und Fixture (`execution-bridge-demo`) wurden in diesem Lauf nicht verändert; beide zeigen denselben, bereits vor diesem Lauf bestehenden lokalen Stand (Health: `M package.json` + drei unversionierte Dateien; Fixture: `M FIXTURE_CALC.js`) – nichts davon stammt aus V7.6.1.
- **Nicht enthalten:** Google-Kontoanmeldung, OAuth, Browserlogin, Gmail-/Kalender-/Drive-/Kontaktzugriff, E-Mail-Versand, Termin-/Datei-/Kontakterstellung, Apple-Datenmigration, iCloud-Veränderung, Bankverbindung, Zahlung, Lexoffice-/Lexware-Anbindung, Rechnungsbuchung/-versand, Plugininstallation, Deployment, Commit, Push.

## V7.5 – Agentenorganisation, tägliches HR-Coaching und Technologie-/Plugin-Marktradar (lokal umgesetzt, zur Commitprüfung bereit)

**Ausgangsstand (real gemessen):** Branch `main`, HEAD/`origin/main` `c5b4130b8a3f6dbf4f6fb0d6f8b01598e595a43e` („V7.4 Authentifizierten Canva-Abnahmelauf dokumentieren"), Working Tree sauber, 72 GET/52 POST/8 GET-Präfixe/5 POST-Präfixe/29 statische Assets/**76 Testdateien/2136 automatisierte Prüfpunkte grün**, 25 kanonische Agenten, `better-sqlite3@13.0.1`, `npm audit`: 0 Schwachstellen. Ausdrücklich: keine automatische Autonomieerhöhung, keine Plugininstallation, keine externen Requests, keine Browserautomation, keine Modell-/Providerkosten, keine Kundenaktionen, keine Veröffentlichung, kein Billing, kein Deployment, kein Commit, kein Push.

- **Ziel:** eine dauerhafte Führungs- und Entwicklungsstruktur für alle 25 bestehenden Agenten – Jamal sieht sofort die Organisation, erhält täglich für jeden Agenten einen konkreten 1%-Entwicklungsvorschlag, prüft Trainings-/Qualitäts-/Autonomieempfehlungen kontrolliert, bewertet neue Technologien/Plugins strukturiert und erkennt, welcher Agent von welchem Werkzeug profitieren könnte – ohne dass dabei jemals ungeprüft eine Autonomie- oder Pluginänderung ausgelöst wird.
- **Architekturentscheidung – keine zweite Agenten-/HR-/Pluginarchitektur:** `agent-registry.js` bleibt die einzige kanonische Quelle der 25 Agenten-IDs; die neue Organisationssicht (`agent-organization-service.js`) berechnet Bereich/Führungsebene/Berichtslinie/Qualitätsverantwortung/Autonomierahmen/Werkzeugfähigkeit ausschließlich deterministisch aus dem eingefrorenen Register (+ optional `tool-registry.js`), ohne eigene Persistenz. Kein Agent wurde erfunden, gelöscht oder umbenannt; jeder Agent ist genau einer von zehn Zielstrukturen primär zugeordnet (Finance/Controlling bleibt ehrlich leer, da kein bestehender Agent dazu passt). `orchestrator-agent` bleibt der zentrale Orchestrator (Projektmanager-Agent), HR bleibt reine Entwicklungs-/Autonomieempfehlungsinstanz, Jamal bleibt alleinige Entscheidungsinstanz.
- **Tägliches HR-Coaching (`agent-hr-coaching-service.js`):** ein persistenter, additiver täglicher Lauf erzeugt pro Kalendertag höchstens einen aktiven Vorschlagssatz mit exakt 25 rollenbezogenen Vorschlägen (1%-Verbesserung, Trainingsziel, konkrete Übung, Qualitätskriterium, möglicher Spielraum, Risiko/Grenze, benötigte Jamal-Entscheidung, HR-Empfehlung `KEEP_CURRENT`/`TRAIN_FIRST`/`RECOMMEND_SMALL_EXPANSION`/`REDUCE_SCOPE`/`ESCALATE`, Begründung, Status `PROPOSED`/`REVIEWED`/`APPROVED`/`REJECTED`/`DEFERRED`, Jamals Notiz). Rein lokal-deterministisch (FNV-1a-Tagesindex, kein Zufall, kein externer Modellaufruf), rollenspezifisch formuliert statt Floskeln, idempotent (zweiter Aufruf am selben Tag liefert denselben Lauf zurück), revisionsfähig, überschreibt nie bestehende Vorschläge. **Verbindliche Autonomiegrenze:** `RECOMMEND_SMALL_EXPANSION` bleibt ausschließlich Empfehlung; keine Funktion in diesem Modul verändert jemals eine tatsächliche Autonomiestufe – auch eine `APPROVED`-Genehmigung markiert ausschließlich den Prüfstatus, niemals eine Berechtigung.
- **Technologie-/Plugin-Marktradar (`technology-radar-service.js`):** ein persistentes Radar bewertet Technologien/Modelle/Plugins/Integrationen/Werkzeuge strukturiert (Typ, Kurzbeschreibung, mögliche Agenten, möglicher Unternehmensnutzen, Reifegrad, Sicherheits-/Datenschutzrisiko, Kostenklasse, Integrationsaufwand, Vendor-Lock-in-Risiko, Schreibzugriff/menschliche Freigabe erforderlich, Empfehlung `WATCH`/`RESEARCH`/`TEST_READ_ONLY`/`PILOT_WITH_APPROVAL`/`NOT_RECOMMENDED`/`BLOCKED`, letzter Prüfstand, nächste Prüfung, Quelle, Status). Startbestand ausschließlich aus dem bereits bestehenden `tool-registry.js` abgeleitet (u. a. GitHub, Airtable, Canva, HeyGen, Vercel, Google Workspace, Notion, n8n, Slack, HubSpot, Webflow, Lexoffice/Lexware, Make) – Reifegrad/Risiko/Empfehlung mechanisch aus bestehenden Werkzeugfeldern abgeleitet, mit zwei dokumentierten Ausnahmen (Canva/HeyGen erhalten `PILOT`/`PILOT_WITH_APPROVAL`, da bereits ein getesteter kontrollierter Pilotpfad existiert). **Keine automatische Webrecherche, keine Verbindung, keine Installation** – Jamal kann zusätzlich lokal neue Kandidaten erfassen/aktualisieren.
- **Agent-Technology-Fit:** eine kontrollierte Zuordnung zwischen Radar-Einträgen und Agenten (Nutzen, konkreter Einsatzfall, benötigte Berechtigungen, Sicherheitsgrenze, Testvoraussetzung, Empfehlung, Priorität, Status), Startbestand aus `tool-registry.js#allowedAgents` abgeleitet. Zeigt Jamal, welcher Agent welches Werkzeug unter welcher Grenze zuerst read-only testen sollte – eine Bewertung ändert ausschließlich Prüfstatus/Priorität, niemals eine Verbindung oder Autonomie.
- **Persistenz:** additive **Migration 14** (`agent_hr_daily_runs`, `agent_hr_daily_proposals`, `technology_radar_items`, `agent_technology_fit`; Migrationen 1–13 unverändert; keine Secrets/Tokens/Providerpayloads/Chain-of-Thought in den neuen Tabellen). Die Organisationssicht selbst wird bewusst **nicht** persistiert (vollständig aus `agent-registry.js` ableitbar, nichts kann veralten).
- **UI – „Agenten führen"** (`index.html#agent-leadership-view`, gerendert von `agent-leadership-ui.js`): kompakte Kopfübersicht (25 Agenten, heutiger HR-Laufstatus, wichtigste Entscheidung, höchstens drei priorisierte Agentenhinweise, Radar-Hinweis), fünf Bereiche als Tabs (`Organisation`, `Heute entwickeln`, `Autonomie prüfen`, `Technologie-Radar`, `Agenten & Werkzeuge`) mit Filtern nach Bereich/Empfehlung/Status, progressiver Offenlegung über `<details>`-Gruppen statt 25 gleichzeitiger Karten, fester Sicherheits-/Autonomiehinweistext auf jeder Ebene. Wiederverwendet bestehende Designklassen (`v71-*`, `primary-button`/`secondary-button`) – keine Designüberarbeitung.
- **API:** fünf neue OWNER_ONLY-GET-Routen (`/api/agent-leadership/summary`, `.../organization`, `.../hr-daily-run`, `.../technology-radar`, `.../agent-technology-fit`) und ein neuer POST-Aktions-Prefix `/api/agent-leadership/:action` (`create-hr-daily-run`, `review-hr-proposal`, `upsert-radar-item`, `review-agent-technology-fit`) – identische CSRF-/Origin-Prüfung/Bodylimit/Known-fields-Allowlist/`Cache-Control: no-store` wie bestehende Owner-Routen; keine Kunden-/Supportrolle, keine generischen freien Schreibendpunkte. Routenzahlen: **77 GET, 52 POST, 8 GET-Präfixe, 6 POST-Präfixe, 30 statische Assets**.
- **Audit:** neun neue, datensparsame Ereignistypen (`AGENT_ORGANIZATION_REVIEWED`, `HR_DAILY_RUN_CREATED`, `HR_PROPOSAL_REVIEWED`/`APPROVED`/`REJECTED`/`DEFERRED`, `TECH_RADAR_ITEM_CREATED`/`UPDATED`, `AGENT_TECH_FIT_REVIEWED`) – ausschließlich ID/Agenten-ID/Datum/Status/Empfehlung/Radar-ID/Entscheidungstyp, niemals vollständiger Vorschlagstext, Token, Secret oder Providerpayload.
- **Tests:** drei neue Testdateien – `agent-leadership.test.js` (**26** Prüfpunkte: exakt 25 Agenten ohne Duplikat/Lücke, Organisation aus kanonischem Register, täglicher Lauf mit exakt 25 Vorschlägen, Idempotenz, Neustartpersistenz, rollenbezogene Vorschläge, gültige Enums, keine automatische Autonomieänderung, Radar-/Fit-Persistenz, keine Tokens/Secrets, datensparsames Audit), `agent-leadership-security.test.js` (**33** Prüfpunkte: Kunden/Support blockiert (404) auf allen fünf Routen, OWNER erlaubt, CSRF/Origin wirksam, unbekannte Felder abgewiesen, keine externen Requests (statische Quelltextprüfung), keine Tokens in Antworten, datensparsames Audit) und `agent-leadership-ui.test.js` (**28** Prüfpunkte: UI rendert 25 Agenten kompakt/gruppiert, Filter, Sicherheits-/Autonomiehinweise sichtbar, kein Tracking/LocalStorage/externe Ressource, alle Aktionen ausschließlich über `/api/agent-leadership/*`). Bestehende Routen-/Migrationszahlen-Assertions real nachgezogen (77/52/8/6/30 in `route-access-policy.test.js`/`server-http-router.test.js`/`agent-runtime.test.js`/`daily-work-run.test.js`, Migrationsarray 1–14 in `portal-operations-acceptance.test.js`) – keine bestehende Assertion entfernt oder abgeschwächt. Realer, reproduzierbarer Gesamtstand: **2223** Prüfpunkte / **79** Testdateien (**+87** neue Prüfpunkte in den 3 neuen Testdateien). `npm run check`/`npm test` Exit 0, `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert.
- **Health/Fixture read-only verifiziert:** Health (`work/check-start-gate-2026-07-19`, HEAD unverändert, bekannter Dirty-Stand unverändert) und Fixture (`execution-bridge-demo`, HEAD unverändert, weiterhin ausschließlich `M FIXTURE_CALC.js`) – nichts verändert, nichts committet.
- **Nicht enthalten:** automatische Webrecherche für den Technologie-Radar, Plugininstallation/-verbindung, automatische Autonomieänderung, Kundenrolle/Supportrolle-Zugriff, Veröffentlichung, Billing, Deployment. Ein möglicher Folgeschritt (separate Freigabe erforderlich) wäre ein expliziter, separater Autonomie-Freigabekorridor, über den Jamal eine `RECOMMEND_SMALL_EXPANSION`-Empfehlung bewusst in eine tatsächliche Autonomieänderung überführen könnte – dieser existiert in V7.5 bewusst noch nicht.

## V7.5 – Unternehmensleitlinien V1.0 als verbindliche Betriebslogik verankert (lokal umgesetzt, zur Commitprüfung bereit)

**Ausgangsstand (real gemessen):** Branch `main`, HEAD/`origin/main` unverändert `c5b4130b8a3f6dbf4f6fb0d6f8b01598e595a43e`, V7.5-Agentenführung (oben) lokal bereits umgesetzt (77 GET/52 POST/8 GET-Präfixe/6 POST-Präfixe/30 statische Assets/79 Testdateien/2223 Prüfpunkte), 25 kanonische Agenten, Migration 14. Dieser Schritt baut auf der bestehenden V7.5-Grundlage auf und übersetzt die gemeinsam beschlossenen Unternehmensleitlinien in echte Betriebslogik statt reiner Dokumentation.

- **Ziel:** die Unternehmensleitlinien existieren nicht nur als Textseite, sondern wirken als **Version 1.0** direkt in Agentenorganisation, täglichem HR-Coaching, Qualitätsprüfung und Technologie-/Zukunftsradar – mit der Leitformel „Wir verfolgen das richtige Ziel, kommunizieren klar und respektvoll, verbessern uns kontinuierlich, prüfen die Wirkung, erkennen Risiken früh und bereiten die Zukunft rechtzeitig vor."
- **Kanonische Dokumentation (`COMPANY_PRINCIPLES.md`, Version `1.0`, Status `verbindliche Arbeitsgrundlage`):** 13 Abschnitte (Selbstverständnis, Unternehmenszweck, Grundwerte, Führungs- und Arbeitsprinzipien, Qualitätsverständnis, Sicherheits- und Entscheidungsplanken, Umgang mit den 25 Agenten, Technologiehaltung, Kunden- und Kommunikationshaltung, Gestaltungsprinzipien, Zukunftsverständnis, Versionierung und Änderungsregel, Inkrafttreten als Version 1.0). Alle sechs Führungsrahmen (Covey, Rosenberg, 1%-Methode, PDCA, Hochzuverlässigkeit, strategische Vorausschau) und alle zehn Grundwerte in eigenen Worten formuliert, **keine** Fremdzitate oder urheberrechtlich geschützten Buchpassagen. Nicht unveränderlich: künftige Änderungen ausschließlich bewusst, begründet und versioniert (Änderung/Grund/Auswirkung/Gültigkeitsbeginn), keine stille Veränderung.
- **Maschinenlesbares Leitlinienmodell (`company-principles.js`, Version `1.0` – identisch zur Markdown-Version):** ausschließlich strukturierte Regelobjekte (`principleId`, `category`, `title`, `internalRule`, `requiredEvidence`, `applicableContexts`, `safetyBoundary`, `version`) für sechs Führungsrahmen, zehn Grundwerte und elf Sicherheitsplanken. Sieben Kontexte (`AGENT_WORK`, `HR_COACHING`, `QUALITY_REVIEW`, `TECH_RADAR`, `FUTURE_FORESIGHT`, `CUSTOMER_COMMUNICATION`, `PROJECT_LEADERSHIP`). Markdown bleibt einzige menschliche Leitlinienwahrheit, Code enthält ausschließlich die für Prüfungen benötigten Regeln – keine zweite Dokumentationswahrheit, keine dynamische Ausführung externer Inhalte, keine freie Regel-Engine.
- **HR-Coaching verbindlich erweitert (`agent-hr-coaching-service.js`):** jeder der täglich 25 Vorschläge enthält jetzt zusätzlich `observation`, `businessMeaning`, `desiredOutcome`, `priorityReason`, `onePercentStep`, `trainingExercise`, `successMetric`, `safetyBoundary`, `pdcaStage`, `reliabilitySignal`, `communicationPattern` (Rosenberg-Reihenfolge: Beobachtung → Bedeutung → Empfehlung → Entscheidung), `ownership` (genau eine verantwortliche Agentenrolle), `expectedBenefit`, `nextReviewDate`. **Kein Floskel-Coaching:** keine identischen Standardtexte für alle 25 Agenten, Beobachtung rollenbezogen, Verbesserung an einem Tag trainierbar, Erfolg messbar. Ohne echte Leistungsdaten spricht das System ausschließlich von „Entwicklungspotenzial"/„präventivem Trainingsfokus"/„heutiger Übung" – **niemals** eine unbelegte historische Fehlerbehauptung.
- **PDCA als echter, aber minimaler Entwicklungszyklus:** neuer Vorschlag startet in `PLAN`; Jamals Genehmigung erlaubt `PLAN`→`DO`; eine spätere Ergebnisprüfung erlaubt `DO`→`CHECK`; der Übergang zu `ACT` erfordert eine explizite Abschlussentscheidung (`KEEP`/`ADJUST`/`REPEAT`/`DISCARD`). Kein automatischer Fortschritt, kein automatisches `KEEP`, kein Stufensprung (z. B. `DO`→`ACT` ohne `CHECK` wird abgelehnt), keine Autonomieänderung, kein externer Test.
- **Hochzuverlässigkeits-Signale (`agent-reliability-signal-service.js`, neue Tabelle `agent_reliability_signals` innerhalb Migration 14):** lokal erfassbare Signale (`UNCERTAINTY`/`EARLY_WARNING`/`DEVIATION`/`NEAR_MISS`/sicherheitsrelevante Eskalation) mit Agenten-ID, Bezug zu HR-Vorschlag/Radar-Eintrag, Signaltyp, sachlicher Beobachtung, möglicher Auswirkung, empfohlener Prüfung, Status, Jamal-Entscheidung, Zeitstempel. **Keine automatische Sanktion, keine automatische Autonomiereduktion, keine dramatisierende Sprache, keine erfundenen Vorfälle** – Signale können präventiv als `UNCERTAINTY` erfasst werden.
- **Zukunfts- und Szenariologik im Technologie-Radar (`technology-radar-service.js`):** jeder Radar-Eintrag um `signalType`, `signalDescription`, `timeHorizon` (`NOW`/`1_2_YEARS`/`3_5_YEARS`/`5_PLUS_YEARS`), `uncertaintyLevel` (`LOW`/`MEDIUM`/`HIGH`), drei Szenarien (`scenarioConservative`/`scenarioLikely`/`scenarioDynamic`), `strategicImpact`, `todayPreparationStep`, `nextReviewDate` erweitert. Bekannte interne Kandidaten erhalten ehrliche, neutrale Platzhalterwerte – **keine** erfundenen Marktbehauptungen, **keine** automatische Webrecherche, Szenario ist ausdrücklich keine Prognosegarantie und löst **keine** Installation/Investition aus.
- **Unternehmensnutzen (`benefitArea`):** jede HR- und Radar-Empfehlung kennzeichnet einen Nutzenbereich (`TIME_SAVING`/`QUALITY_IMPROVEMENT`/`RISK_REDUCTION`/`COST_CONTROL`/`REVENUE_OPPORTUNITY`/`CUSTOMER_VALUE`/`EMPLOYEE_RELIEF`/`STRATEGIC_READINESS`) – **keine** erfundenen Eurobeträge oder Zeitersparnisse, Quantifizierung nur bei echten Daten.
- **Ergebnisverantwortung:** jede Aufgabe/Empfehlung besitzt genau eine verantwortliche Agentenrolle (`ownerAgentId`); Zusammenarbeit verteilt Beiträge, nicht Verantwortung – geprüft in Organisation, HR-Vorschlägen und Radar-Fit, kein gleichrangiger zweiter Owner.
- **Fokus und Abschluss (`priorityBucket`):** Vorschläge/Radar-Einträge unterscheiden `NOW`/`NEXT`/`LATER`/`WATCH`; die Führungsübersicht begrenzt sich serverseitig auf höchstens drei priorisierte Hinweise oben – „weniger beginnen, Wichtiges zuverlässig abschließen". Neue Ideen landen als Radar-/Backlog-Eintrag, verdrängen aber nie automatisch den aktiven Schwerpunkt.
- **Finance-/Controlling-Lücke ehrlich verankert:** die Organisationsgruppe Finance/Controlling bleibt mit 0 Agenten bestehen und ist explizit als `CAPABILITY_GAP` markiert (kein Agent künstlich zugeordnet, kein automatischer 26. Agent); die Übersicht zeigt die Lücke nur, wenn sie für eine aktuelle Entscheidung relevant ist, mit vier Optionen (bestehenden Agenten weiterbilden, neuen Agenten definieren, externes Werkzeug/Partner nutzen, Lücke bewusst offen lassen).
- **UI „Agenten führen" zurückhaltend erweitert:** oben Leitlinienversion `1.0`, heutiger Führungsfokus, maximal drei wichtige Entscheidungen, Hinweis „Leitlinien sind Betriebsregeln, keine Motivationsseite"; im HR-Detail Beobachtung/Bedeutung/Ziel/1%-Schritt/Messkriterium/PDCA-Stand/Reliability-Signal/benötigte Entscheidung; im Radar Signal/Zeithorizont/Unsicherheit/drei progressiv aufklappbare Szenarien/heutiger Vorbereitungsschritt. Keine neue Designüberarbeitung, bestehende Tabs und `<details>`-Offenlegung wiederverwendet.
- **Persistenz:** ausschließlich additive Erweiterung der bestehenden **Migration 14** (11 neue Spalten auf `agent_hr_daily_proposals`, 11 neue Spalten auf `technology_radar_items`, eine neue Tabelle `agent_reliability_signals`) – **keine Migration 15**, Migrationen 1–13 unverändert. Von den 14 in Auftrag Abschnitt E benannten HR-Feldern sind fünf (`onePercentStep`/`trainingExercise`/`successMetric`/`safetyBoundary`/`ownership`) bewusst Aliase auf bereits bestehende Spalten und `communicationPattern` wird nur zur Lesezeit zusammengesetzt – keine unnötige doppelte Speicherung.
- **API:** bestehende V7.5-Routen erweitert (keine neue parallele Leitlinien-API); zwei neue read-only OWNER_ONLY-GET-Routen (`/api/agent-leadership/company-principles`, `.../reliability-signals`) sowie vier neue Aktionen im bestehenden POST-Aktions-Prefix (`advance-hr-pdca-stage`, `record-reliability-signal`, `review-reliability-signal`, `review-foresight-scenario`) – `Cache-Control: no-store`, fail-closed, keine externe Aktion. Neue Routenzahlen: **79 GET, 52 POST, 8 GET-Präfixe, 6 POST-Präfixe, 30 statische Assets** (+2 GET gegenüber der V7.5-Agentenführungs-Baseline 77/52/8/6/30).
- **Audit:** fünf neue, datensparsame Ereignistypen (`COMPANY_PRINCIPLES_REVIEWED`, `HR_PDCA_STAGE_CHANGED`, `RELIABILITY_SIGNAL_RECORDED`, `RELIABILITY_SIGNAL_REVIEWED`, `FORESIGHT_SCENARIO_REVIEWED`) – ausschließlich ID/Agenten-ID/Regel-ID/Status/Stufe/Entscheidungstyp/Zeitstempel, niemals vollständige Texte, Secrets oder Gedankengänge.
- **Tests:** eine neue Testdatei `company-principles.test.js` (**51** Prüfpunkte: Leitlinienversion, alle sechs Führungsrahmen/zehn Grundwerte/elf Sicherheitsplanken strukturiert und wortgleich vorhanden, HR-Lauf weiterhin exakt 25 Vorschläge mit allen neuen Feldern, kein Floskel-Coaching, PDCA-Statuswechsel mit Guards, keine automatische Autonomieänderung, Reliability-Signal-Persistenz ohne automatische Sanktion, Radar-Zeithorizont/Unsicherheit/Szenarien ohne Installation, Finance-Capability-Gap, kein 26. Agent, keine zweite Registry, datensparsames Audit, keine externen Requests/Secrets); `agent-leadership-security.test.js` um 15 neue HTTP-Prüfpunkte auf **48** erweitert (neue Routen/Aktionen OWNER_ONLY, Kundenrolle blockiert, `no-store`). Bestehende hartkodierte Routenzahlen-Assertions real nachgezogen (79 GET in `route-access-policy.test.js`/`server-http-router.test.js`/`agent-runtime.test.js`/`daily-work-run.test.js`) – keine bestehende Assertion entfernt oder abgeschwächt. Realer, reproduzierbarer Gesamtstand: **2269** Prüfpunkte / **80** Testdateien. `npm run check`/`npm test` Exit 0, `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert.
- **Health/Fixture read-only verifiziert:** nichts verändert, nichts committet.
- **Nicht enthalten:** automatische Autonomieerhöhung, externe Recherche, Plugininstallation, Veröffentlichung, Social-Media-Posting, Billing, versteckte Berechtigungsänderung, Deployment, Commit, Push.

## V7.4 – Canva-Produktionskorridor abgeschlossen: Offline-/Stub-Grundlage committed und authentifizierter externer Canva-Abnahmelauf dokumentiert

**Abschlussstand:** Der Offline-/Stub-Produktionskorridor (Schritt 1, siehe unten) wurde committed und gepusht (Commit `f2b0909498a8fe2e552529980586ad68ea9ba722`, „V7.4 Canva-Produktionskorridor: Offline- und Stub-Grundlage absichern"). Anschließend wurde – da aus dieser Cursor-Umgebung kein authentifizierter Canva-Zugriff verfügbar ist (kein Token, keine Browsersitzung, kein Canva-MCP-Server) – **außerhalb von Cursor**, über Jamals eigene, bereits mit seinem Canva-Konto verbundene **ChatGPT-Canva-Integration**, genau ein neutraler Testauftrag real durchgeführt: mehrere Vorschau-/Generierungsrunden, am Ende **ein** dauerhaft übernommenes finales Design (Canva-Design-ID `DAHQkWMxdPo`, Titel „AI Enterprise Headquarters in Twilight", 1 Seite), keine Veröffentlichung, kein Social-Media-Posting, kein Billing. Vollständige, ehrliche Dokumentation inkl. der Abweichung von der ursprünglich geplanten „genau ein Provideraufruf"-Grenze und deren fachlicher Begründung: `CANVA_AUTHENTICATED_RUN_ACCEPTANCE.md`. Das Design ist ausdrücklich eine **Arbeitsgrundlage**, kein finales Corporate Design; der im Design sichtbare „Zentrale starten"-Button ist rein visuell, der echte Start-Button bleibt Teil der Weboberfläche. Der direkte technische Übergang vom lokalen Jamal-Work-Item zum externen Canva-Konto ist weiterhin **nicht automatisiert** – dieser Lauf wurde manuell durchgeführt und nachträglich dokumentiert, nicht automatisch in `jamal_canva_productions` zurückgeschrieben.

## V7.4 Schritt 1 – Kontrollierte externe Werkzeugnutzung: Canva als erster Produktionskorridor – Offline-/Stub-Grundlage (committed und gepusht `f2b0909`; **kein echter Canva-Handoff in diesem Schritt** – siehe authentifizierter Abnahmelauf oben)

**Ausgangsstand (real gemessen):** Branch `main`, HEAD/`origin/main` `1fc142d27f2469703ae544a3862b0b872cf43a2c` („V7.3 Jamal-Arbeitsmodus: Vom Ergebniswunsch zum persistenten Agentenergebnis"), Working Tree sauber, 71 GET/52 POST/8 GET-Präfixe/5 POST-Präfixe/28 statische Assets/**73 Testdateien/2026 automatisierte Prüfpunkte grün**, `better-sqlite3@13.0.1`, `npm audit`: 0 Schwachstellen, Health/Fixture read-only unverändert. Ausdrücklich: erster Schritt der kontrollierten externen Werkzeugnutzung, ausschließlich Jamals interner Arbeitsmodus/Owner, keine Kunden-Self-Service-Nutzung, keine automatische Veröffentlichung, kein Auto-Post, kein Billing, kein Deployment, kein Commit, kein Push.

- **Ziel:** ein geeigneter interner Jamal-Arbeitsauftrag soll kontrolliert in einen Canva-Produktionsauftrag übersetzt werden können – Canva erstellt/bearbeitet genau ein Design, das Ergebnis wird zurückgeführt und intern geprüft, Jamal kann Änderungen anfordern; keine automatische Veröffentlichung, kein Social-Media-Post, kein Kundenzugriff auf Jamals Canva-Konto.
- **Verbindlicher Produktionskorridor:** ausschließlich Jamals interner Arbeitsmodus, der Plattform-Owner, kontrollierte interne Testproduktion. Noch nicht für Kunden-Self-Service, `CUSTOMER_ADMIN`, `CUSTOMER_USER`, `SUPPORT`, automatische Produktion, automatische Veröffentlichung, Social-Media-Posting.
- **Architekturentscheidung – keine zweite Canva-Architektur, keine Erweiterung des Jamal-Arbeitsmodus-Hauptflusses:** vier neue, bewusst eigenständige Module (`jamal-canva-briefing.js`, `jamal-canva-production-service.js`, `jamal-canva-routes.js`, `jamal-canva-ui.js`) statt einer Erweiterung von `jamal-work-mode.js`/`jamal-work-mode-ui.js` – diese beiden bleiben **byteidentisch unverändert** ohne jeden Canva-Bezug (erzwungen durch `jamal-work-mode-ui.test.js#"kein Provider (Canva/HeyGen)"`). Die vorhandene Canva-Connector-Grundlage aus V7.1 (Pilot-/Reviewmodell, `canva-design-result.js#validateResultReferenceUrl`) wird direkt wiederverwendet, keine zweite Connector-Architektur.
- **Canva-Eignungsprüfung (`jamal-canva-briefing.js#evaluateCanvaSuitability`):** führt vor jeder Werkzeugwahl **erneut und eigenständig** die bestehende Business-Use-Policy aus (zweite, unabhängige Sicherheitsebene, unabhängig vom bereits am Arbeitsmodus-Gate erfolgten ersten Safety-Check) und liefert `CANVA_RECOMMENDED`/`CANVA_OPTIONAL`/`CANVA_NOT_SUITABLE`/`CANVA_BLOCKED_BY_POLICY` mit Begründung, vorgeschlagenem Format, notwendigen Ausgangsmaterialien, offenen Rechte-/Consent-Fragen und geschätzter Designanzahl – keine Preisbehauptung ohne echte Daten.
- **Canva-Briefing (`buildCanvaBriefing`):** Auftragsreferenz, Projekt, Designziel, Zielgruppe, Kernaussage, gewünschtes Format, Plattform, Textinhalte, visuelle Richtung (Jamals Design-DNA „Apple statt Dubai": ruhig, hochwertig, warm, mediterran, keine Stock-Grinsen, keine visuelle Überladung, klare Hierarchie, wenige starke Elemente), Markenhinweise, Bildhinweise, Pflichtbestandteile, auszuschließende Elemente, Qualitätskriterien, Business-Use-Policy-Status, Consent-/Rechte-Status, Reviewmodus (`OWNER_REVIEW`), Veröffentlichung strukturell gesperrt (`publicationLocked: true`). Keine internen Systemprompts, keine Chain-of-Thought, keine Geheimnisse, keine Kundendaten anderer Mandanten.
- **Rechte- und Consent-Prüfung (`evaluateRightsAndConsent`):** Bildrechte, Markenverwendung, reale Person, ausreichende Zustimmung bei realistischer Personendarstellung, Avatar-Unterscheidung (`avatarConsentConfirmed` allein gilt ausdrücklich als für Dritte unzureichend). Bei ungeklärten Rechten `BLOCKED` statt Handoff, mit klarer, verständlicher Meldung. `AVATAR_CONSENT_POLICY.md` bleibt unverändert verbindlich; Connys späterer Avatar-Test wird hier bewusst nicht umgesetzt.
- **Externe Freigabegrenze (Auftrag Abschnitt F):** vor jedem echten Canva-Handoff sieht Jamal klar, welcher Auftrag/welches Format/welche Texte/welche Bildreferenzen übertragen werden, ob Rechte/Consent geklärt sind und dass noch keine Veröffentlichung erfolgt sowie ggf. externe Nutzung/Kosten entstehen können. Primäre Aktion `Canva-Produktion starten`, sekundär `Briefing ansehen`/`Noch nicht`/`Intern weiterbearbeiten`. Die Freigabe wird serverseitig gespeichert und auditiert (`CANVA_HANDOFF_APPROVED`) – keine versteckte automatische Freigabe.
- **Handoff-Statusmodell und Persistenz:** neue additive **Migration 13** (`jamal_canva_productions`; Statuswerte `DRAFT/READY_FOR_APPROVAL/APPROVED_FOR_HANDOFF/HANDOFF_STARTED/WAITING_FOR_CANVA/RESULT_RECEIVED/NEEDS_REVISION/ACCEPTED_INTERNAL/FAILED/CANCELLED/BLOCKED`; speichert interne Arbeitsreferenz, Canva-Job-/Design-ID, Design-Titel, geprüfte Edit-/View-Links, Format, Briefingversion, Status, Reviewmodus, Freigabezeitpunkt/-owner, Providerstatus, Fehlercode, Ergebniszeitpunkt, Revisionsnummer). **Keine** Zugangstoken, **keine** Provider-Secrets in SQLite, **keine** Veröffentlichungstabelle; Migrationen 1–12 byteidentisch unverändert (per Snapshot-Konstante `AUDIT_EVENT_TYPES` weiterhin eingefroren, `auth_audit_events` additiv über eine neue `AUDIT_EVENT_TYPES_AT_MIGRATION_13`-Konstante um zehn Ereignistypen erweitert, gleiches bewährtes Recreate-Muster wie bei früheren Erweiterungen dieser Tabelle).
- **Canva-Adapter/Stub-Modus:** `defaultCanvaConnectorStub` ist vollständig deterministisch (kein Netzwerkzugriff, kein Provider-Secret) und der Standard für `npm test`; ein echter Connectoraufruf ist ausschließlich über Dependency-Injection für Tests erreichbar, die Produktivroute übergibt niemals einen client-wählbaren Connector. Providerfehler/Zeitüberschreitung führen kontrolliert zu `FAILED` mit Fehlercode – keine Endlosschleife, keine automatische Wiederholung mit Kosten, kein stiller Fallback auf einen anderen Provider.
- **Interne Qualitätsprüfung (`runCanvaQualityCheck`):** rein deterministisch (kein KI-Modell, keine Zufallszahl), prüft strukturelle Vollständigkeit von Briefing/Ergebnis gegen Design-Titel/Zielgruppe/visuelle Richtung; Ergebnis `PASSED`/`PASSED_WITH_NOTES`/`REVISION_REQUIRED`/`BLOCKED` – keine automatische Kundenfreigabe, keine automatische Veröffentlichung.
- **Revisionsfluss:** „Änderung anfordern" markiert die aktuelle Revision `NEEDS_REVISION` und erzeugt im selben Aufruf sofort eine neue Briefingversion (Revisionsnummer erhöht, alte Revision bleibt als Historie in `getCanvaSafeView#history` nachvollziehbar, keine stille Überschreibung); jeder weitere externe Lauf benötigt erneut eine explizite Freigabe (`APPROVED_FOR_HANDOFF` ausschließlich über `canva-approve-handoff`).
- **Jamal-UI:** additiver, eigenständiger Bereich (`index.html#jamal-canva-card`, gerendert von `jamal-canva-ui.js`) innerhalb derselben Hauptarbeitskarte „Heute arbeiten" – Empfehlungshinweis, vorgeschlagenes Format, Briefingstatus, Rechte-/Consentstatus, primäre Aktion `Canva-Produktion starten`; während des Laufs `Canva-Produktion läuft` ohne Providerdetails im Hauptbereich; nach Ergebnis Design-Titel, Qualitätsstatus, geprüfter Link, `Passt`/`Änderung anfordern` primär, `In Canva öffnen`/`Briefing ansehen` sekundär. **Keine** Publish-/Social-Media-/Billing-Schaltfläche, kein Kundenkonto-Zugriff, keine Tokenanzeige.
- **API:** eine neue GET-Route (`GET /api/jamal-work-mode/canva-state`) und sechs neue Aktionen im bestehenden POST-Aktions-Prefix `/api/jamal-work-mode/:action` (`canva-prepare-briefing`/`canva-approve-handoff`/`canva-start-handoff`/`canva-request-revision`/`canva-accept-result`/`canva-cancel`) – beide `OWNER_ONLY`, identische CSRF/Origin-Prüfung/Bodylimit/Known-fields-Allowlist/`Cache-Control: no-store` wie der bestehende Jamal-Arbeitsmodus; kein Tenant-/Kundenbezug in diesen Handlern. Routenzahlen: **72 GET, 52 POST, 8 GET-Präfixe, 5 POST-Präfixe, 29 statische Assets** (jeweils +1 gegenüber Ausgangsstand).
- **Tests:** drei neue Testdateien – `jamal-canva-production.test.js` (**30** Prüfpunkte: Eignungsprüfung inkl. Safety-Block als eigenständige zweite Sicherheitsebene, Rechteprüfung blockiert Handoff, Briefingerzeugung/-versionierung ab 1, keine Systemprompts/Chain-of-Thought/Secrets, explizite Freigabe erforderlich, ohne Freigabe kein Connectoraufruf, nach Freigabe genau ein Aufruf, kein paralleler Handoff, doppelter Start blockiert, Providerfehler → `FAILED`, Ergebnisrückführung mit Design-ID/sicherem Link, Qualitätsprüfung, Revision mit neuer Version/nachvollziehbarer alter Revision/erneuter Freigabe, `Passt` → `ACCEPTED_INTERNAL`, keine Veröffentlichung/Billing/Social-Media-Post, vollständiges Audit), `jamal-canva-security.test.js` (**31** Prüfpunkte: `CUSTOMER_ADMIN`/`CUSTOMER_USER`/`SUPPORT ohne Grant` blockiert (`404`), `OWNER` erlaubt, Loopback-Dev-Modus kontrolliert, manipuliertes Cookie/CSRF/fremde Origin abgelehnt, unbekannte Arbeits-ID/fremde Referenz im Körper abgewiesen, kein Token in DB/Response/Audit, keine vollständige Providerantwort gespeichert, Rechte-/Consent-BLOCK verhindert Handoff, kein Publish-/Social-/Kunden-Canva-Endpunkt, `no-store`, generische Fehler ohne Stacktrace/Pfad, kein Connectoraufruf ohne Freigabe) und `jamal-canva-ui.test.js` (**29** Prüfpunkte: Empfehlung/Format/Briefingstatus/Rechtestatus sichtbar, genau eine primäre Aktion je Zustand, `Canva-Produktion starten`/`läuft`/`Passt`/`Änderung anfordern`/`In Canva öffnen` (sekundär), keine Publish-/Social-Media-/Billing-Aktion, keine Tokenanzeige, keine Providerdetails im Hauptbereich, Labels/aria-live/Fokuszustände/mobiler Breakpoint, keine externe Ressource außer geprüftem Canva-Link, kein Tracking/LocalStorage). Keine bestehende Assertion verändert/entfernt, nur reale Routen-/Migrationszahlen aktualisiert (72/52/8/5/29 in `route-access-policy.test.js`/`server-http-router.test.js`/`agent-runtime.test.js`/`daily-work-run.test.js`, Migrationsarray 1–13 in `portal-operations-acceptance.test.js`). Realer, reproduzierbarer Gesamtstand: **2136** Prüfpunkte / **76** Testdateien (**+90** neue Prüfpunkte in den 3 neuen Canva-Testdateien; die zuvor dokumentierte V7.3-Ausgangsbaseline nannte 2026/73 – die reale Nachmessung der 73 unveränderten Bestandstestdateien ergibt 2046, eine historische Dokumentationsabweichung von 20 Prüfpunkten vor diesem Schritt, unabhängig von den hier vorgenommenen Änderungen; siehe Abschlussbericht). `npm run check`/`npm test` Exit 0, `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert.
- **Testmodi:** Standard-`npm test` läuft vollständig im Fixture-/Stub-Modus (offline, deterministisch, `defaultCanvaConnectorStub`). Ein echter Canva-Abnahmelauf wurde in diesem Schritt **nicht** durchgeführt – aus dieser Cursor-Umgebung steht kein authentifizierter Canva-Connector-Zugriff zur Verfügung (kein Browser-/Netzwerkzugriff auf Jamals reale Canva-Verbindung). Es wurde **nichts simuliert und als echt behauptet**: der Offline-Produktionskorridor (Schritte 1–13 der Auftrag-Abnahme, Abschnitt R) ist vollständig fertiggestellt und manuell nachvollzogen; der reale Canva-Connectoraufruf (Schritte 1–10 der optionalen echten Abnahme) bleibt ein separater, klar benannter externer Schritt, den Jamal über seine bereits bestehende authentifizierte Canva-Verbindung (siehe Pilot-Design `DAHQeIjc2ls`) selbst auslösen muss.
- **Health/Fixture read-only erneut verifiziert:** Health (`work/check-start-gate-2026-07-19`, HEAD unverändert `395bf9e01f26d63dc4cc0bbc8343d10535c1ad64`, bekannter Dirty-Stand unverändert) und Fixture (`~/Library/Application Support/KI-Unternehmenszentrale/fixtures/execution-bridge-demo`, HEAD unverändert `e38c1985b896ed34fa5f94615fef5794c363449e`, weiterhin ausschließlich `M FIXTURE_CALC.js`) – nichts verändert, nichts committet.
- **Bereits fertig (Offline-/Stub-Grundlage):** Canva-Eignungsprüfung, Briefing, Rights-/Consent-Gate, externe Freigabegrenze, Persistenz (Migration 13), Stub-Handoff, Ergebnisrückführung, Qualitätsprüfung, Revisionsfluss, UI, Security-/Produktions-/UI-Tests (90 neue Prüfpunkte, alle grün).
- **Noch offen (bewusst nicht Teil dieses Schritts):** reale Providerfehler-/Timeoutprüfung des Node-seitigen Korridors, automatisierte Rückführung eines echten Providerergebnisses in `jamal_canva_productions`, finales Corporate Design, echtes Web-UI-Design. Der frühere reale Pilot (Design-ID `DAHQeIjc2ls`) ist ausschließlich eine **Referenz** aus V7.1 Phase C.1 – er ist **keine** Abnahme dieses neuen Jamal-Korridors. Der authentifizierte echte Canva-Handoff selbst wurde **nachträglich außerhalb von Cursor real durchgeführt und dokumentiert** (Design-ID `DAHQkWMxdPo`, siehe `CANVA_AUTHENTICATED_RUN_ACCEPTANCE.md` und Abschnitt oben) – **nicht** über den Node-seitigen Connector-Injektionspunkt, sondern über Jamals eigene ChatGPT-Canva-Integration.
- **Gemeinsame Canva-Kernarchitektur vs. getrennte Produktschicht (Architekturabgleich vor Commit):** die sicherheitskritische Ergebnis-/Linkvalidierung (`canva-design-result.js#validateResultReferenceUrl`) wird von der neuen Jamal-Schicht **direkt wiederverwendet** (keine zweite Implementierung dieser Prüfung). Bewusst **getrennt** bleiben: Persistenz (bestehender Canva-Kern nutzt eine dateisystembasierte JSON-Ablage für Kunden-/Agenturaufträge, `canva-store.js`; Jamals Schicht nutzt die bereits für den gesamten Arbeitsmodus verwendete SQLite-Datenbank, Migration 13 – zwei unterschiedliche Konsumenten mit unterschiedlichen Anforderungen, keine Vermischung von Kundenmandanten mit Jamals Owner-Modus), Statusvokabular (der bestehende Kern bildet einen siebenstufigen, mandantenfähigen Freigabeprozess ab; Jamals Schicht einen bewusst einfacheren, einstufigen Owner-Freigabeprozess) und Revisionsverfolgung (bestehende `canva-pilot-result-record.js#changeRequestHistory` für die Kundenfeedback-Schleife; Jamals einfachere `revisionNumber`-Fortschreibung). Diese Trennung ist **keine langfristig problematische Doppelarchitektur**, sondern bildet zwei unterschiedliche, real unterschiedliche Produktprozesse korrekt ab (siehe Abschlussbericht der Architekturabgleich-Runde). Ein echter, netzwerkfähiger Canva-Client existiert **in keinem** der beiden Pfade – der bestehende Kern bereitet ausschließlich ein Hand-off-Payload für eine manuell durchgeführte Aktion in Jamals authentifizierter Canva-Sitzung vor; Jamals neue Schicht kapselt diese künftige reale Aktion hinter einer einzigen injizierbaren `connector(payload)`-Schnittstelle, damit ein späterer echter Client ohne Umbau angeschlossen werden kann.
- **Nicht enthalten:** Kunden-Self-Service-Nutzung von Canva, automatische Produktion, automatische Veröffentlichung, Social-Media-Posting, Billing, Mailversand, Deployment, Mehrsprachigkeit, neue npm-Abhängigkeit, echter Canva-Connectoraufruf **aus dem Node-Server dieser Umgebung** (der reale Aufruf erfolgte nachweislich außerhalb, siehe oben). V7.4 ist mit diesem Schritt **fachlich abgeschlossen**; ein möglicher Folgeschritt (separate Freigabe erforderlich) wäre die Automatisierung der Ergebnisrückführung eines echten Providerlaufs in `jamal_canva_productions`, danach ggf. weitere kontrollierte Werkzeuge oder kontrollierte Kundenwerkzeuge.

## V7.3 – Jamal-Arbeitsmodus: vom Ergebniswunsch zum prüfbaren internen Ergebnis (lokal umgesetzt, ungesichert – Commit/Push stehen aus)

**Ausgangsstand (real gemessen):** Branch `main`, HEAD/`origin/main` `2211db461d6b72ca5c8432d15bb0a8715a3a5c31` („V7.2 Phase C Schritt 2: Kundenänderungen und Ergebnisfreigabe abschließen"), Working Tree sauber, 70 GET/52 POST/8 GET-Präfixe/4 POST-Präfixe/27 statische Assets/**69 Testdateien/1935 automatisierte Prüfpunkte grün**, `better-sqlite3@13.0.1`, `npm audit`: 0 Schwachstellen. Ausdrücklich: interne Produktivitäts-/Bedienbarkeitsphase – keine Kundenportal-Erweiterung, kein neuer externer Provider, kein Canva, kein HeyGen, keine Veröffentlichung, kein Billing, kein Mailversand, kein Deployment, kein Commit, kein Push.

- **Ziel:** die Zentrale so vereinfachen, dass Jamal sie täglich produktiv für seine eigenen Projekte nutzen kann – öffnen, sofort sehen was wichtig ist, Projekt wählen (oder priorisiertes übernehmen), nur das gewünschte Ergebnis beschreiben, der Projektmanager-Agent strukturiert und wählt Agenten, der interne Lauf läuft kontrolliert, Rückfragen nur bei echtem Bedarf, Ergebnis erscheint oben, Jamal entscheidet nur noch `Passt`/`Änderung anfordern`/`Später`/`Stoppen` (Eskalation automatisch bei Safety-Gate-Verstoß).
- **Architekturentscheidung – keine zweite Laufarchitektur:** `jamal-work-mode.js` ist eine reine, zustandslose Fachlogik (Store als Wert, keine Klassen, kein Singleton in der Logik selbst) und verwendet ausschließlich bereits bestehende Phase-C-Bausteine unverändert: `agent-registry.js` (kanonisches 25-Agenten-Register), `work-order-agent-orchestrator.js` (Agentenauswahl/Arbeitsplan/Ergebniserzeugung/Qualitätsprüfung – Wiederverwendung, kein Fork), `business-use-policy.js` (Safety-Gate). Bewusst **kein** Bezug zu Auth-Rollen, Mandanten- oder Kundenportal-Modulen (`auth-db.js`, `work-order-service.js`, `customer-portal-*`, `owner-admin-*` werden nicht importiert) – dies ist Jamals eigener interner Arbeitsmodus, kein Kundenfluss.
- **Persistenznachtrag (vor Commit-Freigabe ergänzt):** der ursprüngliche Prozessspeicher-Singleton (`server.js#jamalWorkModeStore`) ging bei jedem Serverneustart verloren – ein echter Betriebsblocker für den täglichen Arbeitsmodus. Behoben durch additive **Migration 12** (`auth-db-migrations.js`, zwei neue Tabellen `jamal_work_items`/`jamal_work_results`, Migrationen 1–11 unverändert) plus eine neue reine Übersetzungsschicht `jamal-work-mode-store.js` (`loadStore`/`persistStore`). `jamal-work-mode.js` selbst bleibt **unverändert** ohne jeden Datenbankbezug; ausschließlich `server.js` lädt vor jeder Anfrage den vollständigen Zustand aus der bestehenden Auth-Datenbank und schreibt ihn nach jeder erfolgreichen Aktion vollständig zurück. Ergebnisversionen bleiben append-only (SQLite-Trigger, kein UPDATE/DELETE), Statuswechsel sind transaktional, genau ein aktueller Arbeitswunsch. Details siehe eigener Abschnitt „V7.3 Persistenznachtrag" weiter unten. Weiterhin **kein** LocalStorage für diese Arbeitsdaten (Auftrag Abschnitt K).
- **Zentrale Arbeitskarte „Heute arbeiten"** (`index.html#jamal-work-card`, gerendert von `jamal-work-mode-ui.js`, ganz oben im Cockpit, vor dem bisherigen Tageslauf): priorisiertes Projekt, Ergebniswunsch, aktueller Status (verständliches Deutsch), nächster Schritt, offene Rückfrage falls vorhanden, letztes Ergebnis falls vorhanden. Genau **eine** primäre Hauptaktion je Status (`Ergebniswunsch festlegen`/`Arbeitslauf starten`/`Rückfrage beantworten`/`Passt`+`Änderung anfordern`/`An Jamal eskaliert`/`Neuen Ergebniswunsch festlegen`), ruhigere Sekundäraktionen (`Projekt wechseln`, `Später`, `Stoppen`, `Details`).
- **Ergebniswunsch als einziges Pflichtfeld:** Jamal gibt ausschließlich `Welches Ergebnis möchtest du erreichen?` ein; Projekt, Hinweise und Zeitpunkt sind optional; Agenten-, Tool-, Status- oder Mandantenauswahl sind **nicht** erforderlich. Der Projektmanager-Agent übernimmt Zielstrukturierung, Erkennung fehlender Informationen, 3–5 Arbeitsschritte, Agentenauswahl (max. 3 Fachagenten + genau 1 Qualitätsagent), Ergebnisformat und Qualitätskriterien – identisch zur bestehenden Orchestrierung aus V7.2 Phase C.
- **Automatische Projektlogik:** Kontinuität (zuletzt bewusst gewähltes Projekt dieses Arbeitsmodus) → Standardfall (die Zentrale selbst, `ki-unternehmenszentrale`, Selbstbezug gemäß Selbstverbesserungsauftrag) → kompakte Auswahl (höchstens fünf zuletzt verifizierte Projekte, keine lange Liste, kein Pflichtschritt).
- **Statusmodell (ausschließlich deutsch im Hauptfluss):** `Noch nicht gestartet` → `Bereit` → `In Bearbeitung` → `Rückfrage offen`/`Ergebnis bereit` → `Änderung läuft` → `Erledigt`/`Gestoppt`; `Eskalation nötig` bei Safety-Gate-`BLOCK`/`ESCALATE`. Technische Statuscodes bleiben intern bestehen, erscheinen aber nie unübersetzt im Hauptfluss (per Testsuite erzwungen).
- **Rückfragen:** höchstens eine Hauptfrage gleichzeitig, ausschließlich bei echtem fachlichen Bedarf (`work-order-agent-orchestrator.js#detectMissingInformation`, unverändert wiederverwendet) – keine Rückfrage für optionale Details, Toolwahl, Agentenauswahl oder technische Präferenzen.
- **Ergebnisdarstellung oben:** Titel, Zusammenfassung, vollständiges Ergebnis, Qualitätsstatus (deutsch übersetzt), offene Punkte, beteiligte Agenten (deutsche Rollenbezeichnung: Projektleitung/Fachbeitrag/Qualitätsprüfung statt technischer Rollencode), nächste empfohlene Aktion. `Passt` markiert intern als erledigt (keine externe Aktion, keine Veröffentlichung, kein Billing); `Änderung anfordern` nutzt dieselbe Revisionslogik wie das Kundenportal (neue unveränderliche Ergebnisversion, alte Version bleibt unverändert lesbar).
- **Informationsarchitektur „Oben arbeiten. Unten nachschauen.":** der bisherige Tageslauf (Guided Work, Execution Bridge, V6.46.0-Hybrid-Fallback) bleibt **vollständig erhalten** und unverändert funktionsfähig, ist jetzt aber standardmäßig offen, aber einklappbar unterhalb der neuen Karte (`<details class="jamal-work-secondary" open>`); technische Diagnostik (Agenten-IDs, Arbeitsplan, Versionszähler) liegt in einem separaten, standardmäßig geschlossenen Diagnosebereich innerhalb der neuen Karte.
- **Design/Bedienbarkeit (Auftrag Abschnitt K):** wiederverwendete gedeckte Badge-Farben (kein neues grelles Farbschema), Status immer zusätzlich als Text (keine reine Farbcodierung), großzügige Weißräume, echte `<label for>`-Zuordnungen, `aria-live="polite"` auf Ausgabe- und Diagnosebereich, bestehende globale `:focus-visible`-Regel unverändert genutzt (kein eigener Fokus-Reset), mobiler Breakpoint (`@media (max-width: 640px)`), keine externe Ressource/kein CDN/kein Tracking/kein LocalStorage für diese Arbeitsdaten.
- **API:** eine neue GET-Route (`GET /api/jamal-work-mode/state`) und ein neuer POST-Aktions-Prefix (`POST /api/jamal-work-mode/:action` mit `start-new-item`/`set-desired-outcome`/`choose-project`/`start-run`/`answer-clarification`/`request-change`/`mark-done`/`mark-later`/`stop`) – beide `OWNER_ONLY` (ausschließlich Jamal, kein Mandantenbezug), CSRF/Origin-Prüfung, Bodylimit, Known-fields-Allowlist, sichere generische Fehler, `Cache-Control: no-store`. Routenzahlen: **71 GET, 52 POST, 8 GET-Präfixe, 5 POST-Präfixe, 28 statische Assets** (jeweils +1 gegenüber Ausgangsstand).
- **Erste Selbstverbesserungsaufgabe (Auftrag Abschnitt L) real durchlaufen:** der interne Testauftrag „Vereinfache den täglichen Arbeitsmodus der KI-Unternehmenszentrale so, dass Jamal innerhalb von fünf Sekunden erkennt, was wichtig ist und was als Nächstes zu tun ist." wurde über `jamal-work-mode.js` real gestartet. Ausgewählte Agenten: **Projektmanager-Agent** (Orchestrator-Agent, strukturiert/koordiniert), **Content-Agent** (Kommunikations-Agent, Wording/Verständlichkeit), **QA-Agent** (QS-/Test-Agent, unabhängige Prüfung). Arbeitsplan: 5 Schritte (lesen/strukturieren → fachliche Bearbeitung → zusammenführen → Qualitätsprüfung → freigeben oder Rückfrage). Ergebnis: `RESULT_READY`, Qualitätsstatus `PASSED`, keine offenen Punkte, keine Rückfrage nötig (ausreichender Hintergrund im Auftrag selbst). **Tatsächlich umgesetzte Verbesserung:** exakt die oben beschriebene neue Arbeitskarte „Heute arbeiten" – ein einziger Blick oben im Cockpit zeigt Projekt, Ergebniswunsch/Status und die eine nächste Aktion, ohne dass Jamal weiter unten koordinieren muss.
- **Tests:** drei neue Testdateien – `jamal-work-mode.test.js` (29 Prüfpunkte: Projektlogik, Pflichtfeld, Projektmanager-Strukturierung, Agentengrenzen, Statusübergänge, Versionierung/Unveränderlichkeit, Rückfragelogik, Safety-Gate-Eskalation, sichere Fehler, keine Publish-/Billing-/Provider-/Owner-Kundenrollenmix-Funktion), `jamal-work-mode-ui.test.js` (28 Prüfpunkte: Sichtbarkeit/Reihenfolge der Arbeitskarte, genau eine Hauptaktion je Zustand, deutsche Statuswerte, keine Rohstatuscodes/technischen IDs/Systemprompts, Labels/aria-live/Fokuszustände, mobiler Breakpoint, keine externe Ressource/Tracking/LocalStorage/Publish/Billing/Provider), `jamal-work-mode-e2e.test.js` (16 Prüfpunkte: vollständiger Ablauf gegen den echten `server.js#requestHandler` mit isoliertem `HOME`/`KUZ_DATA_DIR` – öffnen, priorisiertes Projekt, Ergebniswunsch, Lauf, Ergebnis, Änderung mit neuer/unveränderter alter Version, Erledigt, sichere Fehlerfälle, Fremdhost-Ablehnung, keine externe Aktion). Eine bestehende Testdatei musste inhaltlich korrigiert werden: `daily-work-run-ui.test.js` verwendete `htmlSource.indexOf("daily-work-run-output")` als Endmarke für einen Ausschnitt – ein neuer, weiter oben stehender Kommentar in `index.html` erwähnte diese Zeichenkette zufällig zuerst und verkürzte den Ausschnitt fälschlich auf Leerstring; der Kommentar wurde umformuliert, keine Assertion wurde verändert oder abgeschwächt. Sonst **keine** bestehende Assertion verändert/entfernt, nur reale Routen-/Testzahlen aktualisiert (71/52/8/5/28 in `route-access-policy.test.js`, `server-http-router.test.js`, `agent-runtime.test.js`, `daily-work-run.test.js`). Realer, reproduzierbarer Gesamtstand: **2008** Prüfpunkte / **72** Testdateien (**+73** gegenüber Ausgangsbaseline 1935/69). `npm run check`/`npm test` Exit 0, `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert.
- **Manuelle HTTP-Abnahme (isolierter lokaler Testserver, eigener Port, eigenes `/tmp`-Verzeichnis, danach vollständig beendet/entfernt):** Startseite `200` mit sichtbarer „Heute arbeiten"-Karte, priorisiertes Projekt ohne jede Eingabe sichtbar, Ergebniswunsch ohne weitere Pflichtfelder möglich, Lauf startet und liefert Projektmanager+Fachagent+QA-Agent, Ergebnis mit Qualitätsstatus, Änderung anfordern erzeugt neue Version (alte bleibt unverändert), `Passt` markiert `Erledigt`, mobiler Breakpoint und Fokusregeln vorhanden, kein Canva-/HeyGen-Bezug, fremder Host wird mit `404` abgewiesen (kein Dev-Bypass für fremden Host), `writeOperationsBlocked: true`/`madeExternalRequest: false` in jeder Antwort.

### V7.3 Persistenznachtrag (vor Commit-Freigabe, letzter Abgleich)

**Bestätigtes Problem:** der ursprüngliche `jamalWorkModeStore` war eine reine `let`-Variable auf Modulebene von `server.js`. Ergebniswunsch, Projektzuordnung, Status, Rückfrage, Agentenauswahl, Arbeitsplan, Ergebnisversionen und Erledigt-Status lagen ausschließlich im Prozessspeicher; ein Serverneustart erzeugte über `jamalWorkMode.createStore()` einen leeren Zustand, der Browser zeigte nach einem Neustart einen scheinbar leeren Arbeitsmodus. Mehrere parallele Serverprozesse hätten jeweils ihre eigene, unabhängige In-Memory-Kopie gesehen.

- **Architekturentscheidung:** keine neue parallele Architektur, keine neue Datenbank – bestehende SQLite-Schicht (`auth-db.js`) wiederverwendet, additive **Migration 12**. `auth-db.js` bleibt einziges Modul mit direktem `better-sqlite3`-Import; `jamal-work-mode.js` bleibt unverändert und ohne jeden Datenbankbezug. Neue reine Übersetzungsschicht `jamal-work-mode-store.js` (`loadStore(db)`/`persistStore(db, store)`) reicht das bereits geöffnete `db`-Handle ausschließlich an `auth-db.js` weiter.
- **Migration 12** (`jamal_work_items`, `jamal_work_results`): camelCase-Spalten (identische Namenskonvention wie alle bisherigen Tabellen dieser Datenbank) statt der im Auftrag beispielhaft genannten snake_case-Namen; fachlich entsprechen sich `projectId`=`project_id`, `desiredOutcome`=`desired_result`, `importantNotes`=`important_notes`, `preferredTiming`=`desired_timing`, `clarifyingQuestionJson`=`clarification_question`, `selectedAgentsJson`=`selected_agents_json`, `workPlanJson`=`work_plan_json`, `completedAt`=`completed_at`. `jamal_work_results` ist wie `work_order_results` (Migration 10) append-only per SQLite-Trigger (kein UPDATE/DELETE), `UNIQUE(workItemId, versionNumber)`. Migrationen 1–11 inhaltlich unverändert.
- **Persistiertes/bewusst nicht persistiertes Datenmodell:** gespeichert werden exakt die Felder, die `jamal-work-mode.js#getSafeView` dem Browser ohnehin bereits anzeigt (Ergebniswunsch, Hinweise, Zeitpunkt, Status, Rückfrage, Arbeitsplan/Agentenauswahl, Safety-Entscheidung, Eskalation, Entscheidung, alle Ergebnisversionen) – die Persistenz erweitert also **nicht**, was Jamal ohnehin schon sieht. Bewusst **nicht** gespeichert: Systemprompts, Chain-of-Thought, Secrets, Tokens (dieses Modul erzeugt/kennt diese ohnehin nicht).
- **Neustartverhalten:** `server.js` hält keinen Prozessspeicher-Zwischenstand mehr – jede Anfrage lädt den vollständigen Zustand frisch aus der Datenbank (`loadStore`) und schreibt ihn nach einer erfolgreichen Aktion transaktional vollständig zurück (`persistStore`). Ergebniswunsch, Status, Rückfrage, Agentenauswahl, Arbeitsplan, alle Ergebnisversionen und der Erledigt-Status überstehen einen Serverneustart nachweislich (siehe Tests/manuelle Abnahme unten).
- **Mehrprozessverhalten und ehrliche Grenze:** zwei unabhängig geöffnete Datenbankverbindungen auf dasselbe Datenverzeichnis sehen sequenziell denselben persistenten Stand (WAL-Modus, `busy_timeout=5000`, real getestet). **Keine** Cluster-/Mehrprozess-Schreibgarantie wird behauptet: gleichzeitige, überlappende Schreibaktionen zweier Prozesse auf denselben Arbeitswunsch folgen dem einfachen Last-Writer-Wins-Prinzip von SQLite – für Jamal als einzigen Nutzer dieses Arbeitsmodus eine bewusst akzeptierte, dokumentierte Grenze, keine echte Mehrbenutzer-/Clusterfähigkeit.
- **Ergebnisversionierung/Rückfrage/Erledigt-Persistenz:** Ergebnisversionen bleiben unveränderlich und fortlaufend (Version 1, 2, 3, …), append-only sowohl fachlich (`jamal-work-mode.js#pushImmutableVersion`) als auch auf Datenbankebene (Trigger); eine bereits gespeicherte Version kann nachweislich nicht per `UPDATE`/`DELETE` verändert werden. Eine offene Rückfrage (Frage, Status, noch keine Antwort) sowie der `DONE`/`Erledigt`-Status samt `Passt`-Entscheidung bleiben nach Neustart nachweislich erhalten.
- **Neue Tests:** `jamal-work-mode-persistence.test.js` (18 Prüfpunkte: erster Lauf ohne Datensatz entspricht `createStore()`, Ergebnisversion 1 wird gespeichert, zwei simulierte Serverneustarts erhalten Arbeitswunsch/Status/Version 1, Änderung erzeugt Version 2 ohne Version 1 zu verändern, dritter Neustart erhält beide Versionen, `Passt`/`DONE` überlebt Neustart, offene Rückfrage überlebt Neustart, zwei unabhängige Datenbankverbindungen sehen denselben Stand, Append-only-Trigger blockieren UPDATE/DELETE, `persistStore()` ist idempotent, kein tatsächlicher LocalStorage-Zugriff, keine Datenbankdatei in Git, keine Systemprompts/Chain-of-Thought/Secrets in Migration 12, beschädigte Datenbankdatei bleibt fail-closed). Zwei bestehende, hartcodierte Migrationszahlen in `portal-operations-acceptance.test.js` wurden von `1–11` auf `1–12` korrigiert (reale Zahlenaktualisierung, keine Assertion abgeschwächt). Keine bestehende Assertion verändert/entfernt. Realer, reproduzierbarer Gesamtstand: **2026** Prüfpunkte / **73** Testdateien (**+18** gegenüber dem vorherigen V7.3-Stand 2008/72). `npm run check`/`npm test` Exit 0, `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert. Routenzahlen unverändert: **71 GET, 52 POST, 8 GET-Präfixe, 5 POST-Präfixe, 28 statische Assets** (keine neue Route für die Persistenz nötig).
- **Manuelle Neustart-Abnahme (isolierter lokaler Testserver, eigener Port `48213`, eigenes `/tmp`-`HOME`/`KUZ_DATA_DIR`, danach vollständig beendet/entfernt):** Ergebniswunsch angelegt, Lauf durchgeführt, Ergebnis sichtbar, Server kontrolliert beendet, mit derselben isolierten Datenbank neu gestartet – Ergebniswunsch/Status/Ergebnis weiterhin sichtbar; Änderung angefordert, Version 2 entstanden; erneuter Neustart, beide Versionen weiterhin sichtbar; `Passt` markiert, erneuter Neustart, Status bleibt `Erledigt`.
- **Fixture read-only erneut verifiziert** (`~/Library/Application Support/KI-Unternehmenszentrale/fixtures/execution-bridge-demo`): HEAD unverändert `e38c1985b896ed34fa5f94615fef5794c363449e`, Working Tree weiterhin ausschließlich `M FIXTURE_CALC.js` – nichts verändert, nichts committet.
- **Nicht enthalten:** Kundenportal-Erweiterung, neuer externer Provider, Canva, HeyGen, Veröffentlichung, Billing, Mailversand, Deployment, Mehrsprachigkeit, neue npm-Abhängigkeit, echte Cluster-/Mehrbenutzer-Schreibgarantie, Commit/Push. Nächster Schritt (separate Freigabe erforderlich): kontrollierte externe Werkzeugnutzung.

## V7.2 Phase C Schritt 2 – Kundenänderungsrunde, Versionierung und echte fachliche Kundenfreigabe (lokal umgesetzt, ungesichert – Commit/Push stehen aus)

**Ausgangsstand (real, isoliert und zweifach reproduzierbar nachgemessen, siehe Testabschnitt):** Branch `main`, HEAD/`origin/main` `535e648e6cbe1d8653dc7a31e01b36060626c299`, Working Tree sauber, 70 GET/52 POST/8 GET-Präfixe/4 POST-Präfixe/27 statische Assets/**65 Testdateien/1829 automatisierte Prüfpunkte grün** (nicht 1849 – eine abweichende Zwischenbehauptung wurde durch zwei unabhängige, isolierte Testläufe exakt auf diesem Commit widerlegt und von Jamal korrigiert), `better-sqlite3@13.0.1`, `npm audit`: 0 Schwachstellen. Ausdrücklich weiterhin: kein externer Provideraufruf, kein Canva, kein HeyGen, keine Veröffentlichung, kein Billing, kein automatischer Kostenverbrauch, kein Deployment, kein Commit, kein Push.

- **Ziel:** ein Ergebnis mit Status `RESULT_READY` kann jetzt vom Kunden entweder mit einer konkreten Änderung zurückgegeben (kontrollierter Revisionslauf, neue unveränderliche Ergebnisversion) oder fachlich freigegeben werden (`CUSTOMER_APPROVED`). **Ausschließlich der Kunde** trifft diese fachliche Entscheidung – Jamal/OWNER bleibt Plattformbetreiber ohne Freigabe-, Ablehnungs- oder Änderungsanforderungsrecht im Namen des Kunden. Eine Kundenfreigabe löst **keine** Veröffentlichung, kein Billing, keine externe Provideraktion aus.
- **Statusmodell (aktiviert, keine neuen Werte nötig):** die bereits in Migration 8 vorbereiteten Statuswerte `CHANGES_REQUESTED` und `CUSTOMER_APPROVED` werden erstmals real erreicht: `RESULT_READY → CHANGES_REQUESTED → (Revisionslauf, synchron) → RESULT_READY` (neue Version) bzw. `RESULT_READY → CUSTOMER_APPROVED` (terminal für die Kundenentscheidung, technisch bleiben spätere Änderungswünsche auf derselben freigegebenen Version weiterhin möglich, da eine neue Version dadurch nicht rückwirkend entfreigegeben wird).
- **Neue Servicegrenze (zwei Module, keine künstliche Modulvermehrung):** `work-order-change-service.js` (Änderungswunsch, drei Safety-Gates, Revisionslauf-Orchestrierung, Kunden-/Owner-Lesesicht) und `work-order-approval-service.js` (Kundenfreigabe, Idempotenz, Kunden-/Owner-Lesesicht). Beide folgen demselben Muster wie `work-order-execution-service.js` aus Schritt 1 (Statusübergänge, atomare Transaktionen, `auditSafe`).
- **Revisionslauf – Wiederverwendung statt Duplikat:** `work-order-agent-orchestrator.js` bleibt **unverändert**; ein Änderungswunsch komponiert die ursprünglichen Auftragsfelder mit dem Änderungswunschtext (`requestText`/`preserveText`/`importantNote`) zu einem einzigen Eingabeobjekt und durchläuft denselben deterministischen Orchestrator wie ein Erstlauf. Der Revisionslauf ist in diesem Schritt **synchron** (identische Architekturentscheidung wie Schritt 1 – siehe dortige Begründung zum Startbutton), läuft vollständig innerhalb derselben HTTP-Anfrage durch und liefert die neue Version direkt in der Antwort.
- **Unveränderliche Ergebnisversionen:** jeder Revisionslauf erzeugt über die bestehende, bereits append-only geschützte `work_order_results`-Tabelle (Schritt 1) eine neue, fortlaufende Version – niemals ein Überschreiben. Alle bisherigen Versionen bleiben inhaltlich unverändert einsehbar.
- **Drei Safety-Gates (Auftrag Abschnitt K), dieselbe `business-use-policy.js` wie Schritt 1/Phase B, keine neue Regel:** (1) beim Einreichen des Änderungswunsches, (2) unmittelbar vor dem eigentlichen Revisionslauf, (3) unmittelbar vor der Übernahme des neuen Ergebnisses. `BLOCK` verhindert den jeweiligen Schritt vollständig (generische Kundenmeldung `400`, `WORK_ORDER_CHANGE_BLOCKED_BY_POLICY`-Audit plus Verstoßprotokoll); `ESCALATE` versetzt den Auftrag direkt auf `ESCALATED` (kein Änderungswunsch-Datensatz, `WORK_ORDER_CHANGE_ESCALATED_BY_POLICY`).
- **Idempotenz und Parallelität (drei Verteidigungslinien wie in `auth-db.js` dokumentiert):** fachliche Prüfung im Service, atomares Compare-and-Set beim Statusübergang, zusätzlich ein partieller `UNIQUE`-Index auf `work_order_change_requests.workOrderId` (höchstens ein aktiver Änderungswunsch je Auftrag) sowie ein `UNIQUE`-Index auf `work_order_customer_approvals.resultId` (keine doppelte Freigabe derselben Version) – beide Invarianten real gegen die Datenbank verifiziert (`work-order-change.test.js`).
- **Datenmodell (Migration 11, additiv, forward-only, append-only, Migrationen 1–10 unverändert – Migration 10 dabei per Snapshot-Konstante `AUDIT_EVENT_TYPES_AT_MIGRATION_10` byte-identisch gehalten):** zwei neue Tabellen `work_order_change_requests` (`id`, `workOrderId`, `tenantId`, `requestedByUserId`, `basedOnResultId`, `requestText`/`preserveText`/`importantNote`, `status` ∈ `SUBMITTED`/`IN_PROGRESS`/`COMPLETED`/`CANCELLED`, `runId`, `resultingResultId`, Zeitstempel) und `work_order_customer_approvals` (`id`, `workOrderId`, `tenantId`, `resultId`, `approvedByUserId`, `approvalVersion`, `approvalNote`, `approvedAt`) – append-only per SQLite-Trigger (real gegen ein `UPDATE` verifiziert), begrenzte Textlängen (`requestText` 2000/`preserveText` 1000/`importantNote` 500/`approvalNote` 1000 Zeichen).
- **API:** keine neue Route auf oberster Ebene – sechs neue Endpunkte hängen ausschließlich in den bereits bestehenden Prefix-Dispatchern: `GET/POST /api/portal/work-orders/:id/change-request`, `GET /api/portal/work-orders/:id/change-requests`, `POST /api/portal/work-orders/:id/approve`, `GET /api/portal/work-orders/:id/result-versions` (Kunde), `GET /api/owner/work-orders/:id/change-requests`, `GET /api/owner/work-orders/:id/result-versions` (Owner, rein lesend – bewusst **keine** `/approve`- oder `/change-request`-Route für den Owner). Routenzahlen bleiben exakt unverändert: **70 GET, 52 POST, 8 GET-Präfixe, 4 POST-Präfixe, 27 statische Assets**. Jede Route: CSRF/Origin-Prüfung, Bodylimit, Known-fields-Allowlist, sichere generische Fehler, `Cache-Control: no-store`, Audit.
- **Kundenportal** (`portal.html`/`portal-ui.js`, neue Bereiche `work-order-change-request-section`/`work-order-approve-section`/`work-order-versions-section`, sichtbar bei `RESULT_READY`/`CHANGES_REQUESTED`/`CUSTOMER_APPROVED`): Formular für Änderungswunsch (Text/zu erhalten/Hinweis), Freigabebutton mit optionaler Notiz, rein lesende Versionsliste (keine neuen Bedienelemente dort). Statusabhängige, ehrliche Folgehinweise lösen den Schritt-1-Platzhalter „folgen im nächsten Schritt" ab. Weiterhin keine Agentenliste, kein „von Jamal geprüft", keine Veröffentlichungs-/Billing-/Providererwähnung.
- **Owner-Betriebsansicht** (`owner-work-orders.html`/`owner-work-orders.js`, neue Bereiche „Änderungswünsche (Kunde)" + „Ergebnisversionen und Kundenfreigabe"): ausschließlich lesend, Statuswerte in deutscher Sprache übersetzt, Freigabeinformation (wer/wann) sichtbar – **kein** Button/Formular zum Freigeben, Ablehnen oder Anfordern im Namen des Kunden (technisch real geprüft: `owner-work-orders.js` ruft niemals eine schreibende `/approve`- oder `/change-request`-Route auf).
- **Audit** (`auth-audit.js`, additiv): neun Ereignistypen (`WORK_ORDER_CHANGES_REQUESTED`, `WORK_ORDER_CHANGE_REQUEST_STARTED`, `WORK_ORDER_CHANGE_REQUEST_COMPLETED`, `WORK_ORDER_CHANGE_REQUEST_FAILED`, `WORK_ORDER_CHANGE_REQUEST_CANCELLED`, `WORK_ORDER_RESULT_VERSION_CREATED`, `WORK_ORDER_RESULT_APPROVED_BY_CUSTOMER`, `WORK_ORDER_CHANGE_BLOCKED_BY_POLICY`, `WORK_ORDER_CHANGE_ESCALATED_BY_POLICY`); erweiterte Metadaten-Allowlist um `resultId`/`resultVersion`/`changeRequestId`; niemals Änderungswunschtext, Ergebnistext, Freigabenotiz im Klartext.
- **Tests:** zwei neue Testdateien – `work-order-change.test.js` (36 Prüfpunkte: erfolgreicher Änderungswunsch mit Versionssprung, alte Version bleibt lesbar, Owner-Lesesicht, Feldvalidierung/Längenlimits, beide Safety-Gates BLOCK/ESCALATE, Datenbank-Idempotenzinvarianten inkl. Append-only, vollständiger Freigabefluss inkl. erneuter-Freigabe-409, keine Owner-Schreibroute, Mandantentrennung, CSRF/Origin, Audit-Vollständigkeit/-Allowlist/-Datensparsamkeit, `no-store`, keine Publish-/Billing-/Providerroute) und `work-order-change-ui.test.js` (11 Prüfpunkte: Owner-Bereiche vorhanden und rein lesend, keine schreibende Route im Owner-Skript, übersetzte Statuswerte, Leerzustände). Bestehende `work-order-execution.test.js` (Prüfpunkt 28: Kundenfreigaberoute liefert jetzt legitim 200 statt 404), `work-order-result-ui.test.js` (Prüfpunkte 8/9 bewusst umgekehrt: Anwesenheit statt Abwesenheit der neuen Funktionen, ein zusätzlicher Prüfpunkt für die rein lesende Versionsansicht) und `portal-operations-acceptance.test.js` (Migration 11 statt 10) punktuell angepasst. Bestehende Tests **nicht** abgeschwächt. Realer, reproduzierbarer Vergleich (zwei unabhängige, isolierte Testläufe exakt auf dem Ausgangscommit, siehe oben): Ausgangsstand **1829** Prüfpunkte / **65** Testdateien, Stand nach Umsetzung C–P **1877** Prüfpunkte / **67** Testdateien (**+48**, vollständig aufgeschlüsselt: +36 `work-order-change.test.js`, +11 `work-order-change-ui.test.js`, +1 zusätzlicher Prüfpunkt in `work-order-result-ui.test.js`). `npm run check`/`npm test` Exit 0, `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert.
- **Letzter Qualitätsabgleich vor Commit (dediziert, additiv, keine bestehende Assertion verändert/entfernt):** zwei zusätzliche, ursprünglich im Auftrag vorgesehene dedizierte Testdateien ergänzt, weil die Einzelgarantien für Freigabe und Versionssicherheit zuvor nur verteilt/teilweise direkt geprüft waren – `work-order-approval.test.js` (38 Prüfpunkte: Freigabe ausschließlich aus `RESULT_READY` für alle neun übrigen Statuswerte einzeln negativ geprüft, Freigabedatensatz korrekt an Ergebnis-ID/-Version gebunden, Atomarität per simulierter Race Condition, doppelte Freigabe blockiert, alte Version strukturell nicht freigebbar, CUSTOMER_ADMIN **und** CUSTOMER_USER dürfen freigeben, OWNER/SUPPORT/fremder Tenant blockiert, Tenant-/User-ID im Körper abgewiesen, Notizlimit, kein Publish/Billing/Provider, Audit-Allowlist, `no-store`, manipuliertes Cookie/CSRF/Origin, suspendierter Benutzer/Mandant, keine Stacktraces/Secrets) und `work-order-version-security.test.js` (20 Prüfpunkte: strikte Mandantentrennung der Versionsliste in beide Richtungen, fremde `workOrderId` liefert 404, fehlender `resultId`-Parameter auf allen Routen strukturell bestätigt, Append-only-Unveränderlichkeit, fortlaufende lückenlose Versionsnummern über drei Versionen, ausschließlich neueste Version nach Freigabe markiert, Tenant-Bindung von Freigabe/Änderungswunsch direkt in der Datenbank verifiziert, keine Agenteninterna/Systemprompts/Chain-of-Thought/Secrets in der Kundenantwort, manipuliertes Cookie/CSRF/Origin, suspendierter Benutzer/Mandant, kein Publish/Billing/Provider). Beide Dateien in `package.json` (`test`/`check`) registriert. Neuer, real gemessener Gesamtstand: **1935** Prüfpunkte / **69** Testdateien (**+58** gegenüber Stand nach C–P, **+106** gegenüber Ausgangsbaseline 1829/65). `npm run check`/`npm test` weiterhin Exit 0.
- **Nicht enthalten:** externer Provideraufruf, Canva, HeyGen, Veröffentlichung, Billing, automatischer Kostenverbrauch, Owner-Freigabe/-Ablehnung/-Änderungsanforderung im Namen des Kunden, asynchroner/im Hintergrund laufender Revisionslauf, Mehrsprachigkeit, neue npm-Abhängigkeit, Commit/Push/Deployment.

## V7.2 Phase C Schritt 1 – Kontrollierte Übergabe eines Kundenauftrags an die Agentenzentrale und erstes prüfbares internes Ergebnis (lokal umgesetzt, ungesichert – Commit/Push stehen aus)

**Ausgangsstand:** Branch `main`, HEAD/`origin/main` `3815694c1b0a66ef3f3edf14438b01bca6b4c66c`, Working Tree sauber, 70 GET/52 POST/8 GET-Präfixe/4 POST-Präfixe/27 statische Assets/62 Testdateien/1749 automatisierte Prüfpunkte grün, `better-sqlite3@13.0.1`, `npm audit`: 0 Schwachstellen. Ausdrücklich weiterhin: kein externer Provideraufruf, kein Canva, kein HeyGen, keine Veröffentlichung, kein Billing, kein automatischer Kostenverbrauch, kein Deployment, kein Commit, kein Push.

- **Ziel:** ein Auftrag mit Status `READY_FOR_PROCESSING` kann erstmals kontrolliert an die bestehende interne Agentenzentrale übergeben werden: Projektmanager-Agent strukturiert, passende Fachagenten arbeiten rein intern/textbasiert, ein Qualitätsagent prüft, ein prüfbares internes Ergebnis wird gespeichert, der Auftrag wird `RESULT_READY`, der Kunde sieht das Ergebnis im Portal zur Ansicht – **noch keine** Veröffentlichung, **noch keine** Kundenfreigabe.
- **Architekturentscheidung (Startlogik):** der Lauf startet in diesem Schritt ausschließlich über eine kontrollierte Owner-Aktion (`POST /api/owner/work-orders/:id/run`, Startbutton „Technischen Agentenlauf starten“), **nicht** automatisch bei `READY_FOR_PROCESSING`. Begründung: `work-order-routes.test.js`/`work-order-security.test.js`/`work-order-ui.test.js` prüfen bereits fest, dass ein frisch angelegter Auftrag sofort innerhalb derselben Anfrage mit `READY_FOR_PROCESSING` zurückkommt; ein automatischer synchroner Lauf innerhalb der Anlegefunktion hätte diese nicht abzuschwächenden Tests gebrochen oder einen asynchronen Hintergrundprozess/eine Warteschlange erfordert (widerspricht „keine künstliche Modulvermehrung“/„reproduzierbarer synchroner Lauf ohne Polling“). Der Startbutton ist ausdrücklich **keine** fachliche Freigabe (Jamal beurteilt keinen Auftragstext); `work-order-execution-service.js#startRunForWorkOrder` ist bewusst mit optionalem `actorUserId` geschnitten, damit ein späterer automatischer Trigger dieselbe Funktion unverändert wiederverwenden kann.
- **Neue Servicegrenze (drei Module, keine künstliche Modulvermehrung):** `work-order-agent-orchestrator.js` (reine, deterministische Funktion ohne I/O: Agentenauswahl, Arbeitsplan, Erkennung fehlender Angaben, Ergebniserzeugung, Qualitätsprüfung), `work-order-execution-service.js` (Statusübergänge, Idempotenz-/Parallelitätsschutz, zweites Safety-Gate, atomare Persistenz), `work-order-result-service.js` (reine Lese-/Formatierungsschicht für Kunden-/Owner-Ansicht, erzeugt/ändert nie ein Ergebnis).
- **Projektmanager-Agent und Agentenauswahl:** deterministische Auswahl aus dem bestehenden kanonischen 25-Agenten-Register (`agent-registry.js`, ergänzt um die Rollen-Mappings `Content-Agent`→`communication-agent` und `QA-Agent`→`quality-test-agent`); immer genau ein Projektmanager-Agent, bis zu drei Fachagenten anhand von Stichwortmustern im Auftragstext (z. B. Design-/Technik-Signalwörter), genau ein Qualitätsagent. Jede Auswahl speichert Auswahlgrund, Rolle und Reihenfolge (`work_order_run_agents`). Keine neue Agentenrolle, kein externer Tool-/Provideraufruf, keine Kosten, keine Veröffentlichung ausgelöst.
- **Fachagenten:** ausschließlich interne, textbasierte, deterministische Bearbeitung ohne jede I/O – kein externer API-Aufruf, kein Canva, kein HeyGen, kein GitHub-/Airtable-Schreibzugriff, keine Kundenkommunikation außerhalb des Portals.
- **Fehlende Angaben / Rückfrage:** erkennt `work-order-agent-orchestrator.js#detectMissingInformation` eine echte fachliche Lücke (z. B. sehr knappe Beschreibung ohne Hintergrund), wird der Auftrag `NEEDS_CLARIFICATION` mit konkreter, im Portal sichtbarer Rückfrage statt eines Ergebnisses; der bestehende Resubmit-Fluss aus Phase B Schritt 1 greift unverändert.
- **Zweites Safety-Gate (Auftrag Abschnitt J):** greift jetzt zusätzlich zum bestehenden Gate beim Auftragseingang ein zweites Mal unmittelbar vor der Agentenausführung. `BLOCK` verhindert den Lauf vollständig (generische Kundenmeldung `400`, kein Run-/Agenten-/Ergebnisdatensatz, `WORK_ORDER_EXECUTION_BLOCKED_BY_POLICY`-Audit plus Verstoßprotokoll wie in Phase B). `ESCALATE` setzt den Auftrag direkt auf `ESCALATED` (ebenfalls kein Run, kein Agent, kein Ergebnis) und liefert `200 { started: false, workOrderStatus: "ESCALATED", run: null }` statt eines Fehlerstatuscodes – das kontrollierte Nicht-Starten ist selbst der korrekte, erwartete Ausgang dieser Route.
- **Fehlerverhalten:** ein unerwarteter technischer Fehler während Agentenauswahl/Arbeitsplan/Ergebniserzeugung führt zu einem `FAILED`-Lauf (`failureCode: ORCHESTRATION_ERROR`) und der Auftrag kehrt kontrolliert zu `READY_FOR_PROCESSING` zurück (später erneut startbar) – niemals fälschlich `RESULT_READY`, keine stille Teilverarbeitung, generische Fehlermeldung ohne Stacktrace/interne Details.
- **Datenmodell (Migration 10, additiv, forward-only, append-only, Migrationen 1–9 unverändert):** drei neue Tabellen `work_order_runs` (`id`, `workOrderId`, `tenantId`, `runNumber`, `status` ∈ `PREPARED`/`IN_PROGRESS`/`NEEDS_CLARIFICATION`/`COMPLETED`/`FAILED`/`CANCELLED`, `startedAt`/`completedAt`/`failedAt`/`failureCode`, `orchestratorVersion`, `createdAt`), `work_order_run_agents` (`runId`, `agentKey`, `agentRole` ∈ `PROJECT_MANAGER`/`SPECIALIST`/`QUALITY`, `sequenceNumber`, `selectionReason`, `status`, `startedAt`/`completedAt`), `work_order_results` (`id`, `workOrderId`, `runId`, `tenantId`, `versionNumber`, `resultTitle`/`resultSummary`/`resultBody`, `qualityStatus` ∈ `PASSED`/`PASSED_WITH_NOTES`, `qualityNote`, `openPoints`, `createdAt`). Tenant-/Work-Order-/Run-Foreign-Keys, Status-`CHECK`-Constraints, begrenzte Textlängen, append-only per SQLite-Trigger (kein UPDATE/DELETE auf `work_order_results`), Ergebnisversionen unveränderlich (jede spätere Revision erzeugt eine neue Version statt zu überschreiben).
- **Idempotenz und Parallelität:** ein aktiver Lauf (`PREPARED`/`IN_PROGRESS`) blockiert jeden weiteren parallelen Start für denselben Auftrag atomar innerhalb derselben Transaktion (`getActiveWorkOrderRunForWorkOrder`, geprüft sowohl vor als auch innerhalb der Transaktion gegen Races); Run-Nummern sind je Auftrag fortlaufend; ein zweiter Startversuch auf einen bereits verarbeiteten Auftrag liefert `409`; ein `FAILED`-Lauf ist später erneut startbar; alte Ergebnisse bleiben unverändert erhalten.
- **API:** keine neue Route auf oberster Ebene – fünf neue Endpunkte hängen ausschließlich in den bereits bestehenden Prefix-Dispatchern: `GET /api/portal/work-orders/:id/result`, `GET /api/portal/work-orders/:id/run-status` (Kunde, Tenant ausschließlich aus Session), `GET /api/owner/work-orders/:id/runs`, `GET /api/owner/work-orders/:id/runs/:runId`, `POST /api/owner/work-orders/:id/run` (Owner). Routenzahlen bleiben exakt unverändert: **70 GET, 52 POST, 8 GET-Präfixe, 4 POST-Präfixe, 27 statische Assets**. Jede Route: CSRF/Origin-Prüfung (zentral im Guard), Bodylimit, Known-fields-Allowlist (leere Menge beim Run-Start – ein Tenant-/User-ID-Feld im Körper wird mit `400` abgewiesen), sichere generische Fehler, `Cache-Control: no-store`, Audit.
- **Kundenportal-Ergebnisansicht** (`portal.html`/`portal-ui.js`, neuer Bereich `work-order-result-section`, wird bei `RESULT_READY` nachgeladen): Ergebnisversion, Qualitätsstatus in deutscher Sprache, Zusammenfassung, vollständiges Ergebnis, optionale Qualitätsnotiz/offene Punkte, verbindlicher Hinweis „Dieses Ergebnis wurde noch nicht vom Kunden freigegeben.“ und ehrlicher Folgehinweis „Sie können das Ergebnis derzeit ansehen. Änderungswünsche und Freigabe folgen im nächsten Schritt.“ **Keine** Agentenliste, **keine** Freigabe-/Änderungsfunktion, **keine** Erwähnung von Veröffentlichung/Billing/Provider, kein „von Jamal geprüft“.
- **Owner-Betriebsansicht** (`/owner/auftraege`, neuer Bereich „Technischer Laufstatus“ + Startbutton): zeigt je Lauf Run-Nummer, Status, ausgewählte Agenten (Rolle, Auswahlgrund, Status), Start-/Endzeit, Fehlercode, Qualitätsstatus – ausschließlich technische Betriebsinformation, keine Systemprompts, keine Chain-of-Thought, keine Providerdaten, keine fachliche Freigabefunktion.
- **Audit** (`auth-audit.js`, additiv): neun Ereignistypen (`WORK_ORDER_RUN_PREPARED`, `WORK_ORDER_RUN_STARTED`, `WORK_ORDER_RUN_COMPLETED`, `WORK_ORDER_RUN_FAILED`, `WORK_ORDER_RUN_CANCELLED`, `WORK_ORDER_RESULT_CREATED`, `WORK_ORDER_AGENT_SELECTED`, `WORK_ORDER_EXECUTION_BLOCKED_BY_POLICY`, `WORK_ORDER_EXECUTION_ESCALATED_BY_POLICY`); erweiterte Metadaten-Allowlist um `runId`/`agentKey`/`failureCode`; niemals Auftragstext, Ergebnistext, Systemprompts, Chain-of-Thought, Tokens, Passwörter, IP-/User-Agent-Klartext.
- **Tests:** drei neue Testdateien – `work-order-execution.test.js` (33 Prüfpunkte: Laufstart/-status, Run-Nummerierung, Parallelitätsschutz, Agentenauswahl/-grenzen, Arbeitsplan, Ergebnis/Version/Unveränderlichkeit, `NEEDS_CLARIFICATION`, `FAILED`+Neustart, keine Owner-/Kundenfreigabe, Audit-Vollständigkeit/-Datensparsamkeit, `no-store`, sichere Fehler), `work-order-execution-security.test.js` (30 Prüfpunkte: Mandantentrennung bei Ergebnis/Runs, fremde IDs, Tenant-/User-ID-Manipulation, Rollen-/Grant-/Sperrzustände, Cookie/CSRF/Origin, zweites Safety-Gate BLOCK/ESCALATE inkl. Datenbankinvarianten, kein paralleler Lauf, keine Publish-/Billing-/Providerroute, Audit-Allowlist), `work-order-result-ui.test.js` (17 Prüfpunkte: Ergebnisdarstellung, deutscher Qualitätsstatus, keine Agentenlog-/Systemprompt-/Freigabe-/Änderungsanzeige, ehrlicher Folgehinweis, aria-live, Labels, keine externe Ressource, kein Storage). Bestehende `portal-operations-acceptance.test.js` punktuell an Migration 10 angepasst (erwartete Versionsliste `1–10`). Bestehende Tests **nicht** abgeschwächt. Realer, reproduzierbarer Vergleich: Ausgangsstand 1749 Prüfpunkte / 62 Testdateien, aktueller Stand **1829** Prüfpunkte / **65** Testdateien (**+80**). `npm run check`/`npm test` Exit 0, `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert, `git diff --check` sauber.
- **Nicht enthalten:** externer Provideraufruf, Canva, HeyGen, Veröffentlichung, Billing, automatischer Kostenverbrauch, reguläre Owner-Freigabe, echte Kundenfreigabe (`CUSTOMER_APPROVED`/`CHANGES_REQUESTED` bleiben unerreichbar), Mehrsprachigkeit, neue npm-Abhängigkeit, Commit/Push/Deployment. Nächster Schritt (separate Freigabe erforderlich): Kundenänderungsrunde und echte fachliche Kundenfreigabe eines Ergebnisses (Phase C Schritt 2).

## V7.2 Phase B – Schutz- und Einwilligungsgrundlage vor Commit der Self-Service-Korrektur (lokal umgesetzt, ungesichert – Commit/Push stehen aus)

**Vor dem Commit der Self-Service-Korrektur (siehe nächster Abschnitt) verbindlich ergänzt.** Ausgangsstand: Branch `main`, HEAD/`origin/main` `be9848afc06c26dc056b33811dc2560e56696b5a`, Working Tree unverändert gegenüber dem read-only geprüften Self-Service-Stand, 1693 Prüfpunkte grün, 60 Testdateien, `npm audit`: 0 Schwachstellen. Jamal bleibt durchgehend **Plattformbetreiber, kein fachlicher Pflichtprüfer** normaler Kundenaufträge – die drei Ergänzungen unten verschärfen ausschließlich Ausnahmefälle.

- **Business-Use-Policy (`BUSINESS_USE_POLICY.md`):** verbindlicher Zweck („ausschließlich legitime geschäftliche Nutzung"), Positivliste (Werbung, Websites, Präsentationen, Prozesse, Kundenkommunikation, Recherche, Bild-/Video-/Textproduktion) und Negativliste (u. a. sexualisierte Inhalte mit Minderjährigen, rassistische/antisemitische/menschenverachtende Inhalte, Betrug/Identitätsmissbrauch, illegale Zwecke, unrechtmäßige Überwachung, politische Manipulation, Persönlichkeits-/Marken-/Urheberrechtsverletzung). Hält fest: normale legitime Aufträge laufen ohne Jamal, klar verbotene Aufträge werden blockiert, Grenzfälle eskalieren, schwere/wiederholte Verstöße **können** zu Verwarnung, Auftragssperre, Benutzersperre, Sessionwiderruf, Mandantensperre, Vertragsbeendigung oder Lizenzentzug führen – **kein automatischer, endgültiger** Lizenzentzug/Vertragsbeendigung in diesem Schritt. Ausdrücklich **keine** fertigen AGB, **keine** abgeschlossene juristische Prüfung.
- **Technisches Safety-Gate (`business-use-policy.js`, `SAFETY_ENFORCEMENT_MODEL.md`):** reine, deterministische Funktion ohne Datenbankzugriff, aufgerufen aus `work-order-service.js` **vor** jeder Speicherung (Erstanlage UND erneutes Einreichen), noch vor der automatischen Vollständigkeitsregel aus Schritt 1. Liefert `decision` (`ALLOW`/`BLOCK`/`ESCALATE`), `reasonCode`, `severity` (`LOW`/`MEDIUM`/`HIGH`/`CRITICAL`), `policyVersion`. `BLOCK` verhindert jede Speicherung als normaler Auftrag (generische Kundenmeldung, `400`); `ESCALATE` speichert den Auftrag direkt mit Status `ESCALATED` statt der automatischen Einstufung. Ausdrücklich **kein** vollständiger KI-Moderationsersatz – ein kleiner, konservativer, regelbasierter Filter für eindeutige Signale (fünf BLOCK-Kategorien, drei ESCALATE-Kategorien); künftiger modell-/providergestützter Check mit Deny-/Escalate-by-default bei Unsicherheit ist dokumentiert, nicht implementiert.
- **Verstoß-/Sanktionsgrundlage (Migration 9, `policy_violations`):** additive, append-only Tabelle (`id`, `tenantId`, `userId`, `workOrderId` optional, `reasonCode`, `severity`, `actionTaken`, `createdAt`) – **niemals** der vollständige Auftragstext. `actionTaken` ∈ `BLOCKED`/`ESCALATED`/`WARNED`/`USER_SUSPENDED`/`TENANT_SUSPENDED`/`LICENSE_REVIEW_REQUIRED`; die letzten drei sind Datenmodell-Vorbereitung für künftige **manuelle** Owner-Aktionen, werden von keiner automatischen Funktion dieses Schritts gesetzt. Bei `severity=CRITICAL` (eng gefasst auf `CHILD_SAFETY_VIOLATION`): automatischer Sofort-Sessionwiderruf des handelnden Benutzers (`USER_SESSIONS_REVOKED`, `actorUserId: null`) und `actionTaken=LICENSE_REVIEW_REQUIRED` (Markierung zur sofortigen Betreiberprüfung). **Keine** automatische Mandanten-/Benutzersperre, **kein** automatischer Lizenzentzug. Migration 8 dabei korrigiert (Snapshot-Fix `AUDIT_EVENT_TYPES_AT_MIGRATION_8`, analog zur bereits behobenen Migration-7-Historie), Migrationen 1–8 inhaltlich unverändert. Zwei neue Audit-Ereignistypen (`WORK_ORDER_BLOCKED_BY_POLICY`, `WORK_ORDER_AUTO_ESCALATED_BY_POLICY`), Metadaten-Allowlist um `severity` erweitert.
- **Avatar- und Persönlichkeitsrechte-Grundsatz (`AVATAR_CONSENT_POLICY.md`):** verbindlicher Grundsatz „Kein realistischer Avatar einer echten Person ohne direkte, dokumentierte und widerrufbare Zustimmung der dargestellten Person" – bloße Auftraggeberbehauptung reicht nicht. Hält ausdrücklich fest, dass das bestehende `avatarConsentConfirmed`-Feld (`heygen-job-package.js`, V7.1 Phase B, technisch unverändert) für Drittanbieterfälle **nicht ausreichend** ist. Dokumentiert Zweckbindung, erlaubte Marken/Kanäle/Sprachen, Laufzeit, Widerruf, Verbot der Weitergabe an andere Mandanten, Ergebnisfreigabe durch die dargestellte Person, künftige Artikel-50-Prüfung vor Veröffentlichung. Connys Testfall (erste echte Testperson, Bilder/Sprachaufnahmen/Videos bis Ende August 2026 vorzubereiten) ist als Referenzfall dokumentiert – **kein** echter Consent-Datensatz, **keine** neue Avatar-Funktion in diesem Schritt; eigener Folgebaustein „Avatar Consent & Identity Verification" ist noch nicht begonnen.
- **Tests:** neue Testdatei `business-use-policy.test.js` (27 Prüfpunkte: Gate-Funktionstests + Ende-zu-Ende über `server.js#requestHandler`: BLOCK/ESCALATE/CRITICAL, Verstoßprotokoll, Audit-Datenminimierung, Sessionwiderruf, kein automatischer Lizenzentzug, Owner-Ausnahmerolle bleibt korrekt) und `policy-documentation.test.js` (29 Prüfpunkte: Dokumentationskonsistenz aller sechs kanonischen Dokumente plus der drei neuen Policy-Dokumente). Bestehende `portal-operations-acceptance.test.js` punktuell an Migration 9 angepasst (erwartete Versionsliste `1–9`). Bestehende Tests **nicht** abgeschwächt. Realer, reproduzierbarer Vergleich: Self-Service-Ausgangsstand 1693 Prüfpunkte / 60 Testdateien, aktueller Stand **1749** Prüfpunkte / **62** Testdateien (**+56**). `npm run check`/`npm test` Exit 0, `npm audit`: 0 Schwachstellen, `better-sqlite3@13.0.1` unverändert, `git diff --check` sauber. Routenzahlen unverändert (70 GET / 52 POST / 8 GET-Präfixe / 4 POST-Präfixe / 27 statische Assets) – keine neue HTTP-Fläche.
- **Nicht enthalten:** Agentenausführung, Veröffentlichung, Billing, echter Mailversand, Mehrsprachigkeit, neue Avatar-Funktion, fertige AGB, abgeschlossene Rechtsprüfung, Owner-Oberfläche für `policy_violations`, automatische Eskalationsstufen bei wiederholten Verstößen, Commit/Push/Deployment.

## V7.2 Phase B Schritt 1 – Erste echte Kundenfachfunktion: Arbeitsauftrag anlegen, prüfen, Status verfolgen (lokal umgesetzt, ungesichert – Commit/Push stehen aus; PRODUKTKORRIGIERT)

**Erste kundenfachliche Funktion nach Abschluss von Phase A.** Ausdrücklich weiterhin keine automatische Ausführung, kein Agentenstart, keine Toolauswahl, keine Veröffentlichung, kein Billing, kein echter Mailversand, keine Mehrsprachigkeit, kein Commit, kein Push, kein Deployment. Ausgangsstand: HEAD/`origin/main` `be9848afc06c26dc056b33811dc2560e56696b5a`, Working Tree sauber, 68 GET/51 POST/8 Prefix/23 statische Assets/57 Testdateien/1597 automatisierte Prüfpunkte grün, `better-sqlite3@13.0.1`, `npm audit`: 0 Schwachstellen.

**PRODUKTKORREKTUR (nach erster Umsetzung, noch vor Commit):** Die erste Umsetzung sah den OWNER fälschlich als regulären fachlichen Prüfer jedes Kundenauftrags vor (Freigabe/Ablehnung/Rückfrage als Pflichtschritt). Das widerspricht dem Selbstbedienungs-Grundprinzip der KI-Unternehmenszentrale: Jamal (OWNER) kann und soll Marketing-, Design-, Strategie- oder sonstige Kundenaufträge nicht inhaltlich prüfen oder freigeben. Die Korrektur wurde **vor** jedem Commit/Push vollständig nachgezogen (Migration, Service, Routen, beide Oberflächen, Audit, Tests, Dokumentation) – siehe Statusmodell und Rollenbeschreibung unten.

- **Ziel:** ein Kunde kann erstmals im Portal eine fachliche Aufgabe an die KI-Unternehmenszentrale übergeben – anlegen, automatischen Status verfolgen, bei Rückfrage ergänzen, bei Bedarf selbst stornieren. Der Schritt endet bewusst vor jeder automatischen Ausführung durch Agenten.
- **Datenmodell** (`auth-db-migrations.js`, Migration 8, additiv, forward-only, transaktional, idempotent): neue Tabelle `work_orders` (`id`, `tenant_id`, `created_by_user_id`, `title`, `desired_result`, `context`, `deadline_text`, `status`, `status_note`, `created_at`, `updated_at`, `submitted_at`, `decided_at`, `decided_by_user_id`), serverseitig erzeugte ID, Tenant-/User-Foreign-Keys, `CHECK`-Constraint auf zehn Statuswerte (sechs in Schritt 1 erreichbar, vier bereits für spätere Schritte vorbereitet, siehe Statusmodell), begrenzte Textlängen, keine HTML-/Datei-/Anhang-/URL-/Kosten-/Provider-/Agentenfelder, kein Soft-/Hard-Delete. Migrationen 1–7 unverändert.
- **Statusmodell (produktkorrigiert, kein Owner als Pflichtstation):** in Schritt 1 erreichbar sind `DRAFT`, `SUBMITTED`, `NEEDS_CLARIFICATION`, `READY_FOR_PROCESSING`, `ESCALATED`, `CANCELLED`. Die Zentrale entscheidet **automatisch** (deterministische Vollständigkeitsregel in `work-order-service.js`, kein Owner beteiligt): `DRAFT/NEEDS_CLARIFICATION → SUBMITTED → READY_FOR_PROCESSING` oder `→ NEEDS_CLARIFICATION` (Kunde ergänzt und reicht erneut ein). Der Kunde kann seinen eigenen Auftrag jederzeit außer aus `ESCALATED` stornieren (`→ CANCELLED`, terminal). Der OWNER greift ausschließlich in Ausnahmefällen ein: `escalate` (`→ ESCALATED`, bei Sicherheit/Missbrauch/Rechtsfragen/außergewöhnlichen Kosten/technischer Blockade/expliziter Eskalation) und `stop` (`→ CANCELLED`, terminal, Sicherheits-/Betriebsstopp), beide mit Pflichtgrund. **Kein `APPROVED`/`REJECTED` durch den OWNER.** Vier weitere Statuswerte (`IN_PROGRESS`, `RESULT_READY`, `CHANGES_REQUESTED`, `CUSTOMER_APPROVED`) sind bereits in der `CHECK`-Constraint vorbereitet, werden aber von keiner Funktion dieses Schritts gesetzt oder vorausgesetzt (reine Datenmodell-Vorbereitung für die spätere Agentenübergabe und die spätere fachliche Kundenfreigabe eines Ergebnisses).
- **Kundenformular** (`/portal/auftrag-neu`, `portal-work-order-new.html` + `portal-work-order.js`): vier Felder, nur Titel und gewünschtes Ergebnis Pflicht; keine Tenant-/Rollen-/Provider-/Kostenwahl; deutsche, unternehmensfremde Sprache (kein „Payload“/„Tenant“/„Execution“/„Workflow“/„Agent“); nach Absenden klare Bestätigung, dass die Zentrale die Angaben automatisch prüft, kein Hinweis auf bereits laufende Arbeit.
- **Kundenportal** (`portal.html`/`portal-ui.js`, erweitert): primäre Aktion „Neuen Arbeitsauftrag anlegen“ und kompakte Liste „Meine Arbeitsaufträge“ auf `/portal`; Detailansicht inkl. Resubmit-Formular bei `NEEDS_CLARIFICATION` und Storno-Aktion als eingebetteter Bereich derselben Seite (keine serverseitige dynamische Route `/portal/auftrag/:id`, da `server-http-router.js` keine dynamischen Pfadsegmente für statische Assets unterstützt); pro Ansicht genau eine sichtbare Hauptaktion (Liste **oder** Detail, nie beides gleichzeitig); deutsche Statustexte (`DRAFT`→Entwurf, `SUBMITTED`→Eingereicht – wird geprüft, `NEEDS_CLARIFICATION`→Rückfrage offen, `READY_FOR_PROCESSING`→Bereit zur Bearbeitung, `ESCALATED`→In besonderer Prüfung, `CANCELLED`→Storniert); ehrlicher `READY_FOR_PROCESSING`-Hinweis „Der Auftrag wurde automatisch geprüft und ist bereit zur Bearbeitung. Die Bearbeitung durch Agenten ist in diesem Schritt noch nicht gestartet.“; keine rohen IDs/Statuscodes für Kunden.
- **Owner-Betriebsübersicht** (`/owner/auftraege`, `owner-work-orders.html` + `owner-work-orders.js`, kein Pflichtschritt mehr): mandantenübergreifende Liste mit Mandant, Titel, Ersteller, Status, Datum; Detailansicht mit Hintergrund/gewünschtem Zeitpunkt/bisheriger Statusnotiz; genau zwei Ausnahmeaktionen (Eskalieren, Stoppen) statt regulärer Freigabe/Ablehnung/Rückfrage, jede mit Pflichtgrund (intern, wird dem Kunden nicht wörtlich angezeigt), keine Toolauswahl, keine Agentenzuweisung, keine Veröffentlichung; Owner kann Status nur gemäß den erlaubten Übergängen setzen, keine stille Änderung von Kundeninhalten.
- **Service-/Routengrenze** (`work-order-service.js` Fachlogik/Statusübergänge/automatische Vollständigkeitsregel, `work-order-routes.js` HTTP-Glue): kein SQL in Portal-JavaScript, kein Tenant aus dem Request-Body (Known-fields-Allowlist weist `tenantId`/`customerId`/`userId`/`createdByUserId`/`status`/`statusNote` im Kunden-Anlegen-Body mit `400` ab), jede Änderung transaktional, `updated_at` konsistent.
- **API:** additiv **2 neue GET-Routen** (`/api/portal/work-orders`, `/api/owner/work-orders`), **1 neue exakte POST-Route** (`/api/portal/work-orders`), **2 neue GET-Präfixe** und **2 neue POST-Präfixe** (`/api/portal/work-orders/*` für Resubmit/Cancel, `/api/owner/work-orders/*` für Escalate/Stop); insgesamt jetzt **70 GET, 52 POST, 8 GET-Präfixe, 4 POST-Präfixe** (siehe `API_REGISTER.md`). Jede Route explizit klassifiziert (`CUSTOMER_TENANT`/`OWNER_ONLY`), CSRF/Origin-/Content-Type-Prüfung, Bodylimit, Known-fields-Allowlist, sichere generische Fehler, Audit, `Cache-Control: no-store`. Keine Route für Ausführung, Agentenstart, Publish, Billing, Datei-Upload, Provider, Mailversand; **keine** reguläre Owner-Freigabe-/Ablehnungs-/Rückfrageroute (`/approve`, `/reject`, `/request-clarification` liefern 404).
- **Audit** (`auth-audit.js`, additiv): acht Ereignistypen (`WORK_ORDER_CREATED`, `WORK_ORDER_SUBMITTED`, `WORK_ORDER_RESUBMITTED`, `WORK_ORDER_AUTO_READY`, `WORK_ORDER_AUTO_NEEDS_CLARIFICATION`, `WORK_ORDER_ESCALATED`, `WORK_ORDER_CANCELLED`, `WORK_ORDER_TENANT_MISMATCH_BLOCKED`); automatische Entscheidungen werden mit `actorUserId: null` auditiert (System, kein Mensch), Eskalation/Stopp mit dem Owner als Akteur; erweiterte Metadaten-Allowlist (`workOrderId`, `statusTransition`); niemals Auftragstext/Owner-Grund/Passwort/Token/Session/IP-Klartext/User-Agent-Klartext im Audit.
- **Statische Assets:** additiv **4 neue statische Arbeitsauftrags-Assets** (`/portal/auftrag-neu`, `/portal-work-order.js`, `/owner/auftraege`, `/owner-work-orders.js`); insgesamt jetzt **27 statische Assets**.
- **Tests:** drei neue Testdateien `work-order-routes.test.js` (52 Prüfpunkte, Selbstbedienungs-Fluss/automatische Statusübergänge/Escalate/Stop/Cancel/Audit/Fehlerformen), `work-order-security.test.js` (23 Prüfpunkte, Mandantenisolation/Rollen/Sperrzustände/Manipulationsversuche/keine alten Owner-Prüfrouten mehr), `work-order-ui.test.js` (21 Prüfpunkte, Sprache/Bedienbarkeit/Barrierefreiheit/Datenschutz der neuen Oberflächen, Owner-Ausnahmeformular statt Prüfformular); bestehende Testdateien punktuell an die neuen Routen-/Migrations-/Assetzahlen angepasst (`route-access-policy.test.js`, `server-http-router.test.js`, `agent-runtime.test.js`, `daily-work-run.test.js`, `portal-operations-acceptance.test.js`). Realer, reproduzierbarer Vergleich (kein Schätzwert): Baseline-Commit `be9848afc06c26dc056b33811dc2560e56696b5a` liefert 1597 Prüfpunkte, aktueller (produktkorrigierter) Arbeitsstand liefert **1693** Prüfpunkte – **+96 neue Prüfpunkte durch Schritt 1 (inkl. Produktkorrektur)**. Gesamt **1693 automatisierte Prüfpunkte grün** (`npm test`, Exit-Code 0, 60 Testdateien; `npm run check`, Exit-Code 0; `npm audit`: 0 Schwachstellen; `git diff --check` sauber; `better-sqlite3@13.0.1` weiterhin exakt gepinnt).
- **End-to-End-Verifikation des Selbstbedienungs-Flusses:** vollständig gegen den echten `server.js#requestHandler` mit isolierter, temporärer Auth-Datenbank in `work-order-routes.test.js` durchlaufen (kein Mock der Fachlogik) – zwei echte Mandanten (Café/Fitnessstudio), Auftrag mit knapper Beschreibung startet automatisch `NEEDS_CLARIFICATION` ohne jede Owner-Beteiligung, Kunde ergänzt und reicht erneut ein → automatisch `READY_FOR_PROCESSING`, ein ausreichend beschriebener Auftrag wird direkt `READY_FOR_PROCESSING`, Owner sieht beide Mandanten in der Betriebsübersicht, Owner eskaliert einen Auftrag (Pflichtgrund) → `ESCALATED`, Owner stoppt einen eskalierten Auftrag → `CANCELLED` (terminal), ein dringender Sicherheitsstopp funktioniert auch ohne vorherige Eskalation, Kunde storniert einen eigenen Auftrag selbst, ein eskalierter Auftrag kann vom Kunden nicht per einfacher Stornierung umgangen werden (409), Mandantentrennung bestätigt (fremder Auftrag → generisches 404), alte `/approve`/`/reject`/`/request-clarification`-Routen liefern 404. Frühere Phase-A-Testserver-Läufe (Dev/Prod, `__Host-`-Cookies, kein Dev-Bypass, CSRF/Origin) bleiben unverändert gültig und wurden für Schritt 1 nicht wiederholt, da die Routen-/Session-/CSRF-Schicht identisch bleibt.
- **Nicht enthalten:** automatische Ausführung, Agentenstart, Toolauswahl, Veröffentlichung, Billing, echter Mailversand, Mehrsprachigkeit, neue npm-Abhängigkeit, Commit/Push/Deployment, reguläre Owner-Freigabe/-Ablehnung. Nächste Phase erst nach separater Produktentscheidung: kontrollierte, automatische Übergabe eines `READY_FOR_PROCESSING`-Auftrags an einen Agentenlauf, gefolgt von einer echten fachlichen Kundenfreigabe des Ergebnisses (nicht des Owners).

## V7.2 Phase A Schritt 4 – Betriebs-, Sicherheits- und Produktabnahme der Portalbasis (geprüft, lokal korrigiert, ungesichert – Commit/Push stehen aus)

**Gesamtabnahme der Portalbasis aus Schritt 1–3, kein neuer Produktfunktions-Sprint.** Ausdrücklich weiterhin kein Commit, kein Push, kein Deployment, keine Veröffentlichung, keine Fachaufträge, kein Billing, kein echter Mailversand, keine Mehrsprachigkeit, keine Phase B. Ausgangsstand: HEAD/`origin/main` `5bd302d`, Working Tree sauber, 68 GET/51 POST/54 Testdateien/1535 automatisierte Prüfpunkte grün.

- **Vollständiger Abnahmebericht:** siehe `V7_2_PHASE_A_ACCEPTANCE.md` (Read-only-Architekturprüfung, Rollenmatrix, Mandantenisolation, Audit, DB/Migrationen, Dev-/Prod-Abnahme, Datenschutz, Bedienbarkeit, End-to-End-Ablauf, Restlücken, Phase-A-Urteil).
- **Ein Sicherheitsbefund gefunden und behoben:** die Owner-Benutzeraktionsrouten (`suspend`/`reactivate`/`revoke-sessions`/`reissue-invitation`/`revoke-invitation`/`prepare-password-reset`) prüften nicht, dass die Ziel-Benutzer-ID zu einer Kundenrolle gehört – eine OWNER-Session hätte darüber auch eine fremde OWNER- oder SUPPORT-ID adressieren können. Korrektur in `owner-admin-service.js` (`findCustomerUserOrThrow`): diese Routen liefern für OWNER-/SUPPORT-Ziel-IDs jetzt generisch `404`, mit Regressionstest.
- **Drei neue Akzeptanztestdateien:** `portal-security-acceptance.test.js` (27 Prüfpunkte, echte Zwei-Mandanten-Sicherheitsabnahme), `portal-operations-acceptance.test.js` (15 Prüfpunkte, echter Betriebslebenszyklus inkl. Migrations-Nachrüstung und Owner-Bootstrap als Kindprozess), `portal-usability-acceptance.test.js` (20 Prüfpunkte, statische Bedienbarkeits-/Datenschutzprüfung der Oberflächen).
- **Manuelle End-to-End-Abnahme real durchgeführt:** zwei eigenständige isolierte Testserver (Dev-Modus, Prod-Modus mit `KUZ_MODE=prod`/`KUZ_PUBLIC_ORIGIN`), zwei echte Mandanten (Café/Fitnessstudio), vollständiger 33-Schritte-Ablauf (Owner-Bootstrap, Einladung, Login, Mandantentrennung, Sperren/Reaktivieren/Sessionwiderruf, Passwort-Reset, Logout, Prod-Secure-Cookies, Execution-Deaktivierung) – alle 33 Schritte grün, danach beide Testserver gezielt beendet und alle eigenen Testdaten entfernt.
- **Tests:** additiv **62 neue Prüfpunkte**, **0 neue/entfernte Routen**. Gesamt **1597 automatisierte Prüfpunkte grün** (`npm test`, Exit-Code 0, 57 Testdateien; `npm run check`, Exit-Code 0; `npm audit`: 0 Schwachstellen; `git diff --check` sauber; `better-sqlite3@13.0.1` weiterhin exakt gepinnt).
- **Dokumentierte, bewusste Restlücken (keine akute Sicherheitslücke):** Ratenlimiter In-Memory (kein Neustart-übergreifender Schutz), kein dokumentierter Backupweg/keine Verschlüsselung für die Server-Auth-Datenbank, offenes Löschkonzept/Datenschutztext, kosmetische rohe Statuswerte im internen Owner-Werkzeug.
- **Phase-A-Urteil:** V7.2 Phase A ist mit klar benannten Restgrenzen abgenommen und commitbereit (siehe `V7_2_PHASE_A_ACCEPTANCE.md` Abschnitt „Phase-A-Urteil“).
- **Nicht enthalten:** jede Art von Fachauftrag, Veröffentlichung, Billing, echter Mailversand, Mehrsprachigkeit, Phase B, Commit/Push/Deployment.

## V7.2 Phase A Schritt 3 – Deutsches Kundenportal und Owner-Verwaltung (lokal umgesetzt, ungesichert – Commit/Push stehen aus)

**Ausdrücklich noch kein Commit, kein Push, kein Deployment, keine Veröffentlichung, keine Fachaufträge/Business-Routen, keine Canva-/HeyGen-Kundenfreigabe, kein Billing, kein echter Mailversand, keine Mehrsprachigkeit, keine Phase B.** Baut additiv auf V7.2 Phase A Schritt 2 auf (HEAD zu Beginn dieses Schritts: Commit `41b6dbc602f9fd4f5e91099492cc08be72b0014c`, Working Tree sauber, 65 GET/51 POST/51 Testdateien/1450 automatisierte Prüfpunkte grün – verifiziert durch einen realen, isolierten Testlauf des Baseline-Commits, siehe Testabschnitt unten). V7.0 bleibt `FROZEN`. V7.1 Phase A bis C.1.1 sowie V7.2 Phase A Schritt 1/2 bleiben unverändert grün.

- **Ziel:** Jamal kann als Owner Mandanten und Benutzer verwalten (aktivieren/suspendieren, einladen, sperren/reaktivieren, Sitzungen widerrufen); eingeladene Kunden können ihr Konto über einen Einladungslink aktivieren, sich anmelden, ausschließlich ihre eigene Portalstartseite mit Konto-/Sitzungsinformationen sehen und sich sicher abmelden. Das Portal bietet ausdrücklich noch keine Fachaufträge, Werkzeuge oder Veröffentlichungsfunktionen an.
- **Neue Zugriffsklasse `STATIC_AUTHENTICATED_PORTAL`** (`route-access-policy.js`): erlaubt `OWNER`/`CUSTOMER_ADMIN`/`CUSTOMER_USER` für die statischen Kundenportal-Assets, blockiert `SUPPORT` ohne aktiven Grant, kein Dev-Bypass, `HIDDEN_404`. Jede neue Route und jedes neue statische Asset wurde explizit einer Klasse zugeordnet (`STATIC_PUBLIC` für Login/Einladung/Reset, `STATIC_AUTHENTICATED_PORTAL` für die Kundenportal-Startseite, `STATIC_OWNER_ONLY` für die Owner-Verwaltungsseite, `OWNER_ONLY` bzw. `CUSTOMER_TENANT` für die zugehörigen API-Routen).
- **Owner-Verwaltung** (`owner-admin-service.js` reine Geschäftslogik + `owner-admin-routes.js` HTTP-Schicht, neue Seite `/owner/kunden`): Mandanten aktivieren/suspendieren, Mandanten-/Benutzerliste, Kunden einladen (nur Rolle `CUSTOMER_ADMIN`/`CUSTOMER_USER`, niemals `OWNER`/`SUPPORT`; doppelte E-Mail wird mit 409 abgelehnt), Benutzer sperren/reaktivieren, alle Sitzungen eines Benutzers widerrufen, Einladung erneuern/widerrufen, Passwort-Reset vorbereiten. Einladungs- und Reset-Tokens erscheinen ausschließlich einmalig in der Owner-Antwort – kein Mailversand, kein Logging, keine Audit-Metadaten mit Token/Passwort/Session-ID (der bestehende Sensitivinhalt-Filter in `auth-audit.js` weist solche Metadaten unabhängig vom Feldnamen fail-closed ab).
- **Kundenportal** (`customer-portal-service.js` + `customer-portal-routes.js`, neue Seite `/portal`): `GET /api/portal/me` liefert ausschließlich sechs dokumentierte Felder (Name, E-Mail, Rollenbezeichnung, Mandantenname, Kontostatus-Bezeichnung, letzter Login) ohne interne IDs; `GET /api/portal/status` liefert einen statischen, ehrlichen Bereitschaftsstatus (`workOrdersEnabled`/`publicationEnabled`/`billingEnabled` bewusst `false`). Ein gesperrter oder abgemeldeter Benutzer verliert den Zugriff unmittelbar beim nächsten Request (Live-Statusprüfung im bestehenden Route-Guard, keine zwischengespeicherte Berechtigung).
- **Einladung/Passwort-Reset** (`/portal/einladung`, `/portal/passwort-vergessen`, `/portal/passwort-neu`, gemeinsame Auth-Shell `portal-login.html`/`portal-auth.js`): nutzen ausschließlich die bereits in Schritt 2 gesicherten Auth-Routen (`POST /api/auth/invitation/accept`, `/api/auth/password-reset/request`, `/api/auth/password-reset/confirm`); keine neue Auth-Logik, keine offene Selbstregistrierung.
- **Portaldesign** (`portal.css`, geteiltes Stylesheet für alle Portal-/Owner-Seiten): ruhig, hochwertig, mobilfreundlich, ohne externes CDN, ohne externe Schriftart, ohne Tracking; automatisiert gegen genau diese Verbotsmuster geprüft (`portal-assets.test.js`).
- **Migrationen** (`auth-db-migrations.js`, additiv, forward-only): neue Migration 7 (`widen_audit_event_types`) erweitert die `CHECK`-Beschränkung der bestehenden `auth_audit_events`-Tabelle per atomarem Tabellen-Rebuild um neun neue Ereignistypen; Migrationen 1–6 bleiben unverändert. `auth-db.js` erhielt vier additive Funktionen (`updateTenantStatus`, `listUsersByTenantId`, `findLatestPendingTokenForUser`, `revokePendingTokensForUser`) – bleibt das einzige Modul mit direktem `better-sqlite3`-Import.
- **Owner-Bootstrap** (`npm run auth:bootstrap-owner`, `scripts/auth-bootstrap-owner.js`): lokales CLI-Kommando für genau ein Owner-Konto, maskierte Passworteingabe, kein HTTP-Endpunkt, kein statischer Zugangscode, idempotent, Audit-Eintrag pro Lauf. In der manuellen Abnahme (siehe unten) erfolgreich für Dev und Prod verwendet.
- **API:** additiv **3 neue GET-Routen** (`/api/owner/tenants` inkl. Präfixrouten für Einzelabruf/Benutzerliste, `/api/portal/me`, `/api/portal/status`) und **0 neue exakte POST-Routen** (alle Owner-Aktionen laufen über zwei neue POST-Präfixrouten `/api/owner/tenants/*` und `/api/owner/users/*`, `server-http-router.js` wurde dafür additiv um `postRoutePrefixHandlers` erweitert – identisches Verhalten für alle bestehenden Aufrufer ohne diese Option); insgesamt jetzt **68 GET und weiterhin 51 additive POST-Routen** (siehe `API_REGISTER.md`).
- **Tests:** drei neue Testdateien `owner-admin-routes.test.js` (34 Prüfpunkte), `customer-portal-routes.test.js` (18 Prüfpunkte), `portal-assets.test.js` (23 Prüfpunkte); bestehende Testdateien punktuell an die neuen Routen angepasst (`route-access-policy.test.js`, `server-http-router.test.js`, `agent-runtime.test.js`, `daily-work-run.test.js` – Routenzahlen 68 GET/51 POST). Realer, reproduzierbarer Baseline-Vergleich (kein Schätzwert): der unveränderte Baseline-Commit `41b6dbc602f9fd4f5e91099492cc08be72b0014c` liefert bei identischem Testlauf exakt **1450** `ok`-Prüfpunkte; der aktuelle Arbeitsstand liefert exakt **1535** `ok`-Prüfpunkte – **+85 neue Prüfpunkte durch Schritt 3**. Gesamt **1535 automatisierte Prüfpunkte grün** (`npm test`, Exit-Code 0, 54 Testdateien; `npm run check`, Exit-Code 0; `npm audit`: 0 Schwachstellen; `better-sqlite3@13.0.1` weiterhin exakt gepinnt).
- **Werkzeug-Sandbox-Nebenbefund (kein Produktfehler):** In der für diesen Schritt verwendeten Ausführungsumgebung schlug ein einzelner Integrationstest (`server-http-router.test.js`, Execution-Bridge-Fixture-Rundlauf) zunächst mit `400` statt `200` fehl, weil das Werkzeug den Dateisystemzugriff außerhalb des Projektordners einschränkt (derselbe bereits in V7.1 Phase B dokumentierte Effekt). Mit regulären, unbeschränkten Rechten läuft exakt derselbe Test grün (bestätigt: alle 73 Prüfpunkte in `server-http-router.test.js` bestehen). Keine Repository-Änderung wurde vorgenommen, um diese Werkzeug-Einschränkung zu umgehen.
- **Manuelle lokale HTTP-Abnahme:** zwei isolierte Testserver (eigenes `HOME`/`KUZ_DATA_DIR` unter `/tmp`, eigener Port, keine echten Zugangsdaten) für Dev- und Prod-Modus gestartet und wieder kontrolliert beendet. Vollständiger Ablauf in beiden Modi bestanden: Owner-Bootstrap → Owner-Login → `/owner/kunden` → Mandant aktivieren → Kunde einladen → Einladung annehmen (`/portal/einladung`) → Kunden-Login → `/portal` → `/api/portal/me`/`/api/portal/status` → Kunde erreicht `/owner/kunden` nicht (404) → Logout → Session ungültig. Zusätzlich in Prod verifiziert: kein Dev-Bypass auf Loopback (Owner-Routen ohne Session bleiben 404), `__Host-`-Cookienamen mit `Secure`-Flag, Execution-Routen bleiben deaktiviert, Owner-seitiger Sitzungswiderruf invalidiert die Kundensitzung sofort.
- **Nicht enthalten:** Fachaufträge/Business-Routen, Veröffentlichung, Billing, echter Mailversand, Canva-/HeyGen-Kundenbedienung, Mehrsprachigkeit, Support-Grant-Vergabe, Phase B, Commit/Push/Deployment.

## V7.2 Phase A Schritt 2 – Route-Gates, Auth-Routen und Owner-/Customer-Trennung (lokal umgesetzt, ungesichert – Commit/Push stehen aus)

**Ausdrücklich noch kein Commit, kein Push, kein Deployment, keine Veröffentlichung, kein Kundenportal-UI, keine Auftragserfassung, keine Canva-/HeyGen-Kundenfreigabe, kein Billing, keine Mehrsprachigkeit, keine Phase B.** Baut auf V7.2 Phase A Schritt 1 (Auth-Kern/Identitätsschicht, Commit `e2cd018`, HEAD und `origin/main` identisch) auf. V7.0 bleibt `FROZEN`. V7.1 Phase A bis C.1.1 bleiben unverändert grün.

- **Ziel:** eine vollständige, serverseitige Authentifizierungs- und Autorisierungsgrenze vor alle vorhandenen und neuen Routen; Backend ist die verbindliche Sicherheitsgrenze, UI-Verstecken war nie Authentifizierung.
- **Zentrale Zugriffspolitik:** neues Modul `route-access-policy.js` als einzige kanonische Wahrheit für jede Route/jeden Prefix/jedes statische Asset; unklassifizierte Route ist fail-closed (404 in Prod, Testfehler); strukturell gegen die tatsächlich registrierten Routen aus `server.js` geprüft (`route-access-policy.test.js`).
- **Klassifizierung:** alle bestehenden Chef-Routen (Projekte, Airtable, Cockpit, Agenten, Serverstatus, V7-Freeze-Status, V7.1-Dokumente/Tools/Plugin-Gateway/Tool-Routing, Backup-/Restore-, HeyGen-, Canva-, Agency-Routen, alle Chef-Assets) sind `OWNER_ONLY`; `/api/execution/*` zusätzlich `DISABLED_IN_PROD` (im Dev-Modus weiterhin `OWNER_ONLY`). Keine bestehende Canva-/HeyGen-/Pilot-/Feedback-/Kundenfreigabe-/Ergebnisroute wird für Kunden geöffnet.
- **Betriebsmodi:** `KUZ_MODE=dev` (Standard) hält den bestehenden Chef-Modus lokal auf Loopback ohne Owner-Login nutzbar (sichtbare Konsolenwarnung), Kunden-/Tenantgrenzen gelten unverändert auch im Dev-Modus. `KUZ_MODE=prod` verlangt `KUZ_PUBLIC_ORIGIN`, erzwingt Owner-Session überall, deaktiviert Execution vollständig, setzt `Secure`-Cookies, kennt keinen Dev-Bypass und bricht den Start bei fehlender Pflichtvariable ab (fail-closed, manuell verifiziert).
- **Auth-Routen** (`auth-http-routes.js`): `POST /api/auth/login` (generische Fehlermeldung für unbekannte E-Mail/falsches Passwort/INVITED/LOCKED/DISABLED/SUSPENDED-Tenant, Dummy-scrypt, Rate-Limit, Lockout, Sessionrotation), `POST /api/auth/logout` (immer `{ok:true}`, idempotent), `GET /api/auth/session` (minimierte Statusantwort ohne IDs), `POST /api/auth/password-reset/request` (immer identische Antwort, kein Kontonachweis, kein Mailversand in Phase A), `POST /api/auth/password-reset/confirm` (atomare Einlösung, widerruft alle Sessions), `POST /api/auth/invitation/accept` (nur Zweck INVITE, `INVITED`→`ACTIVE`).
- **Auth-Route-Guard** (`auth-route-guard.js`): läuft vor jedem Handler; Tenant/Rolle stammen ausschließlich aus der Session, niemals aus `customerId`/`tenantId`/`brandId`/`campaignId`/`projectId` in Query/Body/Pfad; abweichender Tenantparameter → 404 + Audit `TENANT_MISMATCH_BLOCKED`. `SUPPORT`-Rolle wird korrekt geladen, erreicht aber ohne aktiven Grant (noch nicht implementiert) strukturell keine Kunden-/Ownerdaten (`SUPPORT_GRANT_ONLY` → 404).
- **Auth-HTTP/Cookies** (`auth-http.js`): `__Host-kuz_session`/`__Host-kuz_csrf` in Prod (HttpOnly/Secure/SameSite=Lax), lokale Dev-Cookienamen (`kuz_dev_session`/`kuz_dev_csrf`) ohne HTTPS; CSRF per Double-Submit-Cookie und `x-kuz-csrf`-Header, timing-safe Vergleich; zentrale Sicherheitsheader; `Cache-Control: no-store` für Auth-/Sessionantworten; keine Sessiondaten in localStorage/sessionStorage/URL/HTML/Logs.
- **Rate Limiting** (`auth-rate-limit.js`): In-Memory ohne weitere Dependency; Login 10/15min je E-Mail, 20/5min je IP-Hash; Reset-Anfrage 3/h je Konto, 10/h je IP-Hash; Reset-Bestätigung 20/h je IP-Hash.
- **Router-Integration:** `server-http-router.js` wertet die Policy vor jedem Handler aus (GET/POST/Prefix/statisch); kein Handler lädt oder schreibt vor erfolgreichem Gate; Pfad-Traversal-Schutz unverändert.
- **Tests:** zwei neue Testdateien `route-access-policy.test.js` (52 Prüfpunkte) und `server-auth-routes.test.js` (44 Prüfpunkte); bestehende Testdateien punktuell an die bewusst neue Sicherheitsgrenze angepasst (Routenzahlen 65 GET/51 POST, ein Statuscode `403→404` bei `/api/execution/prepare` mit fremdem Host, da das Auth-Gate nun früher greift). Gesamt **1450 automatisierte Prüfpunkte grün** (`npm test`, Exit-Code 0, 51 Testdateien; `npm run check`, Exit-Code 0; `npm audit`: 0 Schwachstellen; `git diff --check` sauber; `better-sqlite3@13.0.1` weiterhin exakt gepinnt).
- **Manuelle lokale HTTP-Abnahme:** zwei isolierte Testserver (eigenes `HOME`/`KUZ_DATA_DIR` unter `/tmp`, eigener Port, keine echten Zugangsdaten) für Dev- und Prod-Modus gestartet und wieder kontrolliert beendet; alle in Abschnitt Q geforderten Prüfungen (Startseite, Sessionstatus, Login/Logout, Cookie-Flags, Chef-Route ohne Owner blockiert, Kunde auf Owner-Route blockiert, Execution in Prod blockiert, unklassifizierte Route blockiert, Startabbruch ohne `KUZ_PUBLIC_ORIGIN`) bestanden.
- **Nicht enthalten:** Kundenportal-UI, kundenbedienbare Fachroute, Owner-Route zur Benutzeranlage (Schritt 3), Support-Grant-Vergabe, Veröffentlichung, Mehrsprachigkeit, Phase B, Commit/Push/Deployment.

## V7.2 Phase A Schritt 1 – Auth-Kern und persistente Identitätsschicht (umgesetzt, getestet und gesichert mit Commit `e2cd018`)

**Ausdrücklich weiterhin kein Kundenportal, keine Route-Gates, keine Login-Route.** Legt die Datengrundlage für Schritt 2: `auth-db.js` (hinter dem Modul gekapselte `auth.sqlite`, 6 nummerierte Migrationen, Tabellen `tenants`/`users`/`sessions`/`password_reset_tokens`/`auth_audit_events`), `auth-password.js` (`crypto.scrypt`-Hashing), `auth-session.js` (256-Bit-Sessiontokens nur gehasht gespeichert, 12h absolute/60min Idle-Lebensdauer, max. 5 aktive Sessions je Nutzer), `auth-audit.js`, `auth-tenant-link.js` (Projektion der kanonischen `agency-tenant-registry.js` in die Auth-Datenbank; Datenbankprojektionen starten `SUSPENDED`; unbekannte DB-Mandanten führen fail-closed zum Startabbruch). `better-sqlite3@13.0.1` exakt gepinnt, `npm audit`: 0 Schwachstellen.

## V7.1 Phase C.1.1 – Reviewmodell skalierbar und rollenbasiert korrigiert (umgesetzt, getestet und gemeinsam mit Phase C.1 gesichert mit Commit `6621d93`)

**Committed und gepusht mit Commit `6621d93` (gemeinsam mit Phase C.1, HEAD und `origin/main` standen zum Zeitpunkt dieses Commits exakt darauf). Ausdrücklich weiterhin kein Deployment, keine Veröffentlichung, keine neue Canva-Aktion, kein Shopify, keine Phase D.** Produktkorrektur auf dem Phase-C.1-Stand: Jamal darf im späteren Kundenbetrieb **nicht** zum verpflichtenden Prüf-/Freigabeengpass werden. Kein neuer Funktionsbereich – ausschließlich Review-/Freigabemodell.

- **Reviewmodell (`reviewMode`):** `OWNER_REVIEW` (Eigenprojekt), `CUSTOMER_SELF_REVIEW` (Standardkunde), `PREMIUM_INTERNAL_REVIEW` (Premiumkunde), `RISK_ESCALATION` (Risikofall)
- **Service-Tiers (`serviceTier`):** `INTERNAL` / `STANDARD` / `PREMIUM` / `ESCALATED` mit Flags `ownerReviewRequired`, `customerSelfReviewAllowed`, `humanReviewRequired`, `riskEscalationRequired`, `reviewerRole`, `reviewedByActorId` (nur interne minimierte Referenz), `qualityReviewStatus`
- **Agenten-QS:** eigene Prüfstufe mit Ergebnis `PASS` / `PASS_WITH_NOTES` / `FAIL` / `ESCALATE` und neun strukturierten Checklist-Punkten; `FAIL` und `ESCALATE` blockieren Kundenreview
- **Abläufe:** Standardkunde nach bestandener Agenten-QS direkt `READY_FOR_CUSTOMER_REVIEW` (keine Jamal-Prüfung); Eigenprojekt braucht Owner-Review; Premium braucht optionales menschliches Review; Risikofall → `ESCALATED`
- **Café-Amore-Pilot unverfälscht:** `serviceTier: INTERNAL`, `reviewMode: OWNER_REVIEW`, `ownerReviewRequired: true`; Jamals bestehendes Review (`REVIEWED_WITH_NOTES`, Evidence, Entscheidungsverlauf) bleibt vollständig erhalten
- **API:** +3 POST (`/api/v71/canva/pilot-result/agent-qa`, `/human-review`, `/escalate`); Reviewmodus wird über bestehende GET-Pilotakte gelesen; insgesamt jetzt **64 GET und 46 POST**; weiterhin keine Route für Veröffentlichung, Canva-Zugang, Login, Credentials, Abrechnung, Credits oder Mandantenwechsel
- **UI:** zeigt klar Reviewmodell, Tarif, Agenten-QS-Status, menschliche/Jamal-Prüfung erforderlich ja/nein, Kunden-Selbstprüfung erlaubt ja/nein, Eskalation ja/nein, Veröffentlichung nicht freigegeben; **keine** Formulierung „Jamal muss jeden Kundenentwurf prüfen“
- **Mehrsprachigkeits-Leitplanke (nur dokumentarisch, nicht implementiert):** deutsche Version zuerst vollständig; spätere Märkte Portugal/England/Spanien/Frankreich möglich; Sprachversionen werden lokalisiert (nicht nur übersetzt); jede Sprach-/Marktversion hat eigenen Status, eigene QS, eigene Kundenfreigabe, eigene Veröffentlichungssperre; deutsche Freigabe gilt später nicht automatisch für andere Sprachen; keine Sprachrouten, keine Übersetzungsfunktion, keine Länderlogik in dieser Phase
- **Verbindlich unverändert:** Kundenfreigabe ≠ Veröffentlichung; Veröffentlichung bleibt `NOT_APPROVED`; kein automatischer Canva-/Kosten-/Rechte-Schritt; Tarifwechsel ändert keine Historie; Mandantenwechsel blockiert; Reviewmodus nach erster Freigabe nicht stillschweigend änderbar (nur ausdrückliche Eskalation)
- **Tests:** bestehende Phase-C.1-Testdateien um C.1.1-Reviewmodell-Prüfpunkte erweitert; zum Zeitpunkt des Commits `6621d93` gesamt **1308 automatisierte Prüfpunkte grün** (`npm test`, Exit-Code 0, 46 Testdateien; `npm run check`, Exit-Code 0; `git diff --check` sauber)
- **Nicht enthalten:** Phase D, Mehrsprachigkeitsfunktion, Veröffentlichung, neue Canva-Aktion, Deployment

## V7.1 Phase C.1 – realen Canva-Pilot kanonisch abgeschlossen und Kundenfeedback-Schleife vorbereitet (umgesetzt, getestet und gemeinsam mit Phase C.1.1 gesichert mit Commit `6621d93`)

**Committed und gepusht mit Commit `6621d93` (gemeinsam mit Phase C.1.1). Ausdrücklich weiterhin kein Deployment, keine Veröffentlichung, kein Social Posting, kein Kundenzugang zu Canva, keine neue externe Canva-Aktion, keine Kosten, keine Credentials gespeichert, kein Shopify, keine Phase D.** Baut additiv auf dem bereits committeten und gepushten V7.1 Phase C (Commit `52b2d02`) auf. V7.0 bleibt `FROZEN`. V7.1 Phase A/B/B.1/C bleiben unverändert grün. Canva-Basisstatus bleibt in der kanonischen Basiswahrheit weiterhin `NOT_CONNECTED`/`RECOMMENDATION_ONLY`; der additive Canva-Pilotstatus bleibt `PARTIALLY_CONNECTED`/`CONTROLLED_HANDOFF`, niemals `DIRECT`. Phase C.1.1 korrigiert das Reviewmodell skalierbar (siehe Abschnitt oben); die kanonischen Pilotfakten bleiben unverändert.

Ein realer, neutraler Canva-Pilot wurde **außerhalb** des lokalen Servers über den verbundenen Canva-Connector durchgeführt (fiktiver Testkunde, fiktive Marke „Café Amore“, Instagram-Post „Sonntagsfrühstück“). Ablauf: zwei Generierungsläufe à vier Kandidaten, Jamal wählte Kandidat 2, der Kandidat wurde in ein echtes bearbeitbares Canva-Design umgewandelt (Design-ID `DAHQeIjc2ls`, Titel „Instagram-Beitrag - Sonntagsfrühstück“, 1 Seite), intern geprüft, kontrollierte Textänderungen vorgenommen und gespeichert (Zeitangabe größer/fett, neue untere Botschaft, Platzhalter-Webadresse entfernt), Bild unverändert belassen. **Keine Veröffentlichung, keine Freigabe für Social Posting, kein Kundenzugang zum Canva-Konto, kein Brand Kit, keine realen Kundendaten, keine automatischen Kosten** (`costPackageStatus: NOT_BILLABLE_TEST`).

- **Neue Module:** `canva-pilot-result-record.js` (kanonisches, testbares Datenmodell der realen Pilot-Ergebnisakte inkl. `pilotId`/`toolId`/`connectorType`/Mandantenbindung/`jobPackageId`/`providerJobId`/`candidateId`/`designId`/`designTitle`/`designType`/`pageCount`/`costPackageStatus`/`providerExecutionStatus`/`internalReviewStatus`/`customerReviewStatus`/`publicationApprovalStatus`/`evidence`/`feedbackHistory`/`changeRequestHistory`/`decisionHistory`/`immutableTenantFingerprint`; keine edit-/view-URL als öffentliche Wahrheit), `canva-pilot-store.js` (isolierte Metadaten-Persistenz unter App Support, Mandanten-Reassignment blockiert, `listPilotResultRecords` filtert optional nach `customerId`)
- **Kanonische Pilotdaten exakt wie berichtet abgebildet:** `designId: DAHQeIjc2ls`, `designTitle: Instagram-Beitrag - Sonntagsfrühstück`, `designType: INSTAGRAM_POST`, `pageCount: 1`, `costPackageStatus: NOT_BILLABLE_TEST`, `providerExecutionStatus: SAVED`, `internalReviewStatus: REVIEWED_WITH_NOTES`, `customerReviewStatus: CHANGES_POSSIBLE`, `publicationApprovalStatus: NOT_APPROVED`; Jamals reales internes Feedback (Bildwirkung warm/positiv, Hochformat positiv, Tiefenschärfe positiv, Hauptüberschrift gut lesbar, Frühstücksdarstellung zu einfach, Kaffee nicht deutlich genug, Zeitangabe ursprünglich zu klein, untere Botschaft auch nach Änderung eher klein, Gesamturteil „solide, aber nicht 100 Prozent final“, Kundensicht „Kunde könnte andere Präferenzen haben“) ist als `evidence` kanonisch hinterlegt
- **Verbindliche Trennung durchgesetzt:** Kandidat ist keine Design-ID; Design-ID ist kein Kundenentwurf; Kundenentwurf ist keine Kundenfreigabe; Kundenfreigabe ist keine Veröffentlichung; Änderungsanforderung startet keine Veröffentlichung; Kundenfeedback/-freigabe ändert weder Kostenfreigabe noch Rechtefreigabe; Veröffentlichung bleibt strukturell `NOT_APPROVED` (keine Funktion in `canva-pilot-result-record.js` kann dieses Feld verändern; das Modul referenziert `canva-connector.js` nicht)
- **Kontrollierte Kundenfeedback-Schleife:** `READY_FOR_CUSTOMER_REVIEW` → `CUSTOMER_CHANGES_REQUESTED` → `READY_FOR_REVIEW_AFTER_CHANGES` → `CUSTOMER_APPROVED`; nur erreichbar mit echter Design-ID, ausgewähltem Kandidaten, vollständiger Mandantenbindung, abgeschlossenem internem Review und weiterhin nicht freigegebener Veröffentlichung; Kundenfeedback-Datensatz (`feedbackId`/`designId`/Mandantenbindung/`feedbackText`/`feedbackType`/`requestedChanges`/`createdAt`/`createdByRole`/`status`/`appliedAt`/`reviewedAt`/`immutableFingerprint`) mit sechs zulässigen Feedbacktypen (`TEXT_CHANGE`/`IMAGE_CHANGE`/`LAYOUT_CHANGE`/`BRAND_ADJUSTMENT`/`MESSAGE_ADJUSTMENT`/`GENERAL_FEEDBACK`); Feedback wird zwingend an den Mandanten der Akte gebunden, nie an Aufrufer-Angaben; ohne echte Design-ID strukturell blockiert
- **API (Phase C.1, danach durch C.1.1 auf 46 POST erweitert):** additiv 1 neue GET-Route (`/api/v71/canva/pilot-results` inkl. Einzelabruf/Feedback-Historie als Präfixrouten) und 5 neue POST-Routen (`pilot-result/internal-review`, `/customer-feedback`, `/request-changes`, `/mark-ready-after-changes`, `/customer-approve`); Phase C.1.1 ergänzt +3 POST (`/agent-qa`, `/human-review`, `/escalate`) → aktuell **64 GET und 46 POST**; weiterhin keine Route für Veröffentlichung, Social Posting, Canva-Login, API-Key, Einladung, Freigabelink, Löschen, Abrechnung, Credits oder Brand-Kit-Zugang; alle bestehenden Sicherheitsregeln (Host/Origin, Content-Type, Bodylimit, keine Stacktraces, keine Secrets, keine absoluten Pfade, Mandantenmismatch 404) unverändert wiederverwendet
- **UI:** additiver Chef-Modus-Bereich „Canva-Pilot-Ergebnisakte“ zeigt das reale Ergebnis (Design gespeichert, Design-ID intern, 1 Seite, intern geprüft, nicht veröffentlicht), Reviewmodell/Tarif/Agenten-QS (Phase C.1.1), Kundenfeedback-Historie und klare Statusanzeige; Aktionsbuttons für Agenten-QS, Owner-Review (nur Eigenprojekt), optionales menschliches Premium-Review, Kundenfeedback, Änderungswunsch, Änderung erledigt/erneut geprüft, Kundenfreigabe (Entwurf, keine Veröffentlichung) und Risikofall-Eskalation; kein Button „Veröffentlichen“, „Teilen“, „Einladen“, „Canva öffnen als Kunde“, „Alles freigeben“, „Credits kaufen“ oder „Löschen“; Jamal prüft Eigenprojekte/Risikofälle – nicht grundsätzlich jeden Kundenentwurf
- **Backup/Restore additiv erweitert** (`canva-backup.js`): sichere Pilot-Metadaten (Pilot-/Design-ID, Titel, Status, Reviewmodell-/Tariffelder, Agenten-QS-Historie, interne Bewertungsnotizen, Kundenfeedback, Änderungs-/Entscheidungsverlauf, Mandanten-Fingerprint) werden exportiert; keine Bilder, Canva-Dateien, Vorschaubilder, Tokens, Credentials, Provider-Komplettantworten, private Canva-URLs, Brand-Kit-Assets oder App-Support-Dateien; Restore startet keine Canva-Aktion, erzeugt kein Design, speichert nichts extern, veröffentlicht nichts, setzt keine Freigabe zurück, lädt keine Medien/Providerdaten nach; eine Mandanten-Neuzuordnung wird beim Restore abgewiesen (bestehende Akte bleibt beim Ursprungskunden)
- **Tests:** Phase-C.1-Testdateien um C.1.1-Reviewmodell-Prüfpunkte erweitert (`canva-pilot-result-record.test.js`, `canva-pilot-store.test.js`, `server-v71-canva-pilot-routes.test.js`, `canva-ui.test.js`, `canva-phase-c-integration.test.js`, Routenzahl in `server-http-router.test.js`); aktuelle Gesamtzahl siehe Abschnitt C.1.1 bzw. Abschlussbericht nach `npm test`
- **Nicht enthalten – weiterhin offen und erfordert separate Jamal-Freigabe:** jede Veröffentlichung, jedes Social Posting, jeder echte Kundenzugang zu Canva, jede weitere externe Canva-Aktion, Brand-Kit-Anbindung, produktive Kundenkommunikation außerhalb dieser internen Erfassung, Mehrsprachigkeitsfunktion, Phase D

## V7.1 Phase C – Canva als zweiter kontrollierter Design-Connector (umgesetzt, getestet und gesichert mit Commit `52b2d02`)

**Committed und gepusht mit Commit `52b2d02` (HEAD und `origin/main` stehen exakt darauf). Ausdrücklich weiterhin kein Deployment, kein echtes Canva-Design außerhalb des dokumentierten Pilots, keine weitere Canva-Übertragung, keine Veröffentlichung, keine Kosten, keine Credentials gespeichert, kein weiterer HeyGen-Render, keine Shopify-Änderung.** V7.0 bleibt `FROZEN`. V7.1 Phase A/B/B.1 bleiben unverändert grün. Architektur-Entscheidung: Canva ist – wie HeyGen – ein `CONTROLLED_CONNECTOR_HANDOFF` – die Zentrale erstellt und validiert lokal ein Designauftragspaket; die tatsächliche Canva-Aktion würde, falls überhaupt, über einen externen, authentifizierten Canva-Connector außerhalb dieses Servers laufen. Kein API-Key wird gespeichert. Canva bleibt in der kanonischen Basiswahrheit (`tool-registry.js`, `plugin-gateway.js`) weiterhin `NOT_CONNECTED` / `RECOMMENDATION_ONLY`; ein additiver, separater Pilotstatus (`canva-connector.js#buildCanvaPilotStatus`) zeigt zusätzlich `PARTIALLY_CONNECTED` / `CONTROLLED_HANDOFF`, niemals `DIRECT` oder autonom.

- **Neue Module:** `canva-design-job-package.js` (kanonisches Designauftragsmodell `canvaDesignJobPackage`, Capability-Profil, Datenschutz-/Marken-/Rechteprüfung, sieben getrennte Freigabestufen), `canva-connector.js` (kontrollierter Adapter, minimales Hand-off-Payload, Kandidaten- und Editing-Transaktionslogik, Tokenbindung über die bestehende `execution-bridge.js`-Tokenarchitektur, Pilotstatus), `canva-design-result.js` (strukturierte Ergebnisrückführung `canvaDesignJobResult`, URL-Validierung, Candidate-ID-ist-keine-Design-ID-Prüfung), `canva-store.js` (lokale Metadaten-Persistenz unter App Support, Mandanten-Reassignment blockiert), `canva-backup.js` (Metadaten-Export/Restore-Vorschau ohne Originaldateien/Credentials/Brand-Kit-Assets)
- **Sieben getrennte Freigabestufen** (Briefing, Assets/Rechte, externe Übertragung, Kostenrahmen, Canva-Übergabe, Designkandidat-Auswahl, Edit-/Speichervorschau); für den ersten Piloten werden ausschließlich diese sieben benötigt (Kundenentwurf und Veröffentlichung folgen erst danach); Veröffentlichung bleibt strukturell unerreichbar (`publicationApprovalStatus` kann in Phase C durch keine Funktion auf `APPROVED` gesetzt werden, Standard bleibt `PUBLICATION_NOT_APPROVED`)
- **Kandidaten- und Editing-Transaktionslogik korrekt gespiegelt:** ein generierter Designkandidat (`GENERATE_DESIGN_CANDIDATES`) ist kein dauerhaftes Design; erst eine getrennte Kandidatenauswahl mit eigenem Token (`APPROVE_CANVA_CANDIDATE`) erlaubt `CREATE_SELECTED_CANDIDATE`; eine Editing-Vorschau (`PREVIEW_READY`) ist nicht gespeichert, Speichern erfordert eine eigene Freigabe (`APPROVE_CANVA_SAVE`), ohne Commit geht der Entwurf verloren; im ersten realen Pilot sind ausschließlich `GENERATE_DESIGN_CANDIDATES`/`CREATE_SELECTED_CANDIDATE` vorgesehen, Brand-Template-/Edit-Aktionen sind nur modelliert und getestet, nicht im ersten Pilot ausgeführt
- **API:** additiv 3 neue GET-Routen (`/api/v71/canva/status`, `/api/v71/canva/job-packages` inkl. Einzelabruf, `/api/v71/canva/backup/export`) und 16 neue POST-Routen (Paket vorbereiten/validieren, vier getrennte Freigaben, Kostenpaketstatus setzen, Hand-off-Token anfordern/einlösen, Kandidaten-Token anfordern/einlösen, Ergebnis-Token anfordern/einlösen, Kundenentwurf freigeben/Änderungen anfordern, Restore-Vorschau); insgesamt jetzt **63 GET und 38 POST**; weiterhin keine Route für Canva-Login, API-Key-Speicherung, automatische Veröffentlichung, öffentliche Freigabelinks, Kunden-/Team-Einladung, Designlöschung, Kontoverwaltung, Creditkauf oder Abrechnung
- **UI:** neuer additiver Chef-Modus-Bereich „Canva-Designpilot · Testmandant“ mit klaren Status-Badges (kontrollierte Übergabe, externe Verarbeitung, Testmandant, nicht abrechenbarer Pilot, noch nicht veröffentlicht, Canva-Konto intern, kein Canva-Zugang), editierbarer Kunde-/Marke-/Kampagne-Auswahl (Testmandantenbasis), getrennten Freigabe-Buttons (keine Sammelfreigabe) und einklappbaren technischen Details; Status Kandidat/Entwurf/Vorschau/gespeichert sind textlich klar unterscheidbar; kein Button „Veröffentlichen“, „Öffentlich teilen“, „Kunden zu Canva einladen“, „Design löschen“ oder „Credits kaufen“
- **Tests:** 236 neue automatisierte Prüfpunkte in 8 neuen Testdateien (`canva-design-job-package.test.js` 47, `canva-design-result.test.js` 24, `canva-connector.test.js` 39, `canva-store.test.js` 10, `canva-backup.test.js` 20, `server-v71-canva-routes.test.js` 47, `canva-ui.test.js` 24, `canva-phase-c-integration.test.js` 25); gesamt **1165 automatisierte Prüfpunkte grün** (`npm test`, Exit-Code 0, 43 Testdateien; `npm run check`, Exit-Code 0)
- **Trockenlauf bestanden:** lokaler, nicht-externer Café-Test (fiktives Café, Instagram-Post, deutsche Sonntagsfrühstück-Botschaft, `NOT_BILLABLE_TEST`) durchlaufen – Paket `DRAFT` → Inhaltsprüfung `READY_FOR_REVIEW` → vier getrennte Freigaben → Hand-off-Token → minimales Hand-off-Payload erzeugt → Status `HANDED_OFF`; keine Canva-Netzwerkverbindung, keine Kosten, keine Veröffentlichung, kein öffentlicher Link, kein Kunde eingeladen, keine automatische Ausführung
- **Browser-/Mobile-Abnahme bestanden:** eigener isolierter Testserver (Port 4199), vollständiger Durchlauf im echten Browser (Kunde/Marke/Kampagne wählen → Paket vorbereiten → Inhalt prüfen → vier getrennte Freigaben → Canva-Übergabe freigeben → Status „übergeben (Ausführung noch nicht bestätigt)“) ohne Konsolenfehler; Mobile 390×844 ohne horizontalen Überlauf (`innerWidth === scrollWidth === 390`), Status-Badges und Formular vollständig bedienbar, keine Textwand. Testserver anschließend beendet, Testauftragspaket aus dem App-Support-Speicher wieder entfernt
- **Nicht enthalten – weiterhin offen und erfordert separate Jamal-Freigabe:** echte Canva-Designerstellung, jede externe Übertragung, jede Kostenübernahme, jede Veröffentlichung, Brand-Template-/Edit-Transaktion im realen Pilot, vollständiges Marketing-Agentur-Gesamtsystem, Shopify-Anbindung

## V7.1 Phase B.1 – HeyGen-Pilot abgeschlossen, Agenturbetrieb mandantenfähig vorbereitet (umgesetzt, getestet und gesichert mit Commit `37e8a28`)

**Committed und gepusht mit Commit `37e8a28` (HEAD und `origin/main` stehen exakt darauf). Ausdrücklich weiterhin kein Deployment, kein weiterer echter HeyGen-Render, keine Veröffentlichung, keine produktive Kundenanlage, keine Canva-/Shopify-Änderung.** Der erste, bereits extern ausgeführte reale HeyGen-Pilot (fiktives Café, öffentlicher Avatar, 9:16, HeyGen-Video-Agent-Session `af66e85f550542b0a5bfcef0fc2939e8`) wurde strukturiert als **kanonisches Pilot-Review** dokumentiert und die Unternehmenszentrale erhielt additiv die sichere Grundlage für einen späteren mandantenfähigen Marketing-Agenturbetrieb. Die Unternehmenszentrale hat diesen externen Connector-Lauf **nicht selbst gestartet**; sie dokumentiert ihn nachträglich, ehrlich und ohne erfundene Werte (Kosten/Credits/Renderdauer/dauerhafte URL bleiben `UNKNOWN`). Kunden erhalten weiterhin **keinen** Zugang zum persönlichen oder zentralen HeyGen-Konto.

- **Neue Module:** `agency-tenant-registry.js` (kanonische Mandantenbasis: zwei neutrale Testkunden, Marken, Kampagnen; strikte Mandantenbindung/-trennung, keine echten Kundendaten), `heygen-pilot-review.js` (dokumentiert den einen realen Pilot strikt getrennt nach Providerstatus vs. lokaler Verifikation; `jamalDecision` niemals automatisch `APPROVED_FOR_CUSTOMER_USE`), `heygen-result-lifecycle.js` (Ergebnisrückführungs-Statuskette `PROVIDER_PROCESSING → … → PUBLICATION_NOT_APPROVED`; `PUBLISHED` bleibt strukturell unerreichbar), `agency-backup.js` (Backup/Restore-Vorschau ausschließlich für Mandanten-/Pilot-Metadaten)
- **`heygen-job-package.js` additiv erweitert:** `customerId`/`brandId`/`campaignId` sind jetzt verpflichtend und gegen die Mandantenbasis geprüft; neue Felder `providerFolderReference` (immer `PLANNED_NOT_CREATED`), `billableUnit`, `customerPackageId`, `costPackageStatus` (`INCLUDED_IN_PACKAGE`/`ADDITIONAL_APPROVAL_REQUIRED`/`UNKNOWN`/`NOT_BILLABLE_TEST`) und eine **fünfte, getrennte Freigabestufe** `customerDraftApprovalStatus` (Kundenentwurf, ausdrücklich nicht gleich Veröffentlichung)
- **`heygen-store.js` additiv erweitert:** ein bereits gespeichertes Jobpaket kann nicht mehr nachträglich einem anderen Kunden/einer anderen Marke zugeordnet werden (Persistenzgrenze); Ergebnisrückgaben übernehmen `customerId`/`brandId`/`campaignId`/`projectId` ausschließlich vom zugehörigen Jobpaket (nie vom Aufrufer); `listPackages`/`listResults` filtern optional nach `customerId`; neue Ablage für Lifecycle-Datensätze
- **Mandantentrennung technisch durchgesetzt:** Kunde A kann keine Referenz/kein Jobpaket von Kunde B lesen (API liefert bei `customerId`-Mismatch bewusst `404`, nicht `403`, um keine Existenz preiszugeben); Backup/Restore wahren die Zuordnung; unbekannte oder nicht zusammengehörige IDs blockieren strukturell
- **API:** additiv 5 neue GET-Routen (`/api/v71/agency/customers`, `/brands`, `/campaigns`, `/pilot-review`, `/backup/export`) und 5 neue POST-Routen (`heygen/job-package/approve-customer-draft`, `/request-customer-draft-changes`, `/set-cost-package-status`, `heygen/result-lifecycle/advance`, `agency/backup/restore-preview`); insgesamt jetzt **60 GET und 22 POST**; weiterhin keine Route für HeyGen-Kundenzugang, Credentialausgabe, automatischen Render, automatische Veröffentlichung oder Abrechnung
- **UI:** additiver Bereich „HeyGen-Agenturbetrieb · Testmandant“ im bestehenden HeyGen-Pilotbereich – zeigt Kunde/Marke/Kampagne/Projekt, Providerstatus, internen Status, Kostenstatus, Kundenfreigabe, Veröffentlichungsstatus und den nächsten Jamal-Schritt je Jobpaket-Karte; klar gekennzeichnet „Testmandant“, „kein echtes Kundenportal“, „Kunde hat keinen HeyGen-Zugang“, „Providerkonto bleibt intern“, „Video noch nicht veröffentlicht“, „erster Pilot ist nicht abrechenbar“; weiterhin kein Veröffentlichungsbutton, keine öffentliche Anmeldung, keine Kunden-E-Mail-Einladung
- **Tests:** 5 neue Testdateien (`agency-tenant-registry.test.js` 17, `heygen-pilot-review.test.js` 17, `heygen-result-lifecycle.test.js` 16, `heygen-store.test.js` 10, `agency-backup.test.js` 8) plus erweiterte Prüfpunkte in `heygen-job-package.test.js`, `heygen-connector.test.js`, `heygen-backup.test.js`, `server-v71-heygen-routes.test.js`, `heygen-ui.test.js`, `heygen-phase-b-integration.test.js`, `server-http-router.test.js`, `daily-work-run.test.js`, `agent-runtime.test.js`; gesamt **929 automatisierte Prüfpunkte grün** (`npm test`, Exit-Code 0, 35 Testdateien; `npm run check`, Exit-Code 0)
- **Nicht enthalten – weiterhin offen und erfordert separate Jamal-Freigabe:** weiterer echter HeyGen-Render, jede externe Übertragung, jede Kostenübernahme, jede Veröffentlichung, echte Kundenanmeldung/echtes Kundenportal, produktive Kundenanlage, Canva-Connector, vollständiges Marketing-Agentur-Gesamtsystem, Phase C

## V7.1 Phase B – HeyGen als erster kontrollierter Medien-Connector (umgesetzt, getestet und gesichert mit Commit `ff43089`)

**Committed und gepusht mit Commit `ff43089`. Ausdrücklich weiterhin kein Deployment, kein echter HeyGen-Render, keine externe Übertragung, keine Kosten, keine Veröffentlichung.** V7.0 bleibt `FROZEN`. V7.1 Phase A bleibt unverändert grün. Architektur-Entscheidung: HeyGen ist ein `CONTROLLED_CONNECTOR_HANDOFF` – die Zentrale erstellt und validiert lokal ein Auftragspaket; die tatsächliche HeyGen-Aktion würde, falls überhaupt, über einen externen, authentifizierten HeyGen-Connector außerhalb dieses Servers laufen. Kein API-Key wird gespeichert. HeyGen bleibt in der kanonischen Basiswahrheit (`tool-registry.js`, `plugin-gateway.js`) weiterhin `NOT_CONNECTED` / `RECOMMENDATION_ONLY`; ein additiver, separater Pilotstatus (`heygen-connector.js#buildHeygenPilotStatus`) zeigt zusätzlich `PARTIALLY_CONNECTED` / `CONTROLLED_HANDOFF`, niemals `DIRECT` oder autonom.

- **Neue Module:** `heygen-job-package.js` (Auftragsmodell, Capability-Profil, Datenschutz-/Rechteprüfung, Freigabestufen), `heygen-connector.js` (kontrollierter Adapter, minimales Hand-off-Payload, Tokenbindung über die bestehende `execution-bridge.js`-Tokenarchitektur, Pilotstatus), `heygen-job-result.js` (strukturierte Ergebnisrückführung, URL-Validierung), `heygen-store.js` (lokale Metadaten-Persistenz unter App Support), `heygen-backup.js` (Metadaten-Export/Restore-Vorschau ohne Originaldateien/Credentials)
- **Vier getrennte Freigabestufen** (Inhalt, externe Übertragung, Kostenrahmen, Veröffentlichung); für den ersten Piloten werden nur die ersten drei benötigt; Veröffentlichung bleibt strukturell unerreichbar (`publicationApproved` kann in Phase B durch keine Funktion auf `true` gesetzt werden)
- **API:** additiv 3 neue GET-Routen (`/api/v71/heygen/status`, `/api/v71/heygen/job-packages` inkl. Einzelabruf, `/api/v71/heygen/backup/export`) und 10 neue POST-Routen (Paket vorbereiten/validieren, drei getrennte Freigaben, Hand-off-Token anfordern/einlösen, Ergebnis-Token anfordern/einlösen, Restore-Vorschau); insgesamt 55 GET und 17 POST; keine Route für API-Key-Speicherung, Login, Löschen, Kauf, Upgrade oder automatische Veröffentlichung
- **UI:** neuer additiver Chef-Modus-Bereich „HeyGen-Pilot“ mit klaren Status-Badges (kontrollierte Übergabe, externe Verarbeitung, kostenpflichtig möglich, noch nicht veröffentlicht, kein automatischer Start, erster Pilot), getrennten Freigabe-Buttons (keine Sammelfreigabe) und einklappbaren technischen Details; kein Button „Veröffentlichen“, „Video löschen“, „Avatar/Voice klonen“ oder „Credits kaufen“
- **Tests:** 144 neue automatisierte Prüfpunkte in 7 neuen Testdateien (`heygen-job-package.test.js` 29, `heygen-connector.test.js` 18, `heygen-job-result.test.js` 16, `heygen-backup.test.js` 15, `server-v71-heygen-routes.test.js` 28, `heygen-ui.test.js` 17, `heygen-phase-b-integration.test.js` 21); gesamt **816 automatisierte Prüfpunkte grün** (`npm test`, Exit-Code 0, 30 Testdateien; `npm run check`, Exit-Code 0)
- **Korrigierte Fehleinschätzung aus der Vorprüfung:** Ein erster `npm test`-Lauf brach scheinbar an einer Baseline-Prüfung der Execution Bridge gegen das (vorschriftsgemäß dirty) Fixture-Repository ab. Die genauere Ursachenprüfung ergab: Das war kein echter Zustandskonflikt im Repository, sondern eine Einschränkung der in diesem Arbeitslauf verwendeten Ausführungsumgebung (eingeschränkter Dateisystemzugriff auf das außerhalb des Projektordners liegende Fixture-Repository). Mit regulären, unbeschränkten Rechten läuft exakt derselbe Test grün durch. Das Fixture-Repository selbst blieb während der gesamten Prüfung im vorgeschriebenen, bekannten Dirty-Zustand (`M FIXTURE_CALC.js`) unverändert
- **Trockenlauf bestanden:** lokaler, nicht-externer Café-Test (Platzhalterinhalt) durchlaufen – Paket `DRAFT` → Inhaltsprüfung `READY_FOR_REVIEW` → drei getrennte Freigaben → `APPROVED_FOR_HANDOFF` → Hand-off-Payload erzeugt → Status `HANDED_OFF`; keine HeyGen-Netzwerkverbindung, keine Kosten, keine Veröffentlichung, keine automatische Ausführung; Token-Zweitnutzung wurde erwartungsgemäß abgelehnt
- **Browser-/Mobile-Abnahme bestanden:** eigener isolierter Testserver (Port 4599), vollständiger Durchlauf im echten Browser (Paket vorbereiten → Inhalt prüfen → drei getrennte Freigaben → Hand-off freigeben → Status „übergeben (Ausführung noch nicht bestätigt)“) ohne Konsolenfehler und ohne jede externe Netzwerkanfrage (`performance.getEntriesByType('resource')` zeigte ausschließlich `127.0.0.1:4599`); Mobile 390×844 ohne horizontalen Überlauf, Status-Badges und Formular vollständig bedienbar, keine Textwand. Testserver anschließend beendet, Testauftragspaket aus dem App-Support-Speicher wieder entfernt
- **Nicht enthalten – weiterhin offen und erfordert separate Jamal-Freigabe:** echter HeyGen-Renderlauf, jede externe Übertragung, jede Kostenübernahme, jede Veröffentlichung, direkter HeyGen-API-Adapter im Node-Server, Canva-Connector, Marketing-Agentur-Gesamtsystem

## V7.1 Phase A – Dokumenten-/Wissenseingang, Werkzeug-/Lizenzregister, Plugin-Gateway (umgesetzt, getestet und gesichert mit Commit `59f985f`)

**Committed und gepusht mit Commit `59f985f`. Ausdrücklich weiterhin kein Deployment.** V7.0 bleibt unverändert `FROZEN` auf `15ce8bb`. V7.1 Phase A wurde lokal umgesetzt, vollständig read-only auditiert und mit 661 automatisierten Prüfpunkten grün getestet (`npm test`, Exit-Code 0, 23 Testdateien; `npm run check`, Exit-Code 0). Browser-/Mobile-Abnahme wurde zusätzlich gegen einen eigenen, isolierten Testserver (Port 4581, eigenes `HOME`) durchgeführt: Dokumentregistrierung, Test-Upload, Traversal-/Unbekannte-Felder-Blockade, Tool-Registry, Plugin-Gateway (Canva/HeyGen/Shopify korrekt nur vorgemerkt), deterministisches Tool-Routing (Beispiel: „Code-Ausführung" → Codex/DIRECT, Fallback Cursor, Jamal-Freigabe erforderlich) und Backup-Export/Import-Preview funktionieren wie spezifiziert; mobile Ansicht (390×844) ohne horizontalen Überlauf (geprüft: `innerWidth === scrollWidth === 390`), Karten und Formulare vollständig bedienbar; keine sichtbaren Fehlertexte im DOM nach Hard-Reload und Durchlauf aller drei neuen Bereiche. Testserver wurde danach beendet, temporäre Testdaten entfernt.

**Nachbesserung nach erster manueller Safari-Abnahme (25.07.2026):** Zwei gemeldete Abschlussfehler wurden mit der kleinstmöglichen Phase-A-Korrektur behoben, ohne die Sicherheits-/Routing-Logik zu ändern:
1. *Checkbox-Layout:* Die allgemeine Formularregel `textarea, input, select { width: 100%; }` bzw. `input, select { min-height: 42px; padding: 8px 10px; }` (`styles.css`) griff ungewollt auch auf `type="checkbox"` und blähte die beiden Checkboxen im Tool-Routing-Formular („externe Übertragung erlaubt“, „Veröffentlichung erlaubt“) zu einer raumfüllenden, umrandeten Fläche auf – in Safari optisch wie „aktiv“ wirkend, Beschriftung gequetscht/abgeschnitten. Korrektur ausschließlich innerhalb von `.v71-checkbox`/`.v71-checkbox-label` (neue, eng begrenzte CSS-Regel, kein globaler Checkbox-Reset); Markup in `v71-ui.js` erhielt zusätzlich ein explizites `for`/`id`-Paar und einen eigenen `<span>` für die Beschriftung. Standardwert beider Checkboxen bleibt unverändert `false` (kein `checked`-Attribut).
2. *Nicht strukturierte Blockierungsantwort:* `plugin-gateway.js` (`recommendToolForTask`) zeigte bei „kein Werkzeug erfüllt alle Grenzen“ (Beispiel: Video-Auftrag, externe Übertragung/Veröffentlichung aus) nur eine Textzeile. Die Funktion liefert bei Blockierung jetzt zusätzlich `status: "BLOCKED"`, `blockedCandidate` (fachlich geeignetes, aber nicht ausführbares Werkzeug samt `connectionStatus`, `missingApprovals`, `costStatus`, `dataClassificationBoundary`, `fallback`), `blockedAlternatives` und `nextAllowedJamalStep`. Es wird weiterhin kein Werkzeug erfunden, empfohlen oder automatisch gestartet; Canva/HeyGen/Shopify bleiben `NOT_CONNECTED`. `v71-ui.js` rendert diese Felder strukturiert. Geänderte Dateien: `plugin-gateway.js`, `v71-ui.js`, `styles.css`; neue Tests: 10 zusätzliche Prüfpunkte in `plugin-gateway.test.js` (jetzt 35) sowie neue Datei `v71-ui.test.js` (12 Prüfpunkte). Automatisiert per `plugin-gateway.test.js` und `v71-ui.test.js` verifiziert (siehe oben, 661 Prüfpunkte gesamt). Die automatisierte Browser-/CDP-Prüfung dieser konkreten Korrektur konnte in diesem Lauf nicht abgeschlossen werden (Browser-MCP-Versuch reagierte nicht innerhalb angemessener Zeit; gemäß Vorgabe kein weiterer Versuch). Beide eigenen, isolierten Testserver dieses Laufs wurden anschließend kontrolliert beendet (Ports frei). Health und Fixture vor/nach dieser Korrektur unverändert. Diese Nachbesserung wurde zusammen mit dem übrigen Phase-A-Stand inzwischen mit Commit `59f985f` gesichert.

- **Neue Module:** `document-registry.js` (Dokumenten-/Wissenseingang), `tool-registry.js` (kanonisches Werkzeug-/Lizenzregister, 21 vorgemerkte Werkzeuge), `plugin-gateway.js` (Plugin-Gateway-Grundmodell + deterministisches Tool-Routing), `v71-registry-backup.js` (additives Backup/Export für die drei neuen Register), `v71-ui.js` (Chef-Modus-UI für alle drei Bereiche)
- **Dokumenten-/Wissenseingang:** ausschließlich sichere Metadaten/Referenzen; Originaldateien nur außerhalb beider Repositories unter `~/Library/Application Support/KI-Unternehmenszentrale/documents/{originals,metadata,previews,quarantine,test-inbox}`; echter Browser-Datei-Upload ist in Phase A bewusst **nicht** produktiv umgesetzt – stattdessen ein isolierter Test-Upload gegen zwei serverseitig erzeugte Fixture-Dateien (`sample-note.txt`, `sample-data.csv`); Klassifizierung NORMAL/SENSITIVE/SECRET; Traversal-, Symlink-, Größen-, Dubletten- und Secret-Dateischutz; kein produktiver Lösch-Endpunkt
- **Werkzeug-/Lizenzregister:** `tool-registry.js` ist die alleinige kanonische Quelle für Werkzeugidentität, Fähigkeiten, Lizenz- und Kostenmetadaten (21 Werkzeuge, alle 14 geforderten Kategorien abgedeckt); eine Lizenz wird niemals automatisch als `CONNECTED` gewertet; keine Zugangsdaten im Register
- **Plugin-Gateway:** `plugin-gateway.js` ist die alleinige kanonische Quelle für Live-/Adapterzustand und Tool-Routing; spiegelt bestehenden Codex-Executor, lokalen read-only Git-Stand („GitHub“) und lokale Airtable-Zugangsdaten-Anwesenheit; Canva, HeyGen und Shopify sind ausschließlich vorgemerkt (`REGISTERED`, `RECOMMENDATION_ONLY`), keine technische Verbindung, kein Produktivlauf; Health-Hardblock für Codex bleibt auch im neuen Gateway wirksam
- **Plugin-Wahrheitsquelle geklärt:** die bestehende, V7.0-eingefrorene `PRODUCTIVE_PLUGIN_REGISTRY` (`server.js`, V6.34.2) bleibt unverändert als reine UI-Textsammlung für den Cockpit-„Plugin-Leitstand"; sie wird von den neuen V7.1-Modulen nicht gelesen, verändert oder dupliziert. Ein Bestandsschutztest (`v71-integration.test.js`) prüft Widerspruchsfreiheit zwischen beiden Quellen
- **API:** additiv genau 5 neue GET-Routen und 3 neue POST-Routen (siehe `API_REGISTER.md`); insgesamt 52 GET- und 7 POST-Routen; alle bestehenden Sicherheitsgrenzen (Host/Origin, Content-Type, Bodylimit, bekannte Feldmengen, keine Stacktraces) wiederverwendet
- **UI:** drei neue additive Chef-Modus-Bereiche „Projektunterlagen & Wissenseingang“, „Werkzeuge & Lizenzen“, „Plugin-Gateway & Tool-Routing“; jeweils klar als „V7.1 Phase A · neu“ gekennzeichnet; Canva/HeyGen/Shopify erscheinen nirgends als produktiv verbunden
- **Bestandsschutz bestätigt:** 25 Agenten, 17 Projekte, Execution Bridge, Guided Work, Team-Editor, V7.0-Freeze (`15ce8bb`) und Health-/Fixture-Repositories unverändert; Health und Fixture ausschließlich read-only referenziert
- **Nicht enthalten – weiterhin offen für Phase V7.1 B:** produktiver Browser-Datei-Upload, echte Canva-/HeyGen-/Shopify-Anbindung, Wissensquellen-Sichtbarkeit direkt in Guided Work UI (Datenmodell und Lesefunktion bestehen bereits, `guided-work-ui.js` bleibt in Phase A bewusst unverändert), Marketing-Agentur, Shopify-Verkaufspfad

## V7.0 offiziell FROZEN – Jamal-Entscheidung vom 2026-07-25

**V7.0 ist mit Jamals ausdrücklicher Entscheidung vom 25.07.2026 offiziell auf `FROZEN` gesetzt.** Basis ist der gesicherte Phase-E-Stand **`52ce012`** (`52ce0125f0d641295bcc1b83ee9442e95abb199d`) auf `origin/main`; Phase A bis E sind damit vollständig abgeschlossen. Der read-only **V7.0-Freeze-Status** (`GET /api/v7-freeze-status`) zeigt diesen Stand jetzt korrekt als `FROZEN` an – ausschließlich weil im Quellcode (`v7-freeze-status.js#MANUAL_FREEZE_DECISION`) genau diese eine, von Hand eingetragene Jamal-Entscheidung hinterlegt ist, niemals automatisch aus Git-Stand oder Testzahl. Es gibt keinen neuen Schreib-Endpunkt, keinen Freeze-/Unfreeze-Button und keinen zweiten Weg, den Status zu ändern.

**Bedeutung:** V7.0 erhält keine neuen Funktionen mehr. Änderungen an V7.0 sind nur noch als belegte Fehlerkorrektur, Sicherheitskorrektur oder Wiederherstellungs-/Betriebsfix zulässig. Neue Funktionen beginnen erst ab **V7.1**: Dokumenten-/Wissenseingang, Werkzeug-/Lizenzregister, Plugin-Gateway, Marketing-Agentur, Canva/HeyGen, Shopify und weitere Umsatzprojekte gehören ausdrücklich **nicht** mehr zu V7.0. Kein Deployment; Health bleibt weiterhin ausschließlich read-only; Codex bleibt weiterhin ausschließlich isoliert gegen das Fixture-Repository begrenzt; Apply bleibt weiterhin getrennt von Commit, Push und Deployment.

## Git- und Versionsstand

- Vorheriger gesicherter Ausgangsstand: **V6.46.0** / Commit **`e611c9c`** (Health Hybrid End-to-End-Pilot)
- **V7.0 Phase A – Guided Work Foundation:** umgesetzt, getestet, browserseitig abgenommen und gesichert mit Commit **`4a74ebe`** auf `origin/main`
- **V7.0 Phase B – Betriebsstabilität:** umgesetzt, getestet, browserseitig abgenommen und gesichert mit Commit **`3487a84`** auf `origin/main`
- **V7.0 Phase C – Execution Bridge Isolation mit Mock-Executor:** umgesetzt, getestet, browserseitig abgenommen und gesichert mit Commit **`0858b4e`** auf `origin/main`
- **V7.0 Phase D – Codex als kontrollierten Executor anbinden:** umgesetzt, getestet, echter Codex-Fixture-Pilot bestanden und gesichert mit Commit **`6553452`** auf `origin/main`
- **V7.0 Phase E – Health-Ende-zu-Ende-Audit, Freeze-Status und Chef-Modus-Klarheit:** umgesetzt, getestet und gesichert mit Commit **`52ce012`** auf `origin/main`
- **V7.0 offiziell FROZEN:** Jamal-Entscheidung vom **2026-07-25** auf Basis von `52ce012`; Phase A bis E abgeschlossen; keine neuen Funktionen mehr in V7.0
- Branch: `main`
- Verbindliche Aussage: Phase A bis E sind gesichert und Jamal hat V7.0 auf dieser Basis offiziell eingefroren; der read-only **V7.0-Freeze-Status** (`GET /api/v7-freeze-status`) zeigt `FROZEN` ausschließlich wegen dieser hinterlegten Jamal-Entscheidung – niemals automatisch aus Git-Stand oder Testzahl allein. **Phase V7.1 ist nicht begonnen** und benötigt eine neue, separate Freigabe
- Einstiegspunkte: `README.md`, `V1_BETRIEBSHANDBUCH.md`

## V7.0 Phase A – Guided Work Foundation (umgesetzt und abgenommen)

- Neuer Tageslauf: `schemaVersion: 2`; v1-Läufe bleiben unverändert lesbar im selben Store
- Module: `guided-work.js` (Domäne), `guided-work-ui.js` (Hauptarbeitsraum); `daily-work-run.js` / `health-hybrid-work.js` bleiben kanonisch
- Geführter Hauptarbeitsraum: „Oben arbeiten. Unten nachschauen.“
- Deterministische, quellenbasierte Arbeitsvorschläge (kein Auto-Start)
- Editierbares Agententeam und `responsibleAgentId` vor Paketfreigabe; Invalidierung + Re-Fingerprint
- Known-dirty-Baseline für Health (read-only Live inkl. relative Pfade/Hashes, keine Dateiinhalte)
- Evidenz-Prefill für Fachbefund/QA/PM als Entwurf, nie als Bestätigung
- V6.46.0-Hybrid-Fallback vollständig erhalten; Roh-JSON standardmäßig geschlossen
- Backup-Schutz bleibt aktiv: Secret-Heuristik unverändert scharf; einzig der bestätigte False Positive bei bloßer `.env`/`.env.local`-Pfadnennung (z. B. in `forbiddenPaths`) wurde gezielt korrigiert, ohne die Erkennung realer Zugangsdaten zu schwächen
- Bekannter, bewusst offener Bedienpunkt: Für `acknowledgeV2Overwrite` (Schutz gegen stilles Überschreiben lokaler v2-Läufe durch einen reinen v1-Import) gibt es noch keine UI-Freigabesteuerung; der sichere Standard („nicht überschreiben“) bleibt dadurch aktiv, ein bewusster Override ist aktuell nur außerhalb der UI möglich
- 322 automatisierte Prüfpunkte grün; vollständige Browser-Abnahme inkl. Mobile 390×844 ohne horizontalen Überlauf bestanden
- **Nicht enthalten – weiterhin offen für spätere Phasen (siehe Phase C/D/E unten):** Codex-/Agentenstart, produktive Repository-Arbeit, Health-Schreiben, Commit/Push/Deployment, Autonomieerhöhung

## V7.0 Phase E – Health-Ende-zu-Ende-Abnahme und Freeze-Kandidat (umgesetzt, getestet und gesichert mit Commit `52ce012`; V7.0 seit 25.07.2026 offiziell FROZEN)

- Ziel war kein neuer Executor, sondern der Nachweis, dass der gesamte V7.0-Ablauf (Fokus → Vorschlag → Team → Baseline → Paket → isolierte Ausführung/Hybrid-Rückführung → Abschluss) stimmig, ehrlich und wiederholbar ist; nur echte Lücken wurden geschlossen
- Neues read-only Modul `v7-freeze-status.js` + Route `GET /api/v7-freeze-status`: Statusmodell `IN_REVIEW | FREEZE_CANDIDATE | FROZEN`, abgeleitet aus Phasenhistorie, aktuellem Git-Commit/Working-Tree und dem zuletzt gemessenen Testlauf; `FROZEN` wird durch dieses Modul **niemals** gesetzt – ausschließlich Jamals separate Entscheidung
- Neue, eingeklappte Freeze-Status-Karte im Guided-Work-Hauptarbeitsraum („unten nachschauen“); keine konkurrierende Primäraktion, kein Button, der den Status verändert
- Sticky-Kopfbereich um „Wer arbeitet daran“ und „Tatsächlich ausgeführt / Evidenz“ ergänzt (liest ausschließlich bereits vorhandene, sicher gefilterte Felder – kein neues Datenmodell)
- Tagesabschluss zeigt jetzt explizit „Ergebnis erreicht/nicht erreicht“, verantwortlichen Agenten und Ausführungs-/Evidenzstand
- Veraltete Oberflächenaussage korrigiert: Der Startkarten-Text behauptete fälschlich „Keine Execution Bridge“, obwohl Mock- und Codex-Fixture-Ausführung seit Phase C/D bereits existieren (Health bleibt davon unberührt: weiterhin read-only, Codex-/Apply-Start für Health bleiben hart blockiert)
- Audit bestätigt: Reload startet nie automatisch einen Attempt neu (Recovery-Fall statt Auto-Resume); `CANCELLED` ist terminal ohne Folgeübergänge; Backup/Restore bleibt vollständig auf `localStorage` beschränkt und startet nie einen Executor
- 47. GET-Route additiv (`/api/v7-freeze-status`); weiterhin keine neue Schreib-Route, keine neue Autonomieerhöhung
- **Nicht enthalten – weiterhin offen für Phase V7.1:** Dokumenten-/Wissenseingang, Werkzeug-/Lizenzregister, Plugin-Gateway, Marketing-Agentur, Canva/HeyGen, Shopify, weitere Umsatzprojekte

## V7.0 Phase D – Codex als kontrollierten Executor anbinden (umgesetzt, getestet, gesichert mit Commit `6553452`)

- Neues Modul `execution-codex-adapter.js`; Codex läuft ausschließlich isoliert, mit `--ask-for-approval never`, gegen das Execution-Bridge-Fixture-Repository – niemals gegen Health oder die Zentrale
- Codex-Start für das Health-Projekt ist serverseitig hart blockiert (Codex-Ausführung und Apply bleiben für Health verboten)
- Erweiterte Executor-Registry (`GET /api/execution/executors`) weist Mock und Codex mit Verfügbarkeit/Autorisierung aus; genau ein primärer Button je Zustand in der UI
- Echter Codex-Fixture-Pilot bestanden: Timeout auf `150000ms` korrigiert (`DEFAULT_CODEX_ATTEMPT_TIMEOUT_MS`), Prozessführung auf `spawn()` mit `stdio: ["ignore","pipe","pipe"]` umgestellt (behebt reales Hängenbleiben von `execFile` bei offenem stdin)
- **Nicht enthalten – weiterhin offen für Phase E:** Health-E2E-Gesamtaudit, Freeze-Status, produktive Codex-/Apply-Freigabe für Health, Commit/Push/Deployment, Autonomieerhöhung

## V7.0 Phase C – Execution Bridge Isolation mit Mock-Executor (umgesetzt, getestet und browserseitig abgenommen, gesichert mit Commit `0858b4e`)

- Eigenständige Module `execution-bridge.js` und `execution-mock-adapter.js`; Tageslauf-, Hybrid- und Guided-Work-Module bleiben kanonisch
- Isolierter Workspace ausschließlich unter `~/Library/Application Support/KI-Unternehmenszentrale/workspaces/` – außerhalb Zentrale und Health
- Deterministischer Mock-Executor (keine KI, kein Netzwerk, kein `shell: true`); klar gekennzeichnet: „Deterministischer Mock-Executor – technische Sicherheitsprüfung, keine KI-Ausführung.“
- Attempt-Statusmaschine: `PREPARED | APPROVED | QUEUED | RUNNING | SUCCEEDED | FAILED | BLOCKED | CANCELLED | TIMED_OUT`
- Apply-Statusmaschine getrennt: `NOT_REQUESTED | APPLY_REVIEW | APPLY_APPROVED | APPLIED | APPLY_DECLINED | APPLY_FAILED | STALE`
- Apply nur gegen Fixture-Repository; Health-Apply in Phase C hart blockiert; Apply ist kein Commit, kein Push, kein Deployment
- Additive localhost-Routen: POST prepare/start/cancel/apply, GET status/result; One-Time-Token nur im Server-RAM; Host-/Origin-/Content-Type-/Bodygrößenprüfung; keine CORS-Öffnung
- Locks, Attempts, Audit und Workspaces außerhalb der Repositories; Phase-B-Controller und Serverstatus bleiben erhalten
- **Nicht enthalten – weiterhin offen für Phase D und Phase E:** Codex-/Cursor-Agentenstart, KI-Ausführung, produktive Health-Apply-Freigabe, Commit/Push/Deployment, Autonomieerhöhung

## V7.0 Phase B – Betriebsstabilität (umgesetzt, getestet und browserseitig abgenommen, gesichert mit Commit `3487a84`)

- Separater lokaler Controller `scripts/zentral-ctl.js` (nicht Teil des laufenden App-Servers) mit `status`, `start`, `stop`, `restart`; npm-Skripte `central:start`, `central:status`, `central:stop`, `central:restart`; `npm start` bleibt einfacher manueller Fallback
- Sicherheitsregeln: verwaltet ausschließlich selbst gestartete Prozesse; beendet nie einen fremden Prozess nur wegen Portbelegung; prüft PID-Existenz, erwartete Node-Anwendung, Projektpfad-Fingerprint und Startnachweis vor `stop`/`restart`; erkennt stale Statusdateien; SIGTERM mit Timeout, kein automatisches `kill -9`; feste argv-Arrays, kein `shell: true`
- App-Support-Verzeichnis `~/Library/Application Support/KI-Unternehmenszentrale/server/` außerhalb beider Repositories; atomare Statusdatei (tmp + rename), Größenbegrenzung, keine Secrets/Env/Browserdaten/Prompts
- Neues Domänenmodul `server-status.js`: unveränderliche Start-Momentaufnahme (App-Version, Git-Commit, Startzeit, Port), Statusmodell `RUNNING | STOPPED | STALE | PORT_CONFLICT | VERSION_MISMATCH | UNKNOWN`, `UNKNOWN` statt erfundener Versionsaussage bei nicht lesbarem Git-Stand
- Neue read-only Route `GET /api/server-status` (Route 43 von 43); andere Methoden bleiben 405; keine vollständigen Pfade, keine Secrets, `writeOperationsBlocked: true`, `madeExternalRequest: false`
- Kompakte Statusanzeige im Guided Work Surface (Port, Version, kurzer Commit, Startzeit, Status); technische Details (PID, vollständiger Commit, Controllerdetails) standardmäßig geschlossen; genau ein sicherer nächster Schritt bei Problemen; **kein** Start-/Stop-/Restart-Button im Browser
- Portstrategie: Standard `4173`, kein automatischer Portwechsel, Konflikt wird klar gemeldet, alternativer Port nur explizit über `--port`
- 32 neue automatisierte Prüfpunkte (`server-status.test.js`: 18, `zentral-ctl.test.js`: 14) plus erweiterte Prüfpunkte in `server-http-router.test.js`, `guided-work.test.js`, `daily-work-run.test.js`, `agent-runtime.test.js`; gesamt **384 automatisierte Prüfpunkte grün** (`npm test`, Exit-Code 0)
- Vollständige Browser-Abnahme bestanden: realer Controller-Lifecycle (`start`/`status`/`restart`/`stop`) gegen einen tatsächlich laufenden Server, echter Portkonflikt auf 4173 gegen einen fremden, unveränderten Prozess ohne Kill, Statusanzeige inkl. Reload-Persistenz, Mobile 390×844 ohne horizontalen Überlauf, Phase-A-Hauptfluss weiterhin vollständig nutzbar
- **Nicht enthalten – weiterhin offen für Phase D und Phase E:** Codex-/Cursor-Agentenstart, produktive Repository-Arbeit, Health-Schreibaktion, automatisches Commit/Push/Deployment, Autonomieerhöhung

## V6.46.0 – vorheriger gesicherter Ausgangsstand

- Hybrid-Pilot nur für **Health Upgrade Kompass**
- Die Unternehmenszentrale liest den lokalen Health-Repository-Status ausschließlich read-only (Branch, HEAD, Working-Tree)
- Cursor/Codex arbeitet weiterhin außerhalb der Unternehmenszentrale
- Die Zentrale startet keinen Cursor-, Codex-, KI-, Test- oder Git-Schreibprozess
- Identitätsgesichertes Auftrags-/Grenzpaket; strukturierte Rückführung als `externalExecutionEvidence`
- Externe Evidenz ist kein automatisch bestätigter Fachbefund
- QA, Projektmanager-Zusammenführung und Jamals Abschlussentscheidung bleiben getrennt
- Commit-, Push-, Deployment- und Außenwirkungs-Gates sind ausschließlich Entscheidungen und führen nichts aus
- Der V6.46.0-Evidenz-Deadlock ist behoben; bestehende betroffene WIP-Läufe werden defensiv geheilt
- Andere Projekte erhalten noch keinen Health-Live- oder Ausführungspaketpfad
- Vollständige Browser-End-to-End-Abnahme bestanden; gesichert mit Commit `e611c9c`

## V6.45.2 – vorheriger gesicherter Ausgangsstand

- Runtime-Pilot-Annahme und finale Projektmanager-Zusammenführung sind entkoppelt
- `runtimePilotEvidence` speichert die Pilotübernahme getrennt vom Orchestrierungsstatus
- Lead-Arbeitskarte wird durch Pilotannahme nicht mehr stillschweigend als finale Zusammenführung markiert
- Gesichert mit Commit `fb9aa0d` auf `origin/main`

## V6.45.0 – vorheriger Funktionsstand (Historie)

- Projektmanager-Agent führt durchgehend; QS-/Test-Agent bleibt über `approvalAgentId` separat
- Normale Health-Tagesläufe mit höchstens fünf Kernagenten plus begründeten Zusatzrollen
- „Prüfphase vorbereiten“ als primäre Aktion nach Vorschlagserstellung
- Autonomie- und Sicherheitsgrenzen unverändert

## V6.44.1 – vorheriger Funktionsstand

- Reine Synchronisierung der kanonischen technischen Health-Momentaufnahme; keine neue Produktfunktion.
- Health vorher: `bc98b5c` → jetzt: `28cdcf7` (PR #1 gemergt, Arbeitscommit `8eadc46`).
- Health Remote-Baseline `baseline/private-health-expansion-2026-07-11`: `28cdcf7`; `origin/main` bewusst `1f4f96d`.
- Health Tests/Build grün inkl. Preview-Demodaten und Check-Datum; Verifizierung `2026-07-19`.
- Expansion teilt die gemeinsamen technischen Git-Referenzen, bleibt **PLANUNG** ohne fachliche/regulatorische Freigabe.
- Keine neue Route, kein neuer Speicher, keine Runtime-Änderung, keine Autonomieerhöhung, keine Außenwirkung.
- Keine medizinische, fachliche, rechtliche oder regulatorische Freigabe.

## V6.44.0 – gesicherter Ausgangsstand (Historie)

- V1-Betriebsfreeze ohne neue Produktfunktion, ohne neue Runtime und ohne Autonomieerhöhung.
- `README.md` und `V1_BETRIEBSHANDBUCH.md` sind die verbindlichen Einstiegspunkte für Start, Backup, Sicherheitsgrenzen und Fehlerfall.
- Kompakter V1-Betriebshinweis in der Oberfläche: lokal fertig, Betrieb auf diesem Mac, Daten im Browser, Außenwirkung blockiert, nächster Schritt klar.
- Gesichert mit Commit `b2f618e` auf `origin/main`.

## V6.43.1 – gesicherter Ausgangsstand (Historie)

- V6.43.0 ist committed und gepusht; V6.43.1 schließt Dokumentation, Agentenbezeichnung und Abnahme ohne neue Ausführung ab.
- Runtime-Infrastruktur in `agent-runtime.js`; Tests in `agent-runtime.test.js`.
- Pilot-Agent: sichtbar **Projektmanager-Agent**; kanonische technische ID **`orchestrator-agent`** (über `ROLE_NAME_MAPPING`, keine neue ID).
- Runtime-Pilot nur für **Health Upgrade Kompass** mit vorbereiteter Agenten-Prüfphase und Projektmanager-Arbeitskarte.
- Erster Executor: `LOCAL_DETERMINISTIC_PILOT` – keine externe KI, kein Plugin, kein Netzwerk, kein Dateischreiben.
- Jamal-Freigabe vor Start; separate Jamal-Annahme vor Ergebnisübernahme; kein automatischer weiterer Executor.
- Persistenz additiv unter `agentRuntimePilot` im bestehenden Tageslauf (`schemaVersion: 1`, Schlüssel `ki-unternehmenszentrale-daily-work-runs-v1`).
- Gesichert mit Commit `16bbf45` auf `origin/main`.

## V6.43.0 – gesicherter Ausgangsstand (Historie)

- `agent-runtime.js` implementiert Snapshot, Fingerprint, Jamal-Freigabe, Statusmaschine, lokalen deterministischen Pilot-Executor, Timeout, Abbruch, Audit und Ergebnisprüfung.
- UI-Integration zurückhaltend in der bestehenden Agenten-Prüfphase; separater Start, keine automatische Ergebnisübernahme.
- Gesichert mit Commit `daa96e9` auf `origin/main`.

## V6.42.1 – gesicherter Ausgangsstand (Historie)

- `server-http-router.js` enthält HTTP-Dispatch, Methodenprüfung, statische Asset-Auslieferung, 404/405 und kontrollierte interne Fehlergrenzen.
- `server.js` bleibt für Serverstart, Konfiguration, Handler, Antwortdaten und die explizite Übergabe von `getRoutes`, `routePrefixHandlers` und `staticAssets` zuständig.
- `project-registry.js` bleibt kanonische Projektquelle; `agent-registry.js` bleibt kanonische Agentenquelle.
- Keine neue Produktfunktion, keine Verhaltensänderung, keine neue Route, keine Schreib-API.
- 17 Projekte, 25 Agenten, 41 GET-Routen, 8 freigegebene statische Pfade und alle Ausführungsverbote bleiben erhalten.
- Nächster geplanter Schritt nach V6.43.0: Runtime-Pilot abnahmefest abschließen (V6.43.1).

## V6.42.0 – gesicherter Ausgangsstand (Historie)

- `daily-work-run-ui.js` enthält die komplette Tageslauf-Präsentations- und Bedienlogik aus dem bisherigen `app.js`-Monolithen.
- `app.js` initialisiert das UI-Modul über `DailyWorkRunUi.init(...)` und ruft `DailyWorkRunUi.render()` aus `renderAll()` sowie nach Register-Refresh auf.
- `daily-work-run.js` bleibt Domänen- und Persistenzmodul; `local-data-backup.js` bleibt Datensicherungsmodul.
- Keine neue Produktfunktion, keine Verhaltensänderung, keine neue Vorbereitungskarte, keine Migration.
- Script-Reihenfolge: `agent-registry.js` → `daily-work-run.js` → `local-data-backup.js` → `daily-work-run-ui.js` → `app.js`.
- 17 Projekte, 25 Agenten, 41 GET-Routen, beide localStorage-Schlüssel und alle Ausführungsverbote bleiben erhalten.

## V6.41.0 – gesicherter Ausgangsstand

Siehe ältere Statusdokumentation und Git-Historie für Detailstände vor dem UI-Modul-Split.

## Bekannte technische Altlasten

- sehr große Monolithen: `app.js`, `server.js` und `styles.css` (Tageslauf-UI seit V6.42.0 ausgelagert)
- historische Register-, Status- und Sicherheitsstrukturen bleiben in Frontend und Server sichtbar; technische Projektverifizierung stammt ausschließlich aus `project-registry.js`
- zahlreiche historische Versionsschichten im laufenden Code
- uneinheitliche Agenten- und Projektnamen
- fehlende zentrale Daten- und API-Spezifikation vor dieser Dokumentation
- reale HTTPS-Fähigkeit einzelner Airtable-Read-only-Pfade bei gesetzter Freigabe

## Bekannte Versionswidersprüche

- Neuer Tageslaufstand V6.40.3, historische Cockpit- und Modulkennzeichnungen teilweise V6.38.x oder älter.
- Zwei Commits tragen V6.37.0; V6.37.2 erscheint in der Historie vor V6.37.1.
- Viele historische V4.x-, V5.x- und V6.x-Bezeichnungen bleiben parallel sichtbar.

## Genau ein empfohlener nächster Produktentwicklungsschritt

V7.0 ist mit Jamals Entscheidung vom 25.07.2026 offiziell FROZEN. Der nächste Schritt ist ausschließlich eine neue, separate Freigabe durch Jamal für den Start von V7.1. Der geplante Pfad nach V7.0 (nur Reihenfolge, keine Umsetzung ohne separate Freigabe): Dokumenten-/Wissenseingang, Werkzeug-/Lizenzregister, Plugin-Gateway, Marketing-Agentur, Canva/HeyGen, Shopify, weitere Umsatzprojekte. Bis zu dieser Freigabe sind an V7.0 nur belegte Fehler-, Sicherheits- oder Betriebsfixes zulässig. Keine Deployment-, V2- oder Außenwirkungsentscheidung aus diesem Stand ableiten.

## Bekannte Widersprüche

Die Gesamtversion und sichtbare Teilversionen stimmen nicht überall überein.

## Noch zu normalisieren

Historische Versionskennzeichnungen, Namensvarianten und nicht-kanonische Altregister, ohne sie in diesem begrenzten Schritt zu entfernen.

## Entscheidung durch Jamal erforderlich

Jede spätere Deployment-, V2- oder Außenwirkungsentscheidung sowie jede Ausweitung des Hybrid-Pfads auf andere Projekte.
